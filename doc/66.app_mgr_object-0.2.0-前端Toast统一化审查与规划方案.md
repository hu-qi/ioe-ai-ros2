# 66. app_mgr_object-0.2.0 前端 Toast 统一化审查与规划方案

> 状态：**审查完成 / 方案待审核（本次未修改任何代码）**
> 工程：`app_mgr_object-0.2.0(标准版)`
> 日期：2026-09-01
> 依据：既有《前端统一弹窗与Toast提示框-方案.md》实施结果 + 本轮全量实勘

---

## 1. 需求

前端 toast 提示统一符合设计 UI 规划，**不出现原生 web 弹窗（alert/confirm/prompt）**，也不允许页面私有的第二套 toast；全部页面（index/monitor/control/alarms/calibration/test）走同一套 toast + 弹窗组件。

---

## 2. 现状审查结论

### 2.1 统一组件（已建立，符合 UI 规划）

| 组件 | 位置 | 说明 |
|---|---|---|
| 全局 Toast | base.html:113-123 `showToast(message, type)` + `#toast-container`（base.html:66） | 深色 `.toast-item`（custom.css:526-540）：四色左边框（success/error/warning/info）、`toast-in` 动画（L561）、浅色主题适配（L678）、5s 自动消失 + 手动关闭 |
| 全局弹窗 | base.html:68-87 `#app-dialog` + `AppDialog.confirm/alert/prompt`（Promise 风格，L154-216） | 深色 modal，复用 CSS 变量；`showResultModal` 已全局化（L221-225） |
| 使用方 | dashboard.js（约 50+ 处 showToast + AppDialog）、control.js（6 处 showToast）、alarms.html（AppDialog/showResultModal）、control.html（部分 AppDialog/showToast） | 主链路已统一 |

### 2.2 遗留问题（本轮实勘发现）

| 编号 | 问题 | 证据 | 影响 |
|---|---|---|---|
| T-01 | **control.html 私有第二套 toast `showNotification(title, message, type)`**（Bootstrap alert 实现：fixed 定位、emoji 图标、`alert alert-${type}` 类） | control.html:2162 定义；**约 20 处激活调用**（L675/818/915/1063/1081/1086/1090/1502/1783/1881/1947/2025/2126/2159/2216/2245/2307） | 样式/位置/行为与统一 toast 不一致（双体系并存） |
| T-02 | **alarms.html 调用了未定义的 `showNotification`**（4 处：L449/472/520/538） | showNotification 仅 control.html 内定义，alarms 页面无定义 | 成功回调触发即 `ReferenceError`（与当年 showResultModal 同款隐患） |
| T-03 | 原生 alert/confirm/prompt | 全 JS 零残留；templates 仅 base.html:157 注释中的 API 文档字样（无害） | 已达标，需保持约束 |
| T-04 | 校验/规范化缺失 | 无 grep 门禁 | 未来新增代码可能再次引入原生弹窗/私有通知 |

### 2.3 各页面使用统计

| 页面 | showToast（统一） | AppDialog（统一） | showNotification（私有） | 原生弹窗 |
|---|---|---|---|---|
| index（dashboard.js） | 约 50 处 | confirm/alert/prompt | 0 | 0 |
| control（control.html/control.js） | 6 处（control.js） | 部分 | **约 20 处** | 0 |
| alarms | 0 | confirm/alert + showResultModal | **4 处（未定义）** | 0 |
| monitor / calibration / test | 0 | 0 | 0 | 0 |

---

## 3. 统一规划

### 3.1 统一 API（唯一入口）

- **Toast**：全局 `showToast(message, type, title?)`（扩展可选标题，兼容现有两参调用）——渲染：

```js
function showToast(message, type, title) {
    type = type || 'info';
    var icons = { success: 'fa-check-circle', error: 'fa-times-circle',
                  warning: 'fa-exclamation-triangle', info: 'fa-info-circle' };
    var toast = document.createElement('div');
    toast.className = 'toast-item ' + type;
    toast.innerHTML =
        (title ? '<div class="toast-title">' + title + '</div>' : '') +
        '<i class="fas ' + (icons[type] || icons.info) + '"></i> ' + message +
        '<span class="toast-close" onclick="this.parentElement.remove()">&times;</span>';
    document.getElementById('toast-container').appendChild(toast);
    setTimeout(function() { if (toast.parentElement) toast.remove(); }, 5000);
}
```

- **确认/信息/输入弹窗**：统一 `AppDialog.confirm / alert / prompt`（已存在，不新增）；
- **禁止**：页面内新增 `alert/confirm/prompt`、自建 `showNotification` 类私有通知函数。

### 3.2 样式（custom.css 增量）

```css
.toast-title { font-weight: 600; margin-bottom: 2px; color: var(--text-primary); }
/* 其余沿用 .toast-item 四色左边框/动画/浅色主题适配 */
```

### 3.3 迁移清单

| 文件 | 改动 | 数量 |
|---|---|---|
| `templates/base.html` | `showToast` 增加可选 `title` 参数 | 1 处 |
| `templates/control.html` | `showNotification(title, message, type)` → `showToast(message, type, title)`（标题并入 toast-title）；**删除私有 `showNotification` 定义**（L2162 起函数体） | 约 20 处调用 + 1 处定义删除 |
| `templates/alarms.html` | `showNotification(title, message, type)` → `showToast(message, type, title)`（修复 T-02 未定义隐患） | 4 处 |
| `static/css/custom.css` | 新增 `.toast-title` | 1 处 |
| `static/js/dashboard.js` / `control.js` | 无需改动（已用 showToast） | — |

### 3.4 约束与门禁（T-04）

- 评审/CI 门禁：全工程 grep 校验——`showNotification` 调用与定义为零；`alert(`/`confirm(`/`prompt(` 仅允许出现在注释/文档字符串/第三方库；
- 新代码规范：toast 一律 `showToast`；需用户确认/重要信息一律 `AppDialog`。

---

## 4. 实施步骤

1. **P1**：base.html `showToast` 扩展 `title` 参数（兼容旧调用）；custom.css 新增 `.toast-title`；
2. **P2**：control.html 全部 `showNotification(...)` → `showToast(message, type, title)`；删除私有 `showNotification` 定义；
3. **P3**：alarms.html 4 处 `showNotification` → `showToast`（修复 ReferenceError 隐患）；
4. **P4**：校验——grep 确认 `showNotification` 零残留、原生弹窗仅注释/第三方；`node --check`（dashboard.js/control.js 未变则跳过）；浏览器逐个触发迁移场景；
5. **P5**：板端回归（index/control/alarms 三页 toast 样式一致、成功/失败/警告/信息四色正确、5s 自动消失、手动关闭可用）。

---

## 5. 测试与验收

1. grep 门禁：`showNotification` 0 调用/0 定义；`alert(/confirm(/prompt(` 仅注释/第三方；
2. 浏览器：control 页系统检查/清理缓存/执行命令/节点操作/操作员切换/导出成功等约 20 个场景统一为深色 toast（含标题行）；alarms 页确认/解决/清空/刷新成功提示不再 ReferenceError，且与 index 页 toast 完全一致；
3. 四色与交互：success/error/warning/info 左边框色、toast-in 动画、5s 消失、右上角 × 关闭、浅色主题正常；
4. 回归：index 页既有 showToast/AppDialog 无变化。

---

## 6. 兼容性与回滚

- `showToast` 增加可选第三参 `title`，旧两参调用完全兼容；
- 迁移为纯模板/样式改动，可整体回滚；
- 删除 `showNotification` 定义前先完成调用迁移（P2 顺序保证）；alarms 迁移同时修复 T-02 隐患。

---

## 7. 结论

1. 统一 Toast（showToast）与统一弹窗（AppDialog）已建立并符合设计 UI；原生弹窗 JS 零残留；
2. 剩余核心工作：**消灭 control.html 私有 `showNotification`（约 20 处）并修复 alarms.html 对其未定义的引用（4 处）**，实现全页面单一 toast 体系；
3. 方案为增量改造（base.html + control.html + alarms.html + custom.css），建议按 P1→P5 实施并加 grep 门禁防止回潮。