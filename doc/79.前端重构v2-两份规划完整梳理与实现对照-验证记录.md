# 79. 前端重构v2 — 04.1/05.1 完整梳理与实现对照(含验证证据)

> 日期:2026-09-12 | 代码基线:13d1461(前端重构v2) + UI优化提交(0f86ea7) + 本轮差距修复
> 目的:固化"两份规划文档逐条梳理 → 实现映射 → 测试验证"的完整证据链,作为验收基准。

---

## 一、规划文档梳理结论

### 1.1 doc/04.1《前端核心界面规划方案 v2.0》要点

- **三页核心**:首页、学员、报告;诊断与闭环整合进报告页 Tab,不再设独立菜单。
- **首页驾驶舱 5 区域**:①实时训练动态(40%,380px) ②概览4指标卡(高160px) ③五维诊断热点Top3(60%,360px) ④需关注学员(40%,360px) ⑤最近完成轮次(5行)。
- **取消近7天趋势图**,首页聚焦实时+诊断+需关注。
- **减少弹窗**:内嵌展开 > 独立页面 > 弹窗(仅确认类);"查看报告"为独立页面。
- **按工序区分**:所有评分/诊断/统计界面支持工序筛选(拆解/组装/全部)。
- **设置页**:评分规则/诊断阈值按工序编辑(保存/重载/校验/回滚)+学员同步。
- **键盘**:Ctrl+F 全局搜索、Esc 收起、←/→ 大图切换。

### 1.2 doc/05.1《视觉与交互设计规范 v1.1》要点

- **设计方向**:简洁/专业/克制的教育管理后台,蓝灰主色 `#2B5CE6`+橙强调 `#F5A623`,中高密度。
- **工作台优先**:典型分析任务 Route 切换 ≤1 次;指标卡点击**原地联动过滤**,不跳页。
- **三级交互层级**:原地联动 > Drawer/Popover/Lightbox > 独立页面;Drawer 优先于 Dialog。
- **三个 Drawer**:StudentDrawer(三问:为何关注/最近表现/薄弱步骤)、ReportDrawer(总分/异常/时间线摘要)、DiagnosisDrawer(描述/建议/证据/涉及轮次)。
- **状态保持**:筛选写入 URL Query;返回恢复上下文;Drawer 支持上一条/下一条;浏览器后退优先关 Drawer。
- **键盘快捷键**:Ctrl+F / Esc / ←→ / Enter。
- **状态色规则**:优秀绿/良好蓝/合格橙/不合格红;超时橙/中断红。
- **验收红线**:无必要跳转、返回不丢上下文、工作台+单 Drawer 完成任务。

## 二、实现对照映射(逐条)

### 2.1 04.1 逐条映射

| 04.1 条款 | 实现 | 位置 | 状态 |
|---|---|---|---|
| 三页核心导航 | 顶栏仅 首页/学员/报告 + ⚙设置 | `App.vue` nav-menu | ✅ |
| ①实时训练动态 | 设备卡列表:学员/工序链接+进度条+当前子步骤+用时;WS增量事件流10条(新事件置顶,带墙钟时间戳) | `DashboardView.vue` rt-panel, `onWsMessage` | ✅ |
| ①每2s轮询兜底 | WS断线兜底 `setInterval(loadRealtime, 5000)`(05.1刷新机制评估后取5s,降低空库压力) | `DashboardView.vue` onMounted | ✅ |
| ②概览4指标卡 高160px | `.ov-panel{height:160px}` 固定,不随左栏拉伸;**按用户反馈 4 卡排 2×2 网格**,数字压缩至24px | `DashboardView.vue` ov-grid | ✅ |
| ③五维诊断热点Top3 | `/analysis/diagnosis` 结果按 metric/threshold 偏差降序取3条;点击原地展开+Drawer | `DashboardView.vue` loadDiagnosis | ✅ |
| ④需关注学员 | `/dashboard/attention_students`;退步红/波动橙标签;点击开 StudentDrawer | `DashboardView.vue` att-panel | ✅ |
| ⑤最近完成轮次 | 5行表格;整行点击开 ReportDrawer;"查看更多"→报告列表 | `DashboardView.vue` recent-panel | ✅ |
| 取消近7天趋势 | 首页无趋势图;趋势仅保留在学员详情页(04.1 §4.2 要求) | — | ✅ |
| 学员列表+详情 | 列表(搜索/班级/状态/新增/CSV导入);详情页(指标×4/趋势折线/薄弱TOP3内嵌展开/退步预警/历史报告) | `StudentsView.vue` `StudentDetailView.vue` | ✅ |
| 报告页3 Tab | 报告列表/五维诊断(雷达图+条目)/教学闭环(顶部内嵌表单+verify前后30天对比) | `ReportsView.vue` | ✅ |
| 报告详情独立页 | 时间线(类型筛选)+子步骤得分明细+证据图墙(按子步骤分组→Lightbox) | `ReportDetailView.vue` | ✅ |
| 设置页 | 评分规则/诊断阈值按工序表格编辑,保存前客户端校验,PUT热重载,回滚=重拉,重载=`/config/reload`,学员同步+系统信息 | `SettingsView.vue` | ✅ |
| 工序全局筛选 | 顶栏下拉→Pinia workspace.process,首页/报告/学员详情 watch 联动重拉 | `App.vue` `stores/workspace.js` | ✅ |
| Ctrl+F/Esc/←→ | App全局键盘:Ctrl+F搜索Dialog、Esc关Drawer、Lightbox ←→ | `App.vue` `EvidenceLightbox.vue` | ✅ |

### 2.2 05.1 逐条映射

| 05.1 条款 | 实现 | 位置 | 状态 |
|---|---|---|---|
| 色彩规范(§3) | `design.css` 令牌:主色/强调/四级状态色+浅色底;状态色按得分等级 | `styles/design.css` `ScoreTag.vue` | ✅ |
| 字号层级(§4) | --fs-h1..aux/num 令牌;系统字体栈 | `design.css` | ✅ |
| 56px顶栏+1600内容区(§5) | `.top-nav{height:var(--nav-h)}` `.page-wrap{max-width:1600px}` | `App.vue` `design.css` | ✅ |
| 实时动态视觉(§6.1) | 呼吸状态点/渐变进度条/等宽时间戳/▶✓⚠✕四色事件图标 | `DashboardView.vue` | ✅ |
| 指标卡原地联动(§6.2) | 点击切换选中态(焦点环+色条加宽),过滤需关注学员与最近轮次,不跳页 | `DashboardView.vue` filtered* | ✅ |
| DiagnosisDrawer(§九) | 描述/指标偏差/建议/证据缩略图/相关轮次(可切ReportDrawer) | `DiagnosisDrawer.vue` | ✅ |
| StudentDrawer(§八) | 三问首屏:关注原因/最近成绩/薄弱TOP3;footer"查看长期分析"才进独立页 | `StudentDrawer.vue` | ✅ |
| ReportDrawer(§七) | 总分28px+等级/异常步骤/子步骤得分/关键时间线;"查看完整报告"才进独立页 | `ReportDrawer.vue` | ✅ |
| Lightbox(§10.2) | 全局单例,组内←→切换,Esc关闭 | `EvidenceLightbox.vue` | ✅ |
| 筛选写URL(§10.4) | `syncQuery()` 写 process/range;`restoreFromQuery()` 挂载恢复 | `stores/workspace.js` | ✅(本轮补) |
| Drawer上/下一条(§10.4) | `drawerContext{items,index}`+`drawerStep()`,两个Drawer footer 接线 | `stores/workspace.js` | ✅(本轮补) |
| 后退关Drawer(§10.3) | `popstate` 监听:Drawer打开时后退仅关Drawer | `App.vue` | ✅(本轮补) |
| Esc/键盘(§10.5) | 见04.1表 | `App.vue` | ✅ |
| 骨架屏/空态/错误(§10.6) | el-skeleton加载、el-empty业务文案空态、接口失败ElMessage+面板failed提示 | 各视图 | ✅ |

### 2.3 本轮审计修复的差距(第2轮)

1. 实时事件时间戳 stub → 按到达墙钟-批内最大ts偏移打 `_wall` 墙钟时间。
2. 筛选未写URL → store `syncQuery/restoreFromQuery` + App 挂载恢复。
3. Ctrl+F 全局搜索 → 学员+报告并行检索 Dialog。
4. Drawer 无上/下一条 → store 上下文导航 + 两 Drawer footer 按钮。
5. 浏览器后退不关 Drawer → popstate 处理。
6. (用户反馈)概览区固定160px 并改 2×2 网格。

## 三、测试验证证据

构建:`npm run build` ✅ 7.20s(vite 5,产物 ui_dist,含 .gz 预压缩)。

接口回归 + SPA 路由(脚本 `/tmp/verify_ui2.sh`,原始输出见 `doc/79-verify-evidence.log`):

- **/api/v1 教学域 19 项全 PASS**:today_summary / realtime_progress / attention_students(S003 命中) / top_error_points / score_trend / diagnosis( scope=class count=8,按工序过滤正常) / step_duration(report_count=91) / student_cumulative(S003: 15次 avg69.8 薄弱3) / reports 列表(total=91) / 报告详情(substeps=8, events=16) / evidence / teaching_actions + verify(trend=declined) / students CRUD 域 / scoring_rules / diagnosis_rules / system_info。
- **SPA 路由 8 项全 PASS**:/ui/ 及 /ui/students、/ui/students/S001、/ui/reports、/ui/reports/xxx(任意深度 fallback)、/ui/settings 均 200;index 引用的 JS/CSS 资产 200。
- **2×2 布局令牌**:`DashboardView-925irlPx.css` 内 `.ov-grid{grid-template-columns:repeat(2,1fr);grid-template-rows:repeat(2,1fr)}` 命中;本地与服务端 md5 一致(9abf43ea…),确认线上为新产物。

初跑 2 个 FAIL 均为验证脚本自身问题(单引号阻止变量展开 / scoped 样式在视图级 chunk 而非入口 CSS),复验澄清后记录于 evidence log 末尾。

## 四、遗留

- 概览 2×2 后左侧实时面板与概览区不再等高(row align-items:start),若需视觉齐平可后续给 rt-panel 设 min-height=160 对齐。
- `visualization-expert` skill 上游路径失效未能安装;`frontend-design-direction` 已安装并指导本轮视觉方向。
