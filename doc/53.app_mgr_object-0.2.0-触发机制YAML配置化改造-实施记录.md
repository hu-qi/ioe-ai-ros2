# 53. app_mgr_object-0.2.0 触发机制 YAML 配置化改造-实施记录

> 状态：**已实施（阶段 0-5 完成，含代码自审与测试）**
> 依据：`doc/52.app_mgr_object-0.2.0-触发机制YAML配置化改造方案.md`
> 工程：`app_mgr_object-0.2.0(标准版)`
> 实施日期：2026-08-31 ｜ 验证方式：ast 语法校验 + 单元测试（无 ROS2 桩模式）+ YAML 解析校验

---

## 1. 实施概述

按方案 52 的优先级顺序完成：

| 阶段 | 目标 | 实施结果 |
|---|---|---|
| 阶段 0 | 配置 Schema 与默认值收敛 | ✅ 新增字段写入 YAML（向后兼容）；引擎默认值与配置注入收敛（R-08） |
| 阶段 1 | 启动注入修复（R-01，最高优先） | ✅ `TriggerEngine` 接收 `config` 直接初始化；`status_poller` 启动注入 `pairing_policy` |
| 阶段 2 | 热加载注册表（R-02/R-03） | ✅ 新增 `HotReloadRegistry`；两个插件订阅 `plugin_configs.updated`；`TaskQueue` 运行时可更新；Web 端点新增 `/api/config/trigger`、`/api/config/status_poller` |
| 阶段 3 | 策略可配置化（R-05） | ✅ AGV 选择策略、始发扫描顺序、RCS 状态映射、派发轮次、绑定失败重试全部 YAML 化 |
| 阶段 4 | 校验、观测（R-07） | ✅ ConfigManager 新增 12 段校验；注册表校验器；启动注入摘要日志；`get_trigger_config` 读取接口 |
| 阶段 5 | 测试 | ✅ 新增 5 项单测全通过；既有 5 项回归全通过 |

---

## 2. 变更文件清单

| 文件 | 阶段 | 改动内容 | 验证 |
|---|---|---|---|
| `app_mgr_object/components/trigger/hot_reload.py` | 2 | **新增**：`HotReloadRegistry`（register/apply，线程安全）+ 通用校验器（validate_positive_number / validate_non_negative_int / validate_dict / validate_list） | 单测 PASS |
| `app_mgr_object/components/trigger/trigger_engine.py` | 1/3 | `__init__` 增加 `config` 参数并优先注入 cargo_priority / max_tasks_per_scan / pair_overrides / pairing_policy / src_scan_order；新增 `set_src_scan_order` | 单测 PASS |
| `app_mgr_object/components/trigger/batch_matcher.py` | 3 | 新增 `set_src_scan_order` / `_src_sort_key`（by_bay_id_asc / by_bind_time_desc / custom），替换硬编码排序 | 单测 PASS |
| `app_mgr_object/components/task_cache/robot_cache.py` | 3 | 新增 `set_select_policy`（round_robin / least_busy / specified + available_statuses）；`get_available_robot` 按策略选择 | 单测 PASS |
| `app_mgr_object/components/trigger/task_queue.py` | 2 | 新增 `set_aging_ttl` / `set_max_size` 运行期更新接口 | 单测 PASS |
| `app_mgr_object/plugins/smart_trigger_plugin.py` | 1/2/3/4 | TriggerEngine 传 `config`；订阅 `plugin_configs.updated`；注册 12 段热加载（节奏/优先级/配对/工作时段/扫描顺序/AGV/绑定重试）；`dispatch_per_round` 替代硬编码 3；`work_schedule` list/dict 兼容；R-06 私有成员访问修正；启动注入摘要日志 | 语法 OK + 单测 PASS |
| `app_mgr_object/plugins/status_poller_plugin.py` | 1/2/3 | 启动注入 `pairing_policy`；订阅 `plugin_configs.updated`；注册 4 段热加载（轮询间隔重建定时器、状态映射、配对策略）；`agv_status_mapping` 替代硬编码映射 | 语法 OK |
| `app_mgr_object/plugins/bay_status_fusion_plugin.py` | 3 | 绑定失败重试：`_do_bind` / `_schedule_bind_retry` / `_retry_bind_tick` / `_cancel_bind_retry` / `set_bind_retry_config`；稳定空/已绑定/停用时取消重试 | 语法 OK |
| `app_mgr_object/components/web/config_manager.py` | 2/4 | `path_map` 扩展 12 段；`_validate_section` 新增节奏/策略/轮询校验；新增 `get_trigger_config`；`get_all_config` 增加 `trigger_params` | 语法 OK |
| `app_mgr_object/components/web/web_server.py` | 2 | 新增 `GET/PUT /api/config/trigger`、`GET/PUT /api/config/status_poller`（持久化+事件驱动热加载） | 语法 OK |
| `config/plugin_configs/smart_trigger.yaml` | 3/4 | 新增 `dispatch_per_round`、`src_scan_order`、`agv`、`bind`（默认值=现状行为） | YAML 解析 OK |
| `config/plugin_configs/status_poller.yaml` | 3 | 新增 `agv_status_mapping`（默认映射=现状行为） | YAML 解析 OK |
| `test/test_trigger_yaml_config.py` | 5 | **新增** 5 项测试：启动注入 / 扫描顺序 / AGV 策略 / 队列热更新 / 注册表 | 5/5 PASS |
| `doc/53.app_mgr_object-0.2.0-触发机制YAML配置化改造-实施记录.md` | — | **本文档** | — |

---

## 3. 与方案 52 目标符合性审查

### 3.1 问题修复对照（方案 §4 R-01 ~ R-08）

| 编号 | 方案问题 | 实施结果 | 证据 |
|---|---|---|---|
| R-01 | 启动注入缺口（引擎读空 ConfigCache、pairing_policy 未注入） | ✅ 已修复 | `trigger_engine.py` `__init__(..., config: dict = None)` 优先注入；`smart_trigger_plugin.py` 创建引擎时传 `config=self.config`；`status_poller_plugin.py` `_configure_impl` 调 `dest_cache.set_pairing_policy(...)` |
| R-02 | 热加载覆盖面不全（节奏类参数需重启） | ✅ 已修复 | `smart_trigger_plugin.py` 注册 12 段（含 max_total_tasks / aging_ttl / scan_interval / max_queue_size / dispatch_per_round 等）；`task_queue.py` `set_aging_ttl/set_max_size` |
| R-03 | `plugin_configs.updated` 无订阅方 | ✅ 已修复 | 两插件 `event_bus.subscribe('plugin_configs.updated', ...)`；Web 新增通用端点触发持久化+事件 |
| R-04 | 持久化不对称（重启回退） | ✅ 已修复（与 R-01 同源） | 启动即从 YAML 注入，Web 保存与重启行为一致 |
| R-05 | 硬编码策略项 | ✅ 5/6 项 YAML 化 | AGV 策略（`robot_cache.set_select_policy`）；扫描顺序（`batch_matcher.set_src_scan_order`）；状态映射（`status_poller._status_mapping`）；派发轮次（`dispatch_per_round`）；绑定重试（`bay_status_fusion` `_schedule_bind_retry`）。“RCS→内部状态映射”为第 6 项已含 |
| R-06 | 私有成员跨插件访问 | ✅ 已修复 | `smart_trigger_plugin.py` 两处 `_schedule_checker` 改 `schedule_checker` property |
| R-07 | 校验不统一 | ✅ 已扩展 | `config_manager.py` 新增 12 段校验；`hot_reload.py` 通用校验器；YAML 解析校验通过 |
| R-08 | 引擎默认值与 YAML 不一致 | ✅ 已收敛 | `trigger_engine.py` 以 `config` 注入 `max_tasks_per_scan/cargo_priority`，ConfigCache 仅兜底 |

### 3.2 方案 §5.3 目标 YAML Schema 落地对照

| 字段 | 方案要求 | 落地 |
|---|---|---|
| `dispatch_per_round` | hot ★ | ✅ smart_trigger.yaml + 插件读取 + 热加载 |
| `src_scan_order` | hot ★ | ✅ smart_trigger.yaml + 引擎/匹配器 + 热加载 |
| `agv.select_strategy / specified_ids / available_statuses` | hot ★ | ✅ smart_trigger.yaml + RobotCache 策略 + 热加载 |
| `bind.retry_enabled / retry_count / retry_delay` | hot ★ | ✅ smart_trigger.yaml + bay_status_fusion 重试 + 热加载 |
| `agv_status_mapping` | hot ★ | ✅ status_poller.yaml + 轮询映射 + 热加载 |
| `robot_poll_interval / dest_poll_interval` | hot ★ | ✅ 定时器重建热更新 |
| `max_total_tasks / max_tasks_per_scan / aging_ttl / scan_interval / max_queue_size` | hot ★ | ✅ 插件属性+队列对象热更新 |
| `cargo_priority / pair_overrides / work_schedule / pairing_policy` | hot（现有） | ✅ 保留原热更新 + 事件链路 |

### 3.3 保留档位（方案 §5.4 一致）

- `enabled`、`operation_mode`：保持 restart 档（生命周期级，方案明确保留）；
- `robot_ids`：restart 档（RobotCache 结构在激活时构建）。

---

## 4. 测试与验证

### 4.1 语法校验

- 对 11 个改动/新增 Python 文件执行 `ast.parse`：**SYNTAX OK**。
- 两个 YAML 配置文件 `yaml.safe_load`：**解析 OK**，新增字段完整。

### 4.2 单元测试（无 ROS2 桩模式，`python test/...`）

新增 `test/test_trigger_yaml_config.py`（5/5 PASS）：

1. `test_engine_startup_injection` — TriggerEngine 启动注入（R-01 回归）；
2. `test_matcher_src_scan_order_modes` — 扫描顺序三种模式；
3. `test_robot_select_policies` — AGV 四种策略场景；
4. `test_task_queue_hot_setters` — set_max_size/set_aging_ttl 热更新与占用释放；
5. `test_hot_reload_registry` — 校验通过应用、非法拒绝。

既有回归 `test/test_trigger_ghost_lock.py`（5/5 PASS）：批量截断无幽灵锁定、greedy 限流、容量缩水释放、显式配对上限、入队移除释放——全部无回归。

### 4.3 未覆盖（板端验证项，需现场）

- 真实 ROS2 环境下的定时器重建与绑定重试（依赖 node.create_timer）；
- Web 端点端到端（需 FastAPI 服务运行）；
- 板端回归清单（doc/52 §9.2）：改 YAML 重启生效、Web 保存热生效、巷道/队列/老化/解绑链路。

---

## 5. 遗留与边界说明

| 项 | 说明 | 后续建议 |
|---|---|---|
| 前端配置页 | 新端点已就绪（/api/config/trigger、/api/config/status_poller），前端按钮/档位提示未做 | 方案 52 阶段 4 的增强项，按需补充 dashboard.js/api.js |
| 启动期非法 YAML | 非法值由 `BasePlugin.configure` 捕获并记 ERROR（拒绝加载），未实现“自动回退上次有效配置”快照 | 可后续在 ParamManager 增加配置版本快照 |
| Web 未启用 | 事件链路已解耦，但无 Web 时缺少持久化/触发入口（方案 O-1） | 按需增加文件监听或 CLI 热加载入口 |
| 热更新一致性 | `robot_ids` 变更需重启（方案 O-5） | 保持 restart 档 |
| 绑定重试 | 重试期间仓位保持 `binding` 状态，超限后 `set_unbound`；与既有滤波/解绑逻辑兼容 | 板端验证重试时序 |

---

## 6. 修改操作记录

| 序 | 操作 | 文件 | 说明 |
|---|---|---|---|
| 1 | 新增 | `components/trigger/hot_reload.py` | 热加载注册表与通用校验器 |
| 2 | 修改 | `components/trigger/trigger_engine.py` | config 启动注入 + set_src_scan_order |
| 3 | 修改 | `components/trigger/batch_matcher.py` | 扫描顺序排序键配置化 |
| 4 | 修改 | `components/task_cache/robot_cache.py` | AGV 选择策略 |
| 5 | 修改 | `components/trigger/task_queue.py` | aging_ttl/max_size 热更新接口 |
| 6 | 修改 | `plugins/smart_trigger_plugin.py` | 启动注入、事件订阅、12 段热加载、dispatch_per_round、R-06 |
| 7 | 修改 | `plugins/status_poller_plugin.py` | pairing_policy 注入、事件订阅、4 段热加载、状态映射 |
| 8 | 修改 | `plugins/bay_status_fusion_plugin.py` | 绑定失败重试机制 |
| 9 | 修改 | `components/web/config_manager.py` | 路径映射扩展、12 段校验、get_trigger_config |
| 10 | 修改 | `components/web/web_server.py` | /api/config/trigger、/api/config/status_poller 端点 |
| 11 | 修改 | `config/plugin_configs/smart_trigger.yaml` | dispatch_per_round/src_scan_order/agv/bind |
| 12 | 修改 | `config/plugin_configs/status_poller.yaml` | agv_status_mapping |
| 13 | 新增 | `test/test_trigger_yaml_config.py` | 5 项单元测试 |
| 14 | 新增 | `doc/53...实施记录.md` | 本文档 |
| 15 | 格式 | 全部改动文件 | 统一为无 BOM UTF-8（消除 PowerShell 写入 BOM 的副作用） |

> 备注：因 `apply_patch` 工具无法写入含中文/括号的路径，全部文件写入/修改经 PowerShell 完成；每次修改后均回读校验内容。

---

## 7. 结论

1. 方案 52 的阶段 1-5 已全部落地，R-01 ~ R-08 中 8 项问题全部处理（R-01/R-02/R-03 为核心缺口）；
2. 触发机制现可按方案 §5.3 的 YAML Schema 配置，启动即生效（阶段 1），运行期可热更新（阶段 2），方向性策略（AGV/扫描顺序/状态映射/派发轮次/绑定重试）可选可配（阶段 3）；
3. 验证结果：11 个 Python 文件语法通过，新增 5 项 + 既有 5 项单测全通过，YAML 解析通过；
4. 遗留增强项（前端配置页、启动快照回退、无 Web 入口）已记录，不影响本次目标达成；
5. 部署提示：板端需重启一次应用使启动注入生效；后续可通过 Web 新端点或直接修改 YAML 调整触发机制。