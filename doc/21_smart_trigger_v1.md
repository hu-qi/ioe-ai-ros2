'# SmartTrigger v1'  
''  
'## 1. Algorithm: Graded Greedy Match'  
'K=3 per src. 20src*30dst - candidates (Cartesian=600). Same-floor priority: A/B-, C/D- Sort: floor_flag  
''  
'## 2. New Files'  
'- models.py: TaskCandidate dataclass with same_floor + sort_key()'  
'- batch_matcher.py: TaskBatchMatcher with floor-aware greedy match, capacity cap'  
'- task_queue.py: TaskQueue with FIFO, AGV-aware dequeue, dedup, stats' 
'## 3. Modified Files'  
''  
'### trigger_engine.py (+40L)'  
'- import TaskBatchMatcher from .batch_matcher'  
'- __init__: create self._bm with config(cargo_priority, floor_mapping, max_dst_per_src)'  
'- new: _cfg() helper, gen_id(), batch_scan_all()'  
''  
'### smart_trigger_plugin.py (full rewrite ~150L)'  
'- v3.0: TaskQueue integration, _dispatch_from_queue, _quick for events'  
'- _scan_loop: batch_scan_all - - 
'- _dispatch: try_dequeue (AGV-aware), per-round max 3'  
'- events: bay.bound -, task_completed - + _dispatch' 
'## 5. Tests'  
'TC1 batch: 3src*3dst->9cand (per src<=3)'  
'TC2 cap: 20src*30dst-><=10global + <=3/src'  
'TC3 floor: A->T2 before A->T3'  
'TC4 queue: BUSY->enqueue, IDLE->dequeue'  
'TC5 chain: task_completed->dispatch next'  
'TC6 dedup: queued src/dst not re-added'  
''  
'## 6. Files'  
'New: models.py(25L) batch_matcher.py(100L) task_queue.py(90L)'  
'Mod: trigger_engine.py(+40L) smart_trigger_plugin.py(~150L) params.yaml(+12L)'  
'Total: ~470L, est 4-5h' 
