# app_mgr_object-0.0.7 — 任务状态周期轮询插件开发指南（v2.0）

> **目标**: 在触发 AGV 搬运任务后，通过**按需**周期轮询 RCS `queryTaskStatus`(3.1.9) 实时获取任务执行状态  
> **核心原则**: 有 monitoring 态任务时定时查询；无任务时完全静默，零 RCS API 消耗  
> **工程**: `app_mgr_object-0.0.7(基础版)-6`  
> **关联**: `厂内物流调度控制系统RCS-2000 V3.3 对外任务接口文档.pdf` §3.1.9  
> **前置修复**: `15.app_mgr_object-0.0.7-工作流悬挂修复与恢复逻辑增强.md`（补偿计数器 + 恢复校准）  
> **编制日期**: 2026-08-01

---

## 目录

1. [v0.0.7-6 基线分析](#1-v007-6-基线分析)
2. [按需轮询设计](#2-按需轮询设计)
3. [RCS 3.1.9 queryTaskStatus 接口规范](#3-rcs-319-querytaskstatus-接口规范)
4. [TaskStatusMonitor 组件实现（增强版）](#4-taskstatusmonitor-组件实现增强版)
5. [task_status_monitor_plugin 插件实现（增强版）](#5-task_status_monitor_plugin-插件实现增强版)
6. [WorkflowEngine 增强接口](#6-workflowengine-增强接口)
7. [配置文件修改](#7-配置文件修改)
8. [事件流与状态推进对照表](#8-事件流与状态推进对照表)
9. [端到端集成测试流程](#9-端到端集成测试流程)
10. [故障排查与注意事项](#10-故障排查与注意事项)

---

## 1. v0.0.7-6 基线分析

### 1.1 已具备的修复（来自文档 15）

| 修复项 | 位置 | 状态 |
|:---|:---|:---|
| 补偿循环计数器 | `instance.py` — `compensate_cycle_count` + `_transition_to` 上限改写 | ✅ 已落地 |
| 恢复前 RCS 校准 | `workflow_engine_plugin.py` — `_validate_and_recover()` | ✅ 已落地 |
| YAML max_retry_reached | `agv_transport_atomic.yaml` — `compensating_query` 新增转移 | ✅ 已落地 |
| monitoring timeout 调整 | `agv_transport_atomic.yaml` — `timeout: 100` (原 600s) | ✅ 已落地 |
| 无匹配事件兜底 | `instance.py` — `_last_unknown_event` 上下文记录 | ✅ 已落地 |

### 1.2 仍需补充的能力

| 缺口 | 当前状态 | v2.0 目标 |
|:---|:---|:---|
| **无 periodical polling** | 仅靠 compensating_query 单次查询 | 新增 `task_status_monitor` 插件 |
| **无按需启停** | — | 有 monitoring 任务 → 轮询；无任务 → 静默 |
| **无状态历史** | — | 记录任务状态演进轨迹 |
| **engine.get_active_monitoring_instances()** | 不存在 | 新增查询方法 |

### 1.3 当前任务状态获取路径（v0.0.7-6 修复后）

```
触发任务后的状态获取：

   方式 A: RCS 回调 (被动)          方式 B: 超时补偿查询 (一次性, 100s)
   ──────────────────────           ──────────────────────────
   smart_trigger                    workflow_engine
     │                                │
     ├─ trigger.task_created          ├─ monitoring (挂起 100s)
     ▼                                │   wait_for_completion
   workflow_engine                    │
     ├─ calling_rcs                   ├─ 100s 超时 →
     │   invoke_gen_task()            ▼
     │                              compensating_query
     ├─ monitoring (挂起 100s)         ├─ queryTaskStatus
     │   wait_for_completion          ├─ rcs_running → monitoring
     │                                │   ↑ (最多 3 轮, compensate_cycle_count)
     │                                └─ max_retry_reached → manual ✓
     ├─ callback_completed ← callback_handler
     └─ releasing → completed
```

**仍需增加的方式 C:**

```
   方式 C: 周期轮询 (主动, 10s)  ← NEW in v2.0
   ──────────────────────────
   task_status_monitor
     ├─ Timer 每 10s 触发
     ├─ engine.get_active_monitoring_instances()  ← NEW API
     ├─ 如果列表为空 → 跳过本轮 (零 RCS 消耗)
     ├─ 如果列表非空 → rcs.queryTaskStatus(all_codes)
     ├─ 检测状态变化 → engine.inject_event()
     └─ 终态任务自动清理
```

---

## 2. 按需轮询设计

### 2.1 核心理念

轮询不是"盲定时器"，而是**事件驱动的定时器**：

```
工作流无任务时:
  ┌─────────────────────────────────────────────────┐
  │  _poll_timer 处于 inactive 状态                  │
  │  无 ROS2 Timer 回调触发                          │
  │  零 RCS API 请求                                 │
  │  零 CPU 消耗（无协程, 无 HTTP）                     │
  └─────────────────────────────────────────────────┘

工作流有 monitoring 任务时:
  ┌─────────────────────────────────────────────────┐
  │  _poll_timer 激活, 每 poll_interval 秒触发         │
  │  → engine.get_active_monitoring_instances()       │
  │  → rcs.queryTaskStatus()                         │
  │  → 状态变化 → inject_event()                      │
  └─────────────────────────────────────────────────┘

最后一个 monitoring 任务终结时:
  ┌─────────────────────────────────────────────────┐
  │  _poll_timer 自动 cancel                         │
  │  回到空闲状态                                      │
  └─────────────────────────────────────────────────┘
```

### 2.2 Timer 生命周期状态机

```
                    ┌─────────────────────┐
                    │    IDLE (初始态)      │
                    │  timer=None           │
                    │  no RCS calls        │
                    └──────────┬──────────┘
                               │ workflow.task_started 事件
                               │ (首个 monitoring 实例)
                               ▼
                    ┌─────────────────────┐
                    │    ACTIVE            │
                    │  timer=create_timer  │
                    │  每 poll_interval 秒  │
                    │  poll_if_needed()    │
                    └──────────┬──────────┘
                               │ poll_if_needed() 返回空
                               │ (两轮连续监测到 0 个活跃实例)
                               ▼
                    ┌─────────────────────┐
                    │    IDLE              │
                    │  timer.destroy()     │
                    │  no RCS calls        │
                    └─────────────────────┘
```

> **设计要点**: 使用"两轮连续空闲"作为停止条件，避免单轮瞬时为空导致的抖动（任务刚完成但新任务立即到达）。

### 2.3 与现有 compensating_query 的分工

| 机制 | 触发频率 | 查询对象 | 退出条件 |
|:---|:---|:---|:---|
| **task_status_monitor** (新增) | 每 10s (按需) | 所有 monitoring 实例 | 无 monitoring 实例时自动停 |
| **compensating_query** (已有) | monitoring 超时后 (100s) | 单个实例 | rcs_completed / rcs_failed / max_retry_reached |

两者互补：轮询提供**全貌感知**和**提前完成检测**；compensating_query 提供**单点深度兜底**和**超限强制终结**。

---

## 3. RCS 3.1.9 queryTaskStatus 接口规范

### 3.1 接口定义

**路径**: `POST /queryTaskStatus`  
**用途**: 查询一个或多个任务当前执行状态  
**参考章节**: RCS-2000 V3.3 对外任务接口文档 §3.1.9

### 3.2 请求格式

```json
{
  "taskCodes": ["TASK_1723456789_01", "TASK_1723456790_02"]
}
```

| 字段 | 类型 | 必填 | 说明 |
|:---|:---|:---|:---|
| taskCodes | string[] | 是 | 客户端 task_id 列表 |

### 3.3 响应格式

```json
{
  "code": "0",
  "msg": "成功",
  "data": [
    {
      "taskCode": "TASK_1723456789_01",
      "taskStatus": "2",
      "agvCode": "1001",
      "positionCode": "A1001",
      "taskNo": "RCS_1723456789_0001"
    }
  ]
}
```

### 3.4 taskStatus 状态码映射

| 状态码 | 名称 | 引擎应触发的动作 |
|:---|:---|:---|
| `"0"` | PENDING | 不注入 |
| `"1"` | DISPATCHING | 不注入 |
| `"2"` | RUNNING | 不注入（正常中间态） |
| `"3"` | PAUSED | 记录警告日志 |
| `"7"` | FAILED | **注入 `callback_exception`** |
| `"8"` | CANCELLED | **注入 `callback_exception`** |
| `"9"` | COMPLETED | **注入 `callback_completed`** |

### 3.5 与 callback / compensating 的协作

| 场景 | 回调 | 轮询 | compensating |
|:---|:---|:---|:---|
| 回调正常(秒级) | ✅ 注入完成 | 检测到 "9" → 跳过(去重) | 不触发(100s内已完结) |
| 回调丢失 | ❌ | ✅ 10-20s 注入完成 | 100s后再次兜底 |
| RCS 异常 | ❌ | ✅ 注入 exception | 100s后再次兜底 |
| RCS 永远 RUNNING | ❌ | 持续报告 RUNNING | 3轮后 max_retry_reached |

---

## 4. TaskStatusMonitor 组件实现（增强版）

文件路径: `app_mgr_object/components/task_status_monitor/monitor.py`

```python
#!/usr/bin/env python3
"""
TaskStatusMonitor — 任务状态按需周期轮询组件（v2.0）

与 v1.0 的差异:
  - 新增强制闲置检测 poll_if_needed(active_instances)
  - 应只在 active_instances 非空时被调用
  - 内置 idle_rounds 计数器用于两轮确认机制
"""

import time
import logging
import threading
from typing import Dict, Optional, Callable


class TaskStatusCode:
    PENDING     = "0"
    DISPATCHING = "1"
    RUNNING     = "2"
    PAUSED      = "3"
    FAILED      = "7"
    CANCELLED   = "8"
    COMPLETED   = "9"

    FINAL_STATES = {FAILED, CANCELLED, COMPLETED}

    @classmethod
    def to_engine_event(cls, status_code: str) -> Optional[str]:
        mapping = {
            cls.COMPLETED:  'callback_completed',
            cls.FAILED:     'callback_exception',
            cls.CANCELLED:  'callback_exception',
        }
        return mapping.get(status_code, None)

    @classmethod
    def status_name(cls, code: str) -> str:
        names = {'0':'PENDING','1':'DISPATCHING','2':'RUNNING',
                 '3':'PAUSED','7':'FAILED','8':'CANCELLED','9':'COMPLETED'}
        return names.get(code, 'UNKNOWN')


class TaskStatusMonitor:

    def __init__(self,
                 on_status_changed: Optional[Callable[[str, str, str], None]] = None,
                 poll_timeout: float = 8.0,
                 logger=None):
        self._on_status_changed = on_status_changed
        self._poll_timeout = poll_timeout
        self._logger = logger

        self._last_known: Dict[str, str] = {}
        self._lock = threading.RLock()

        # 统计
        self._poll_count: int = 0
        self._poll_skip_count: int = 0       # 新增: 无任务跳过次数
        self._poll_error_count: int = 0
        self._change_detected_count: int = 0
        self._last_poll_time: float = 0.0
        self._last_poll_duration_ms: float = 0.0
        self._idle_consecutive_rounds: int = 0  # 新增: 连续空闲轮数

    # ════════════════════════════════════════════════════
    #  核心: poll_if_needed (按需入口)
    # ════════════════════════════════════════════════════

    def poll_if_needed(self, rcs_adapter, active_instances: Dict[str, str]) -> bool:
        """按需执行一轮轮询

        由插件层的 Timer 回调调用。插件层已在外部判断 active_instances
        非空时才调用此方法，但此方法仍做二次防护。

        Args:
            rcs_adapter:      RCSAdapterPlugin 实例
            active_instances: {task_code: rcs_task_no} 映射

        Returns:
            True  — 本轮实际执行了 RCS 查询
            False — 无活跃实例，跳过本轮（调用方可用于停止 timer）
        """
        if not active_instances:
            self._poll_skip_count += 1
            self._idle_consecutive_rounds += 1
            self._log_debug(f"无活跃实例, 跳过第 {self._poll_skip_count} 次轮询 "
                            f"(连续空闲 {self._idle_consecutive_rounds} 轮)")
            return False

        # 有活跃实例时重置空闲计数
        self._idle_consecutive_rounds = 0
        changes = self._do_poll(rcs_adapter, active_instances)
        return True

    def should_stop_timer(self) -> bool:
        """判断是否应该停止定时器（连续 2 轮无活跃实例）"""
        return self._idle_consecutive_rounds >= 2

    # ════════════════════════════════════════════════════
    #  内部轮询逻辑
    # ════════════════════════════════════════════════════

    def _do_poll(self, rcs_adapter, active_instances: Dict[str, str]) -> Dict[str, str]:
        self._poll_count += 1
        task_codes = list(active_instances.keys())
        start_time = time.time()

        self._log_debug(f"第 {self._poll_count} 轮轮询: {len(task_codes)} 个任务")

        try:
            resp = rcs_adapter.query_task_status(task_codes)
            elapsed = (time.time() - start_time) * 1000
            self._last_poll_time = start_time
            self._last_poll_duration_ms = elapsed

            if resp.get('code') != '0':
                self._poll_error_count += 1
                self._log_warning(f"RCS queryTaskStatus 异常: code={resp.get('code')}")
                return {}

            data_list = resp.get('data', [])
            if not isinstance(data_list, list):
                self._poll_error_count += 1
                return {}

            changes = {}
            for item in data_list:
                task_code = item.get('taskCode', '')
                new_status = str(item.get('taskStatus', ''))
                if not task_code:
                    continue

                old_status = self._get_last_known(task_code)
                if old_status == new_status:
                    continue

                self._set_last_known(task_code, new_status)
                changes[task_code] = new_status
                self._change_detected_count += 1

                self._log_info(f"状态变化: {task_code} "
                               f"{old_status or '---'}→{new_status} "
                               f"({TaskStatusCode.status_name(new_status)})")

                if new_status in TaskStatusCode.FINAL_STATES:
                    engine_event = TaskStatusCode.to_engine_event(new_status)
                    if engine_event and self._on_status_changed:
                        try:
                            self._on_status_changed(task_code, new_status, engine_event)
                        except Exception as e:
                            self._log_error(f"状态变化回调异常: {e}")

            self._cleanup_final(active_instances)
            self._poll_error_count = 0
            return changes

        except Exception as e:
            self._poll_error_count += 1
            self._log_error(f"轮询异常: {e}")
            return {}

    # ════════════════════════════════════════════════════

    def _get_last_known(self, task_code: str) -> str:
        with self._lock:
            return self._last_known.get(task_code, '')

    def _set_last_known(self, task_code: str, status: str):
        with self._lock:
            self._last_known[task_code] = status

    def _cleanup_final(self, active: Dict[str, str]):
        with self._lock:
            to_remove = [tc for tc, st in self._last_known.items()
                         if st in TaskStatusCode.FINAL_STATES]
            for tc in to_remove:
                self._last_known.pop(tc, None)
                active.pop(tc, None)
                self._log_debug(f"已清理终态任务: {tc}")

    def reset(self):
        with self._lock:
            self._last_known.clear()
            self._poll_count = 0
            self._poll_skip_count = 0
            self._poll_error_count = 0
            self._change_detected_count = 0
            self._idle_consecutive_rounds = 0

    def get_last_known_status(self, task_code: str) -> Optional[str]:
        with self._lock:
            return self._last_known.get(task_code, None)

    def get_all_last_known(self) -> Dict[str, str]:
        with self._lock:
            return dict(self._last_known)

    def get_stats(self) -> dict:
        return {
            'poll_count': self._poll_count,
            'poll_skip_count': self._poll_skip_count,
            'poll_error_count': self._poll_error_count,
            'change_detected_count': self._change_detected_count,
            'last_poll_time': self._last_poll_time,
            'last_poll_duration_ms': self._last_poll_duration_ms,
            'monitored_tasks': len(self._last_known),
            'idle_consecutive_rounds': self._idle_consecutive_rounds,
        }

    def _log_info(self, msg: str):
        if self._logger: self._logger.info(f"[TaskMonitor] {msg}")

    def _log_debug(self, msg: str):
        if self._logger: self._logger.debug(f"[TaskMonitor] {msg}")

    def _log_warning(self, msg: str):
        if self._logger: self._logger.warning(f"[TaskMonitor] {msg}")

    def _log_error(self, msg: str):
        if self._logger: self._logger.error(f"[TaskMonitor] {msg}")
```

组件包入口 `__init__.py`:

```python
from .monitor import TaskStatusMonitor, TaskStatusCode
__all__ = ['TaskStatusMonitor', 'TaskStatusCode']
```

---

## 5. task_status_monitor_plugin 插件实现（增强版）

文件路径: `app_mgr_object/plugins/task_status_monitor_plugin.py`

```python
#!/usr/bin/env python3
"""
task_status_monitor_plugin — 任务状态按需周期轮询插件 (v2.0)

核心改进（相对 v1.0）:
  - 事件驱动的 Timer 启停: 有 monitoring 任务时激活, 无任务时停止
  - poll_if_needed() 代替盲轮询
  - 两轮连续空闲确认避免抖动
"""

from typing import Dict, Any, Optional

from .base_plugin import BasePlugin
from app_mgr_object.components.task_status_monitor import (
    TaskStatusMonitor, TaskStatusCode
)


class TaskStatusMonitorPlugin(BasePlugin):
    PLUGIN_NAME = "task_status_monitor"
    PLUGIN_VERSION = "2.0.0"

    def __init__(self, node, config: Dict[str, Any] = None):
        super().__init__(node, config)
        self._poll_interval = self.config.get('poll_interval', 10.0)
        self._poll_timeout   = self.config.get('poll_timeout', 8.0)
        self._enabled        = self.config.get('enabled', True)

        self._monitor: Optional[TaskStatusMonitor] = None
        self._poll_timer = None

        # 统计
        self._total_polls: int = 0
        self._status_changes: int = 0

    # ════════════════════════════════════════════════════
    #  生命周期
    # ════════════════════════════════════════════════════

    def _configure_impl(self) -> bool:
        self._monitor = TaskStatusMonitor(
            on_status_changed=self._on_status_changed,
            poll_timeout=self._poll_timeout,
            logger=self.logger,
        )
        self.logger.info(f"TaskStatusMonitor v2 配置: "
                         f"poll_interval={self._poll_interval}s, enabled={self._enabled}")
        return True

    def _activate_impl(self) -> bool:
        if not self._enabled:
            return True

        # 事件订阅（用于按需启停 Timer）
        self.event_bus.subscribe('workflow.task_started', self._on_task_started)
        self.event_bus.subscribe('workflow.task_completed', self._on_task_ended)
        self.event_bus.subscribe('workflow.task_failed', self._on_task_ended)

        # 首次启动时检查是否有历史实例在 monitoring
        self._ensure_timer_if_needed()

        self.logger.info("TaskStatusMonitor v2 激活完成 (按需轮询模式)")
        return True

    def _deactivate_impl(self) -> bool:
        self._stop_poll_timer()
        return True

    def _cleanup_impl(self) -> bool:
        self._monitor = None
        return True

    # ════════════════════════════════════════════════════
    #  Timer 生命周期 (按需启停)
    # ════════════════════════════════════════════════════

    def _start_poll_timer(self):
        """启动轮询定时器（已在运行时不再重复创建）"""
        if self._poll_timer is not None:
            return
        self._poll_timer = self.node.create_timer(
            self._poll_interval, self._poll_once
        )
        self.logger.info(f"轮询定时器已启动 (间隔 {self._poll_interval}s)")

    def _stop_poll_timer(self):
        """停止轮询定时器"""
        if self._poll_timer is not None:
            self.node.destroy_timer(self._poll_timer)
            self._poll_timer = None
            self.logger.info("轮询定时器已停止 (无活跃任务)")

    def _ensure_timer_if_needed(self):
        """根据当前 active instances 状态决定启用/停用 timer"""
        engine = self._get_workflow_engine()
        if not engine:
            return
        active = engine.get_active_monitoring_instances()
        if active:
            self._start_poll_timer()
        else:
            self._stop_poll_timer()

    # ════════════════════════════════════════════════════
    #  轮询执行
    # ════════════════════════════════════════════════════

    def _poll_once(self):
        """定时器回调: 执行一轮按需轮询"""
        try:
            engine = self._get_workflow_engine()
            if not engine:
                return

            active = engine.get_active_monitoring_instances()

            # 无活跃实例 → 停止 timer
            if not active:
                if self._monitor.should_stop_timer():
                    self._stop_poll_timer()
                return

            # 有活跃实例 → 执行轮询
            rcs = self._get_rcs_adapter()
            if not rcs:
                return

            had_changes = self._monitor.poll_if_needed(rcs, active)
            if had_changes:
                stats = self._monitor.get_stats()
                self._status_changes += stats.get('change_detected_count', 0)

            self._total_polls += 1

        except Exception as e:
            self.logger.warning(f"轮询执行异常: {e}")

    # ════════════════════════════════════════════════════
    #  事件回调
    # ════════════════════════════════════════════════════

    def _on_task_started(self, data: dict):
        """有新任务启动 → 激活 timer"""
        task_id = data.get('task_id', '')
        self.logger.debug(f"任务启动，激活轮询: {task_id}")
        self._start_poll_timer()

    def _on_task_ended(self, data: dict):
        """任务终结 → 检查是否需要停 timer"""
        task_id = data.get('task_id', '')
        self.logger.debug(f"任务终结: {task_id}")
        # 延迟检查（下一轮 poll_once 会自动判断）
        # 立即检查一次以加速停止
        self._ensure_timer_if_needed()

    def _on_status_changed(self, task_code: str, new_status: str, engine_event: str):
        """TaskStatusMonitor 检测到终态变化 → 注入引擎"""
        engine = self._get_workflow_engine()
        if not engine:
            return
        import asyncio
        try:
            loop = self.node.asyncio_loop
            asyncio.run_coroutine_threadsafe(
                engine.inject_event(task_code, engine_event, {
                    'task_code': task_code,
                    'task_status': new_status,
                    'source': 'task_status_monitor',
                }), loop
            )
            self.logger.info(f"注入事件: {task_code} → {engine_event} "
                             f"(status={new_status})")
        except Exception as e:
            self.logger.error(f"注入事件失败: {e}")

    # ════════════════════════════════════════════════════
    #  辅助
    # ════════════════════════════════════════════════════

    def _get_workflow_engine(self):
        engine = getattr(self.node, 'workflow_engine', None)
        if engine is None:
            engine = self.node.plugin_manager.get_plugin('workflow_engine')
        return engine

    def _get_rcs_adapter(self):
        adapter = getattr(self.node, 'rcs_adapter', None)
        if adapter is None:
            adapter = self.node.plugin_manager.get_plugin('rcs_adapter')
        return adapter

    # ════════════════════════════════════════════════════
    #  状态查询 (Web/运维)
    # ════════════════════════════════════════════════════

    def get_status(self) -> Dict[str, Any]:
        base = super().get_status()
        stats = self._monitor.get_stats() if self._monitor else {}
        base.update({
            'enabled': self._enabled,
            'poll_interval': self._poll_interval,
            'timer_active': self._poll_timer is not None,
            'total_polls': self._total_polls,
            'status_changes': self._status_changes,
            **stats,
        })
        return base

    def get_task_status(self, task_code: str) -> Optional[Dict[str, str]]:
        if self._monitor:
            s = self._monitor.get_last_known_status(task_code)
            if s:
                return {'task_code': task_code, 'status': s,
                        'status_name': TaskStatusCode.status_name(s)}
        return None

    def get_all_monitored(self) -> Dict[str, str]:
        return self._monitor.get_all_last_known() if self._monitor else {}
```

---

## 6. WorkflowEngine 增强接口

### 6.1 `get_active_monitoring_instances()`

在 `engine.py` 中新增（约第 180 行，`get_active_count` 之后）:

```python
def get_active_monitoring_instances(self) -> Dict[str, str]:
    """获取所有处于 monitoring 状态且 running 的活跃实例

    Returns:
        {task_code: rcs_task_no}  映射
        如: {'TASK_001': 'RCS_123_0001', 'TASK_002': 'RCS_123_0002'}

    注意: 返回的是新 dict，外部修改不影响 self._instances。
    """
    result = {}
    for task_id, instance in self._instances.items():
        if (instance.current_state == 'monitoring'
                and instance.status == 'running'):
            rcs_task_no = instance.context.get('rcs_task_no', '')
            if rcs_task_no:
                result[task_id] = rcs_task_no
    return result
```

### 6.2 恢复校准已集成

v0.0.7-6 的 `workflow_engine_plugin._activate_impl()` 已调用 `_validate_and_recover()`，该函数在恢复前对每个未完成实例做 RCS 状态查询。**task_status_monitor 无需关心恢复逻辑**——该校准在插件激活前已完成，monitor 只需处理正常运行时的新任务。

---

## 7. 配置文件修改

### 7.1 params.yaml — 新增配置节 + 更新 plugin_load_order

在 `plugin_configs` YAML 块中修改:

```yaml
plugin_configs: |
  plugin_load_order:
    [
      "network", "callback_handler", "rcs_adapter",
      "bay_status_fusion", "status_poller",
      "workflow_engine",
      "task_status_monitor",     # ← NEW: 任务状态轮询 (Phase 5)
      "smart_trigger",
      "object_status", "web_monitor",
    ]
  # ... 已有配置 ...

  # ── NEW ──
  task_status_monitor:
    auto_start: true
    enabled: true
    poll_interval: 10.0           # 轮询间隔 (秒), 建议 10~30
    poll_timeout: 8.0             # 单次 HTTP 请求超时 (秒)
```

### 7.2 加载顺序说明

| 顺序 | 插件 | 原因 |
|:---|:---|:---|
| 6 | workflow_engine | 提供 `get_active_monitoring_instances()` + `inject_event()` |
| **7** | **task_status_monitor** | **依赖 workflow_engine + rcs_adapter** |
| 8 | smart_trigger | 依赖 workflow_engine + Phase 2 缓存 |

### 7.3 注意: v0.0.7-6 已有 working schedule 配置

```yaml
work_schedule:
  schedules:
    - day_of_week: "mon-fri"
      start_time: "08:00"
      end_time: "22:00"
      enabled: true
    - day_of_week: "sat"
      start_time: "00:00"
      end_time: "23:59"
      enabled: true
```

`task_status_monitor` 的按需轮询**不受工作时间窗口限制**——只要有 monitoring 态任务就查询（因为任务已在下发阶段通过 smart_trigger 的时间窗口检查生效）。

---

## 8. 事件流与状态推进对照表

### 8.1 完整事件流（v2.0）

```
smart_trigger                         workflow_engine                        task_status_monitor
───────────                           ───────────────                        ───────────────────
  │                                      │                                       │
  ├─ trigger.task_created                │                                       │
  │  ─────────────────────────────────→  │                                       │
  │                                      ├─ _on_trigger_task_created()           │
  │                                      ├─ create_instance()                    │
  │                                      ├─ workflow.task_started ──────────→    │
  │                                      │  (EventBus)                           ├─ _on_task_started()
  │                                      │                                       ├─ _start_poll_timer()
  │                                      │                                       │
  │                                      ├─ calling_rcs → invoke_gen_task        │
  │                                      │   → POST /genAgvSchedulingTask        │
  │                                      │   → rcs_ack_ok → monitoring            │
  │                                      │                                       │
  │                                      ├─ monitoring (挂起, 100s timeout)       │
  │                                      │   wait_for_completion                  │
  │                                      │                                       ├─ Timer: poll_once()
  │                                      │                                       │   → engine.get_active_...()
  │                                      │                                       │   → {TASK_xxx: 'RCS_yyy'}
  │                                      │                                       │   → rcs.queryTaskStatus()
  │                                      │                                       │   → 检测变化
  │                                      │                                       │   → inject_event()
  │                                      │  ←── callback_completed ────────────  │
  │                                      │      (如果检测到 COMPLETED)            │
  │                                      │                                       │
  │                                      ├─ callback_completed → releasing       │
  │                                      │   → completed                         │
  │                                      ├─ workflow.task_completed ────────→    │
  │                                      │                                       ├─ _on_task_ended()
  │                                      │                                       │   → _ensure_timer_if_needed()
  │                                      │                                       │   → 无实例 → _stop_poll_timer()
  │                                      │                                       │
```

### 8.2 按需启停状态机

```
        ┌──────────┐  task_started    ┌──────────────┐  poll_if_needed returns True  ┌──────────┐
        │  IDLE    │ ───────────────→ │  ACTIVE      │ ─────────────────────────────→│  ACTIVE  │
        │ timer=None│                  │ timer=10s    │                               │  (持续)  │
        └──────────┘                  └──────────────┘                               └────┬─────┘
              ↑                             │                                           │
              │                             │ poll_if_needed returns False              │
              │                             │ (连续 2 轮)                               │
              └─────────────────────────────┘                                           │
                                                                                        │
                                          task_ended + ensure_timer → 无实例           │
                                          ─────────────────────────────────────────────┘
```

---

## 9. 端到端集成测试流程

### 9.1 环境

| 组件 | 地址 |
|:---|:---|
| jmsy-server-0.0.1 | `192.168.31.191:8080` |
| app_mgr_object-0.0.7-6 | 本机 (含 task_status_monitor) |
| bay_status_simulator | 本机 |

### 9.2 测试场景 1: 正常完成（轮询提前检测）

**操作**: `A+1` → 等待 RCS 任务执行完成

**预期日志（时间线）**:

```
T+0s   [smart_trigger] 触发任务: TASK_xxx
T+0s   [workflow_engine] workflow.task_started
T+0s   [task_status_monitor] 任务启动，激活轮询: TASK_xxx
T+0s   [task_status_monitor] 轮询定时器已启动 (间隔 10.0s)
T+0s   [workflow_engine] 任务下发成功: TASK_xxx → RCS taskNo=RCS_xxx

T+10s  [task_status_monitor] 第 1 轮轮询: 1 个任务
T+10s  [TaskMonitor] 状态变化: TASK_xxx ---→2 (RUNNING)

T+20s  [TaskMonitor] 第 2 轮轮询: 1 个任务
T+20s  [TaskMonitor] 状态变化: TASK_xxx 2→9 (COMPLETED)
T+20s  [TaskMonitor] 注入事件: TASK_xxx → callback_completed
T+20s  [Engine] 注入事件: task=TASK_xxx, event=callback_completed
T+20s  [Instance] 状态转移: monitoring → releasing → completed
T+20s  [task_status_monitor] 任务终结: TASK_xxx

T+30s  [task_status_monitor] 无活跃实例, 跳过第 1 次轮询
T+30s  [task_status_monitor] 轮询定时器已停止 (无活跃任务)
```

### 9.3 测试场景 2: 无任务时完全静默

**前置**: 系统启动后，不触发任何任务

**预期**:
- `task_status_monitor` 激活后不创建 timer
- 日志中只有 "TaskStatusMonitor v2 激活完成 (按需轮询模式)"
- 无任何 `第 N 轮轮询` 日志
- `timer_active: false`

**验证**:
```bash
# 检查 jmsy-server 日志 — 不应该有任何 /queryTaskStatus 请求
```

### 9.4 测试场景 3: 任务触发 → Timer 自动启动 → 任务完成 → Timer 自动停止

**完整流程**:

```
Phase 1 (无任务):
  [status] timer_active: false, monitored_tasks: 0

Phase 2 (触发 1 个任务):
  按 A+1 → timer_active: true, 开始轮询

Phase 3 (任务完成):
  回调或轮询检测到 COMPLETED → 任务完结

Phase 4 (Timer 自动停止):
  连续 2 轮无活跃实例 → timer_active: false
```

### 9.5 测试 checklist

| # | 场景 | 预期 | 验证 |
|:---|:---|:---|:---|
| 1 | 无任务启动 | timer 不创建, zero RCS calls | jmsy logs |
| 2 | 触发 1 个任务 | timer 自动创建, 每 10s 一次轮询 | monitor logs |
| 3 | 回调正常到达 | 轮询检测到 "9" 时跳过(去重) | 无重复 inject_event |
| 4 | 任务完成 | timer 自动停止 (连续 2 轮空闲) | monitor logs |
| 5 | 任务失败 (RCS 7/8) | 轮询检测并注入 callback_exception | monitor logs |
| 6 | 连续 3 个任务 | timer 持续活跃, 不反复启停 | monitor logs |
| 7 | 系统重启恢复 | _validate_and_recover 先校准, 校准后实例正常进入 monitor if running | engine logs |

---

## 10. 故障排查与注意事项

### 10.1 修改文件清单

| 文件 | 操作 | 行数 |
|:---|:---|:---|
| `components/task_status_monitor/__init__.py` | **新建** | ~4 行 |
| `components/task_status_monitor/monitor.py` | **新建** | ~180 行 |
| `plugins/task_status_monitor_plugin.py` | **新建** | ~220 行 |
| `components/workflow/engine.py` | **修改** | +15 行 (`get_active_monitoring_instances`) |
| `config/params.yaml` | **修改** | +8 行 |

### 10.2 常见问题

| 问题 | 原因 | 解决 |
|:---|:---|:---|
| Timer 频繁启停 | 任务短暂完成又立即触发新任务 | 两轮确认机制(连续 2 轮空闲才停) 可缓解；必要时调小 poll_interval |
| Timer 未启动 | `workflow.task_started` 事件未发出 | 检查 EventBridge.connect 是否在 monitor 激活前执行 |
| 轮询但状态不变 | RCS 侧任务正常执行中 | 正常行为，2=RUNNING 不注入 |
| 轮询发现 "9" 但引擎未响应 | inject_event 运行时实例已不在 monitoring | 检查是否回调先到达导致实例已进入 releasing |
| 轮询对 callback 的影响 | — | 无影响，去重由 last_known_status 保证 |

### 10.3 与已有 v0.0.7-6 修复的互操作性

| v0.0.7-6 修复 | 与 task_status_monitor 的关系 |
|:---|:---|
| compensate_cycle_count | 轮询能提前检测 COMPLETED，减少进入 compensating_query 的次数 |
| _validate_and_recover | 恢复校准在 monitor 激活前完成，恢复的实例正常进入 monitoring → monitor 自动开始轮询 |
| max_retry_reached | 如果轮询无法终结任务（RCS 永远 RUNNING），compensating 仍会触发上限 |

### 10.4 Web 面板集成

```python
monitor = node.plugin_manager.get_plugin('task_status_monitor')
monitor.get_status()
# → {'timer_active': True, 'monitored_tasks': 3, 'poll_count': 42, ...}
monitor.get_task_status('TASK_xxx')
# → {'task_code': 'TASK_xxx', 'status': '2', 'status_name': 'RUNNING'}
monitor.get_all_monitored()
# → {'TASK_001': '2', 'TASK_002': '9'}
```

---

## 附录: 与补偿查询的职责对比

| 维度 | compensating_query (已有) | task_status_monitor (v2.0) |
|:---|:---|:---|
| 触发方式 | monitoring 超时 (100s) | **事件驱动 Timer** (有任务→10s, 无任务→停止) |
| 查询对象 | 单个实例 | 批量所有 monitoring 实例 |
| 退出条件 | rcs_completed / rcs_failed / max_retry_reached | 连续 2 轮无活跃实例 |
| 去重 | 通常执行 1 次 | last_known_status 对比 |
| RCS 消耗 | 每个实例每 100s 最多 1 次 | 所有实例合并为 1 次/10s (有任务时) |
| 空闲时消耗 | 0 | 0 (timer 已停止) |

---

> **文档结束 (v2.0)**。核心改进：按需轮询 — 有 monitoring 任务时每 10s 批量查询，无任务时 timer 自动停止，零 RCS API 浪费。总新增代码约 420 行，需新建 3 个文件、修改 2 个文件。
