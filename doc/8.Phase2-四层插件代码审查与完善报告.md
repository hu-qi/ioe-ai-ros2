# Phase 2：四层插件与组件代码审查报告

> **审查对象**: `app_mgr_object-0.0.3(基础版)` 工程  
> **依据**: `4.任务管理应用-总体技术规划方案-v2.0.md` 第 4.1 ~ 4.4 节  
> **编制日期**: 2026-07-29  
> **审查范围**: 4 个插件层（rcs_adapter / rcs_callback_receiver / bay_status_fusion / status_poller）+ 所有相关 components  
> **目的**: 发现缺失、不一致及质量缺陷，给出完善后的代码与开发步骤

---

## 目录

1. [审查总览](#1-审查总览)
2. [整体架构审查](#2-整体架构审查)
3. [4.1 rcs_adapter_plugin — 缺失分析与完善](#3-41-rcs_adapter_plugin--缺失分析与完善)
4. [4.2 rcs_callback_receiver_plugin — 合并方案审查](#4-42-rcs_callback_receiver_plugin--合并方案审查)
5. [4.3 bay_status_fusion_plugin — 代码审查](#5-43-bay_status_fusion_plugin--代码审查)
6. [4.4 status_poller_plugin — 代码审查](#6-44-status_poller_plugin--代码审查)
7. [Components 组件层审查](#7-components-组件层审查)
8. [配置文件与注册表审查](#8-配置文件与注册表审查)
9. [完善操作步骤速查表](#9-完善操作步骤速查表)

---

## 1. 审查总览

| 插件 | 状态 | 关键结论 |
|:---|:---|:---|
| **4.1 rcs_adapter_plugin** | ❌ 缺失 | 文件不存在。是整个 Phase 2 的核心依赖，bay_status_fusion 和 status_poller 均调用 `_get_rcs_adapter()` 获取它。**必须创建** |
| **4.2 rcs_callback_receiver_plugin** | ⚠️ 已合并 | 未作为独立插件文件存在，但其功能已整合进 `callback_handler_plugin.py` 的 `register_rcs_callbacks()`。v2.0 规划为独立插件，当前简化合并方案可行，建议保持 |
| **4.3 bay_status_fusion_plugin** | ✅ 存在 | 代码完整，与 Phase 2 指南一致。依赖 rcs_adapter 但该插件缺失导致运行时报错 |
| **4.4 status_poller_plugin** | ✅ 存在 | 代码完整，与 Phase 2 指南一致。在 `__init__.py` 中被注释掉（`# from .status_poller_plugin import StatusPollerPlugin`） |
| **Components** | ✅ 完整 | rcs_client / task_cache / trigger / workflow 组件包齐全 |
| **params.yaml** | ⚠️ 不完整 | 缺少 `rcs_adapter` 和 `status_poller` 配置块，缺少 `dest_bay_configs`；`plugin_load_order` 未包含 Phase 2 插件 |
| **__init__.py** | ⚠️ 不完整 | `StatusPollerPlugin` 被注释，缺少 `RCSAdapterPlugin` |

**核心问题**: `rcs_adapter_plugin` 是整个数据融合层的起点。它封装了 RCSHttpClient 和 RCSPayloadBuilder，向上层插件暴露 RCS 接口方法。没有它，bay_status_fusion 无法执行 `bindCtnrAndBin`，status_poller 无法执行 `queryAgvStatus` / `queryPodBerthAndMat`。

**修复优先级**: `rcs_adapter_plugin.py` 新建 → `params.yaml` / `__init__.py` / `plugin_load_order` 更新 → status_poller 解除注释。

---

## 2. 整体架构审查

### 2.1 插件依赖链

```
network_plugin (已有)
  └── rcs_adapter_plugin        ← ❌ 缺失
        ├── rcs_callback_receiver  ← 已合并到 callback_handler
        ├── bay_status_fusion      ← ✅ 存在，但依赖缺失
        └── status_poller          ← ✅ 存在，但依赖缺失 + 注释
```

### 2.2 组件依赖关系

```
plugins/rcs_adapter_plugin.py          → components/rcs_client/rcs_http_client.py
                                       → components/rcs_client/rcs_payload_builder.py
                                       → components/rcs_client/rcs_response_parser.py
                                       → plugins/network_plugin.py (HTTP session)

plugins/bay_status_fusion_plugin.py    → components/device_status/area_filter.py
                                       → components/task_cache/bay_cache.py
                                       → plugins/rcs_adapter_plugin.py (bind_ctnr_and_bin)

plugins/status_poller_plugin.py        → components/task_cache/robot_cache.py
                                       → components/task_cache/dest_bay_cache.py
                                       → plugins/rcs_adapter_plugin.py (query_agv_status, query_pod_berth_and_mat)

plugins/callback_handler_plugin.py     → components/callback/receiver.py
                                       → plugins/bay_status_fusion_plugin.py (缓存同步)
                                       → plugins/status_poller_plugin.py (缓存同步)
```

### 2.3 rcs_callback_receiver 合并决策说明

v2.0 规划中 `rcs_callback_receiver_plugin` 作为独立插件，在其 `_activate_impl` 中调用 `self.node.callback_handler.register_post()` 注册三个 RCS 回调路由。当前 `app_mgr_object-0.0.3` 将这三个路由的注册和处理逻辑**直接放入 `callback_handler_plugin.py`**，具体体现在：

- `register_rcs_callbacks()` — 在 `_activate_impl` 中自动调用
- `_on_agv_callback()` / `_on_warn_callback()` / `_on_bind_notify()` — 三个处理函数
- `_sync_cache_on_bind_notify()` — 缓存同步

**合并优势**:
- 减少插件间启动顺序依赖（无需确保 `rcs_callback_receiver` 在 `callback_handler` 之后启动）
- 减少一个插件文件，降低工程复杂度
- 回调路由注册与 HTTP 服务器启动在同一生命周期内，时序更可靠

**建议**: 保持当前合并方案。文档中将此标记为"已合并到 callback_handler_plugin"。

---

## 3. 4.1 rcs_adapter_plugin — 缺失分析与完善

### 3.1 问题诊断

**现状**: `app_mgr_object-0.0.3(基础版)/app_mgr_object/plugins/` 目录下**没有 `rcs_adapter_plugin.py`**。

**影响**: 
- `bay_status_fusion_plugin._get_rcs_adapter()` 返回 `None` → 抛出 `RuntimeError("RCS 适配器未加载")`
- `status_poller_plugin._get_rcs_adapter()` 返回 `None` → 同上
- 所有需要与 RCS 系统通信的操作全部阻塞

**底层组件已就绪**: `rcs_http_client.py`、`rcs_payload_builder.py`、`rcs_response_parser.py` 三个组件文件均完整，只需插件层封装即可。

### 3.2 设计规格（来自 v2.0 第 4.1 节）

| 属性 | 值 |
|:---|:---|
| **PLUGIN_NAME** | `rcs_adapter` |
| **依赖** | `network`（NetworkPlugin 提供 HTTP session） |
| **职责** | 封装 RCS-2000 V3.3 全部 HTTP 接口，向上层插件暴露标准方法 |
| **使用的组件** | `RCSHttpClient` / `RCSPayloadBuilder` / `RCSResponseParser` |
| **暴露给节点** | `self.node.rcs_adapter = self` |
| **暴露接口** | `gen_task()`, `cancel_task()`, `continue_task()`, `query_agv_status()`, `query_task_status()`, `bind_ctnr_and_bin()`, `query_pod_berth_and_mat()` |

### 3.3 完善代码

**路径**: `app_mgr_object/plugins/rcs_adapter_plugin.py`

```python
#!/usr/bin/env python3
"""
rcs_adapter_plugin — RCS 接口适配器

职责：
  - 封装 RCS-2000 V3.3 全部对外任务接口
  - 向上层插件（bay_status_fusion / status_poller / workflow_engine）暴露标准方法
  - 管理 RCS 服务器连接配置（base_url / timeout / max_retries）

依赖：
  - network (NetworkPlugin — 提供 HTTP 会话)
  - RCSHttpClient / RCSPayloadBuilder / RCSResponseParser (组件层)

暴露方法：
  - gen_task(task_context)                → 生成调度任务
  - cancel_task(task_code, force)         → 取消任务
  - continue_task(task_code)              → 继续执行任务
  - query_agv_status(robot_ids)           → 查询 AGV 状态
  - query_task_status(task_codes)         → 查询任务状态
  - query_pod_berth_and_mat(bay_ids)      → 查询终点仓位绑定
  - bind_ctnr_and_bin(bay_id, cargo_type, ind_bind) → 绑定/解绑
"""

from typing import Dict, Any, List, Optional
import time

from .base_plugin import BasePlugin
from app_mgr_object.components.rcs_client.rcs_http_client import RCSHttpClient, RCSClientError
from app_mgr_object.components.rcs_client.rcs_payload_builder import RCSPayloadBuilder
from app_mgr_object.components.rcs_client.rcs_response_parser import (
    RCSResponseParser,
    RCSTaskStatus,
    RCSRobotStatus,
    RCSResponseCode,
)


class RCSAdapterPlugin(BasePlugin):
    """RCS-2000 V3.3 HTTP 接口适配器插件

    封装了 RCSHttpClient 的全部接口，向上层插件提供统一的 RCS 通信能力。
    所有方法均为同步调用，由调用方决定是否放入线程池。
    """

    PLUGIN_NAME = "rcs_adapter"
    PLUGIN_VERSION = "1.0.0"

    def __init__(self, node, config: Dict[str, Any] = None):
        super().__init__(node, config)

        # RCS 连接配置
        self._base_url = self.config.get('rcs_base_url', 'http://192.168.1.100:8080')
        self._timeout = self.config.get('rcs_timeout', 10.0)
        self._max_retries = self.config.get('rcs_max_retries', 2)
        self._enabled = self.config.get('enabled', True)

        # 组件实例
        self._client: Optional[RCSHttpClient] = None
        self._payload_builder: Optional[RCSPayloadBuilder] = None
        self._response_parser: Optional[RCSResponseParser] = None

    # ═══════════════════════════════════════════════════════════════
    #  生命周期
    # ═══════════════════════════════════════════════════════════════

    def _configure_impl(self) -> bool:
        """配置：初始化 RCSHttpClient 和工具组件"""
        try:
            # 获取 NetworkPlugin 实例
            network = self._get_network_plugin()

            # 创建 RCS HTTP 客户端
            self._client = RCSHttpClient(
                network_plugin=network,
                base_url=self._base_url,
                timeout=self._timeout,
                max_retries=self._max_retries,
                logger=self.logger,
            )

            # 创建辅助组件
            self._payload_builder = RCSPayloadBuilder()
            self._response_parser = RCSResponseParser()

            # 将自身挂载到节点，供其他插件通过 self.node.rcs_adapter 访问
            self.node.rcs_adapter = self

            self.logger.info(
                f"RCSAdapter 配置完成: base_url={self._base_url}, "
                f"timeout={self._timeout}s, max_retries={self._max_retries}"
            )
            return True

        except Exception as e:
            self.logger.error(f"RCSAdapter 配置失败: {e}")
            return False

    def _activate_impl(self) -> bool:
        """激活：执行连通性检查"""
        if not self._enabled:
            self.logger.info("RCSAdapter 已禁用")
            return True

        # 可选：启动时检查 RCS 连通性
        try:
            self._check_connectivity()
        except Exception as e:
            self.logger.warning(f"RCS 连通性检查失败（非致命）: {e}")

        return True

    def _deactivate_impl(self) -> bool:
        return True

    def _cleanup_impl(self) -> bool:
        self._client = None
        self._payload_builder = None
        self._response_parser = None
        return True

    # ═══════════════════════════════════════════════════════════════
    #  任务接口 (2.x)
    # ═══════════════════════════════════════════════════════════════

    def gen_task(self, task_context: Dict[str, Any],
                 task_typ: str = "F13", priority: int = 64) -> Dict[str, Any]:
        """生成 AGV 调度任务

        对应 RCS 接口: 2.1.1 genAgvSchedulingTask

        Args:
            task_context: 任务上下文，包含 task_id / src_bay / dst_bay / robot_id
            task_typ: 任务类型，默认 "F13"（巷道到工作台）
            priority: 优先级 1~127

        Returns:
            RCS 响应体，含 taskNo（任务流水号）

        Raises:
            RCSClientError: 请求失败
        """
        payload = self._payload_builder.build_task_payload(
            task_context, task_typ=task_typ, priority=priority
        )
        self.logger.info(f"下发 RCS 任务: {task_context.get('task_id')}")
        return self._client.gen_agv_scheduling_task(payload)

    def cancel_task(self, task_code: str, force_cancel: bool = True) -> Dict[str, Any]:
        """取消任务

        对应 RCS 接口: 2.1.3 cancelTask

        Args:
            task_code: RCS 任务号
            force_cancel: True=软取消（叉车背货回库），False=硬取消（就地丢弃）

        Returns:
            RCS 响应体
        """
        force_flag = "1" if force_cancel else "0"
        self.logger.info(f"取消 RCS 任务: {task_code}, force={force_flag}")
        return self._client.cancel_task(task_code, force_cancel=force_flag)

    def continue_task(self, task_code: str) -> Dict[str, Any]:
        """继续执行已暂停的任务

        对应 RCS 接口: 2.1.2 continueTask
        """
        self.logger.info(f"继续 RCS 任务: {task_code}")
        return self._client.continue_task(task_code)

    # ═══════════════════════════════════════════════════════════════
    #  查询接口 (3.x)
    # ═══════════════════════════════════════════════════════════════

    def query_agv_status(self, robot_ids: List[int]) -> Dict[str, Any]:
        """查询 AGV 叉车实时状态

        对应 RCS 接口: 3.1.10 queryAgvStatus

        Args:
            robot_ids: AGV 编号列表，如 [1001, 1002]

        Returns:
            RCS 响应体。data 数组中每项含 robotID / agvStatus / battery / positionCode
        """
        return self._client.query_agv_status(robot_ids)

    def query_task_status(self, task_codes: List[str]) -> Dict[str, Any]:
        """查询任务执行状态

        对应 RCS 接口: 3.1.9 queryTaskStatus

        Args:
            task_codes: RCS 任务号列表

        Returns:
            RCS 响应体。data 数组中每项含 taskCode / taskStatus
        """
        return self._client.query_task_status(task_codes)

    def query_pod_berth_and_mat(self, bay_ids: List[str]) -> Dict[str, Any]:
        """查询货架储位与物料批次关系

        对应 RCS 接口: 3.1.6 queryPodBerthAndMat

        用于判断终点仓位是否为空：ctnrCode 为空 → 仓位空闲。

        Args:
            bay_ids: 仓位编码列表，如 ["T2001", "T2002"]

        Returns:
            RCS 响应体。data 数组中每项含 stgBinCode / ctnrCode
        """
        return self._client.query_pod_berth_and_mat(bay_ids)

    # ═══════════════════════════════════════════════════════════════
    #  绑定接口 (3.x)
    # ═══════════════════════════════════════════════════════════════

    def bind_ctnr_and_bin(self,
                          bay_id: str,
                          cargo_type: int,
                          ind_bind: str = "1") -> Dict[str, Any]:
        """容器与仓位绑定/解绑

        对应 RCS 接口: 3.1.8 bindCtnrAndBin

        在起始仓位 AI 检测确认有货后，调用此接口绑定容器与仓位。
        绑定后该仓位才能被调度系统识别为"可调度"。

        Args:
            bay_id:     仓位编码（如 "A1001"）
            cargo_type: 货物类型编号（1~6）
            ind_bind:   "1"=绑定, "0"=解绑

        Returns:
            RCS 响应体
        """
        payload = self._payload_builder.build_bind_payload(
            bay_id, cargo_type, ind_bind
        )
        self.logger.debug(f"RCS 绑定请求: {bay_id} ind_bind={ind_bind}")
        return self._client.bind_ctnr_and_bin(
            ctnr_code=payload['ctnrCode'],
            ctnr_typ=payload['ctnrTyp'],
            stg_bin_code=payload['stgBinCode'],
            ind_bind=payload['indBind'],
        )

    # ═══════════════════════════════════════════════════════════════
    #  辅助方法
    # ═══════════════════════════════════════════════════════════════

    def _get_network_plugin(self):
        """获取 NetworkPlugin 实例"""
        network = getattr(self.node, 'network_plugin', None)
        if network is None:
            network = self.node.plugin_manager.get_plugin('network')
        if network is None:
            raise RuntimeError("NetworkPlugin 未加载，rcs_adapter 依赖 network 插件")
        return network

    def _check_connectivity(self):
        """检测 RCS 服务器连通性（通过查询 AGV 状态接口）"""
        try:
            self.logger.info(f"检测 RCS 连通性: {self._base_url}")
            resp = self._client.query_agv_status(self.config.get('test_robot_ids', []))
            if self._response_parser.is_success(resp):
                self.logger.info(f"RCS 连通性检测成功")
            else:
                self.logger.warning(
                    f"RCS 连通性检测返回非成功码: code={resp.get('code')}"
                )
        except RCSClientError as e:
            self.logger.warning(f"RCS 连通性检测失败: {e}")

    # ═══════════════════════════════════════════════════════════════
    #  状态查询
    # ═══════════════════════════════════════════════════════════════

    def get_status(self) -> Dict[str, Any]:
        base = super().get_status()
        base.update({
            'rcs_base_url': self._base_url,
            'rcs_timeout': self._timeout,
            'rcs_max_retries': self._max_retries,
            'enabled': self._enabled,
        })
        return base
```

### 3.4 rcs_adapter_plugin 开发步骤

| 步骤 | 操作 |
|:---|:---|
| 1 | 创建 `plugins/rcs_adapter_plugin.py`（复制上述代码） |
| 2 | 在 `plugins/__init__.py` 中添加 `from .rcs_adapter_plugin import RCSAdapterPlugin` |
| 3 | 在 `__init__.py` 的 `__all__` 中添加 `'RCSAdapterPlugin'` |
| 4 | 在 `params.yaml` 的 `plugin_configs` 中追加 `rcs_adapter` 配置块（见第 8 节） |
| 5 | 在 `plugin_load_order` 中将 `rcs_adapter` 放在 `network` 之后、`callback_handler` 之前（见第 8 节） |
| 6 | 启动验证：检查日志中 `RCSAdapter 配置完成` |

---

## 4. 4.2 rcs_callback_receiver_plugin — 合并方案审查

### 4.1 问题诊断

**现状**: `app_mgr_object-0.0.3` 中**没有独立的 `rcs_callback_receiver_plugin.py` 文件**。

**v2.0 规划**: 该插件应在 `_activate_impl` 中调用 `self.node.callback_handler.register_post()` 注册 `/agvCallback`、`/warnCallback`、`/bindNotify` 三个路由，并将 RCS JSON 回调转为 EventBus 事件。

**当前实现**: 三个路由的注册和处理逻辑已全部整合进 `callback_handler_plugin.py`：
- `register_rcs_callbacks()` 方法在 `_activate_impl()` 中自动调用
- 三个回调处理函数 `_on_agv_callback()` / `_on_warn_callback()` / `_on_bind_notify()` 
- `_sync_cache_on_bind_notify()` 负责同步更新 BayCache / DestBayCache

### 4.2 审查结论

| 审查项 | 结论 |
|:---|:---|
| 功能完整性 | ✅ 三个 RCS 回调路由全部注册且处理逻辑完整 |
| EventBus 事件发布 | ✅ `rcs.agv_callback` / `rcs.warn_callback` / `rcs.bind_notify` 三个事件正确发布 |
| 缓存同步 | ✅ `_sync_cache_on_bind_notify` 正确更新 BayCache 和 DestBayCache |
| 控制开关 | ✅ 通过 `rcs_callbacks_enabled` 配置项控制是否注册 |
| 日志记录 | ✅ 各回调入口均有 INFO/WARNING 级别日志 |
| 错误处理 | ✅ 每个回调返回标准 `{"code": "0"}` 响应，异常不泄露到 HTTP 层 |

**建议**: 保持当前合并方案。此方案比 v2.0 规划的独立插件更简洁，减少了插件间启动顺序依赖。

**无需额外操作**。callback_handler_plugin.py 的 RCS 回调部分代码完整可用。

---

## 5. 4.3 bay_status_fusion_plugin — 代码审查

### 5.1 审查结论

| 审查项 | 状态 | 评价 |
|:---|:---|:---|
| PLUGIN_NAME | ✅ | `bay_status_fusion`，与 v2.0 一致 |
| 生命周期方法 | ✅ | `_configure_impl` / `_activate_impl` / `_deactivate_impl` / `_cleanup_impl` 完整 |
| BayCache 初始化 | ✅ | 从 config 读取 `bay_configs` |
| AreaStatusFilter 复用 | ✅ | 按 bay_id 独立实例化，互不干扰 |
| 层级消息解析 | ✅ | `_on_bay_status()` 正确遍历 channels→areas |
| 降级兼容 | ✅ | `_on_bay_status_fallback()` 支持 JSON 格式 |
| 绑定流程 | ✅ | 先检查 `bind_status` 避免重复绑定 → `set_binding` → `bind_ctnr_and_bin` → `set_bound`/`set_unbound` |
| EventBus 事件 | ✅ | `bay.status_changed` 和 `bay.bound` 正确发布 |
| 错误回滚 | ✅ | 绑定失败/异常时调用 `set_unbound` 回滚状态 |
| 配置参数 | ✅ | `stable_threshold` / `filter_timeout` / `bind_enabled` / `ai_topic` 均可配 |

### 5.2 发现的问题

#### 问题 1: 依赖 rcs_adapter 缺失（阻塞性缺陷）

```python
# bay_status_fusion_plugin.py: _get_rcs_adapter()
def _get_rcs_adapter(self):
    adapter = getattr(self.node, 'rcs_adapter', None)
    if adapter is None:
        adapter = self.node.plugin_manager.get_plugin('rcs_adapter')
    if adapter is None:
        raise RuntimeError("RCS 适配器未加载")
    return adapter
```

**影响**: 当前 `rcs_adapter_plugin.py` 不存在，`self.node.rcs_adapter` 始终为 `None`，`plugin_manager.get_plugin('rcs_adapter')` 也会返回 `None`，导致 `_on_stable_cargo` 中绑定操作抛 `RuntimeError`。

**修复**: 创建 `rcs_adapter_plugin.py`（见第 3 节）。

#### 问题 2: AI 话题名称不一致

```python
# bay_status_fusion_plugin.py 中使用:
self._ai_topic = self.config.get('ai_topic', '/ai_bay_status')

# params.yaml 中配置为:
ai_topic: "/dev01_object_status"
```

**影响**: 实际部署时话题名称为 `/dev01_object_status`（与当前推理节点发布的话题一致），但默认值 `/ai_bay_status` 也是一个合理的默认值。只要 params.yaml 中正确配置，不影响功能。

**结论**: 无问题，配置值覆盖默认值正常工作。

---

## 6. 4.4 status_poller_plugin — 代码审查

### 6.1 审查结论

| 审查项 | 状态 | 评价 |
|:---|:---|:---|
| PLUGIN_NAME | ✅ | `status_poller`，与 v2.0 一致 |
| 生命周期方法 | ✅ | `_configure_impl` / `_activate_impl` / `_deactivate_impl` / `_cleanup_impl` 完整 |
| RobotCache 初始化 | ✅ | 从 `robot_ids` 配置创建 |
| DestBayCache 初始化 | ✅ | 从 `dest_bay_configs` 配置创建 |
| ROS2 Timer 使用 | ✅ | `create_timer` 而非 asyncio，与现有工程风格一致 |
| oneshot timer | ✅ | `create_timer(0.5, ..., oneshot=True)` 用于初始全量加载 |
| 轮询异常处理 | ✅ | 每次轮询 try/except，错误计数器递增 |
| 错误恢复 | ✅ | 成功轮询后错误计数器归零 |
| 禁用开关 | ✅ | `enabled` 配置控制 |

### 6.2 发现的问题

#### 问题 1: 在 __init__.py 中被注释（阻塞性缺陷）

```python
# plugins/__init__.py 当前:
# from .status_poller_plugin import StatusPollerPlugin
...
# 'StatusPollerPlugin',
```

**修复**: 取消注释。

#### 问题 2: 依赖 rcs_adapter 缺失（阻塞性缺陷）

与 bay_status_fusion_plugin 相同，`_get_rcs_adapter()` 会因为 rcs_adapter 插件不存在而抛 RuntimeError。

**修复**: 创建 `rcs_adapter_plugin.py`。

#### 问题 3: params.yaml 中缺少 status_poller 配置块

`params.yaml` 中有 `bay_status_fusion` 配置，但**没有 `status_poller` 配置块**和 `dest_bay_configs`。

**修复**: 在 `params.yaml` 中追加 `status_poller` 配置（见第 8 节）。

#### 问题 4: 终点仓位数据来源单一

当前 `dest_cache.update()` 只在轮询中调用。RCS 回调 `bindNotify` 的处理在 `callback_handler_plugin._sync_cache_on_bind_notify()` 中，但该方法的 `status_poller` 访问路径为：
```python
status_poller = getattr(self.node, 'status_poller', None)
```

如果 `status_poller` 被注释未加载，缓存同步将静默失败。

**修复**: 解除 `status_poller` 的注释（问题 1）。

---

## 7. Components 组件层审查

### 7.1 rcs_client 组件包（Phase 1 产出）

| 文件 | 状态 | 评价 |
|:---|:---|:---|
| `rcs_http_client.py` | ✅ 完整 | 封装全部 RCS-2000 V3.3 接口（任务/查询/绑定）+ `_post` 重试逻辑 |
| `rcs_payload_builder.py` | ✅ 完整 | 标准化任务/绑定/强制下发 payload 构造 |
| `rcs_response_parser.py` | ✅ 完整 | 提供统一响应解析 + 状态码常量类 |
| `__init__.py` | ✅ 完整 | 导出全部类 |

**审查发现**: 无问题。三个文件接口完整，用 `RCSClientError` 统一异常，`_post` 内置重试。

### 7.2 task_cache 缓存组件包（Phase 2 组件）

| 文件 | 状态 | 评价 |
|:---|:---|:---|
| `bay_cache.py` | ✅ 完整 | 24 仓位状态管理，线程安全，接口齐全 |
| `dest_bay_cache.py` | ✅ 完整 | 终点仓位管理，按类型筛选空仓位 |
| `robot_cache.py` | ✅ 完整 | 叉车状态管理，IDLE/BUSY/OFFLINE/ERROR |
| `config_cache.py` | ✅ 完整 | Web 可动态修改的业务配置热缓存 |
| `__init__.py` | ✅ 完整 | 全部导出 |

**审查发现**: 无问题。所有缓存类使用 `threading.RLock` 保证线程安全，Entry 使用 `__slots__` 优化内存。

### 7.3 可复用组件

| 文件 | 状态 | 评价 |
|:---|:---|:---|
| `device_status/area_filter.py` | ✅ 可复用 | AreaStatusFilter 被 bay_status_fusion_plugin 正确复用于 AI 防抖 |
| `callback/receiver.py` | ✅ 可复用 | CallbackReceiver 被 callback_handler_plugin 正确使用 |

### 7.4 trigger / workflow 组件包（Phase 3 产出）

| 文件 | 状态 |
|:---|:---|
| `trigger/trigger_engine.py` | ✅ 存在 |
| `trigger/schedule_checker.py` | ✅ 存在 |
| `trigger/resource_locker.py` | ✅ 存在 |
| `workflow/engine.py` | ✅ 存在 |
| `workflow/instance.py` | ✅ 存在 |
| `workflow/definition.py` | ✅ 存在 |
| `workflow/action_registry.py` | ✅ 存在 |
| `workflow/persistence.py` | ✅ 存在 |
| `workflow/event_bridge.py` | ✅ 存在 |

> Phase 3 组件存在但不在本次审查范围。后续 Phase 3 开发时需同步审查。

---

## 8. 配置文件与注册表审查

### 8.1 __init__.py 问题

**当前状态**:
```python
from .bay_status_fusion_plugin import BayStatusFusionPlugin
# from .status_poller_plugin import StatusPollerPlugin   ← 被注释

__all__ = [
    ...
    'BayStatusFusionPlugin',
    # 'StatusPollerPlugin',   ← 被注释
]
```

**缺少**: `RCSAdapterPlugin` import

**完善后**:
```python
# plugins/__init__.py
from .base_plugin import BasePlugin
from .network_plugin import NetworkPlugin
from .callback_handler_plugin import CallbackHandlerPlugin
from .web_monitor_plugin import WebMonitorPlugin
from .object_status_plugin import ObjectStatusPlugin

# Phase 1: RCS 接口层
from .rcs_adapter_plugin import RCSAdapterPlugin

# Phase 2: 数据融合层
from .bay_status_fusion_plugin import BayStatusFusionPlugin
from .status_poller_plugin import StatusPollerPlugin

__all__ = [
    'BasePlugin',
    'NetworkPlugin',
    'CallbackHandlerPlugin',
    'WebMonitorPlugin',
    'ObjectStatusPlugin',
    # Phase 1
    'RCSAdapterPlugin',
    # Phase 2
    'BayStatusFusionPlugin',
    'StatusPollerPlugin',
]
```

### 8.2 params.yaml 问题

**当前缺少**:
1. `rcs_adapter` 配置块（RCS base_url / timeout / max_retries）
2. `status_poller` 配置块（robot_ids / poll intervals / dest_bay_configs）
3. `plugin_load_order` 不包含 `rcs_adapter`、`bay_status_fusion`、`status_poller`

**完善后的 plugin_configs 追加内容**（在现有 YAML 的 `plugin_configs` 多行字符串中追加）:

```yaml
# ============ 在 plugin_configs: | 末尾追加 ============

rcs_adapter:
  auto_start: true
  enabled: true
  rcs_base_url: "http://192.168.1.100:8080"    # RCS-2000 服务地址
  rcs_timeout: 10.0                             # HTTP 请求超时（秒）
  rcs_max_retries: 2                            # 最大重试次数
  test_robot_ids: [1001, 1002]                  # 连通性测试用 AGV 列表

status_poller:
  auto_start: true
  enabled: true
  robot_ids: [1001, 1002]                        # AGV 编号列表
  robot_poll_interval: 3.0                       # 叉车状态轮询间隔（秒）
  dest_poll_interval: 60.0                       # 终点仓位轮询间隔（秒）
  dest_bay_configs:                               # 终点仓位配置
    T2001: { cargo_type: 1, floor: 2 }
    T2002: { cargo_type: 1, floor: 2 }
    T2003: { cargo_type: 2, floor: 2 }
    T2004: { cargo_type: 2, floor: 2 }
    T2005: { cargo_type: 3, floor: 2 }
    T2006: { cargo_type: 3, floor: 2 }
    T3001: { cargo_type: 4, floor: 3 }
    T3002: { cargo_type: 4, floor: 3 }
    T3003: { cargo_type: 5, floor: 3 }
    T3004: { cargo_type: 5, floor: 3 }
    T3005: { cargo_type: 6, floor: 3 }
    T3006: { cargo_type: 6, floor: 3 }
    T4001: { cargo_type: 1, floor: 4 }
    T4002: { cargo_type: 2, floor: 4 }
    T4003: { cargo_type: 3, floor: 4 }
    T4004: { cargo_type: 4, floor: 4 }
    T4005: { cargo_type: 5, floor: 4 }
    T4006: { cargo_type: 6, floor: 4 }
```

**完善后的 plugin_load_order**:

```yaml
plugin_load_order:
  [
    "network",                 # 网络层（最先，其他插件依赖）
    "callback_handler",        # HTTP 回调服务器（含 RCS 回调路由注册）
    "rcs_adapter",             # Phase 1: RCS 接口适配器（依赖 network）
    "bay_status_fusion",       # Phase 2: 数据融合（依赖 rcs_adapter）
    "status_poller",           # Phase 2: 状态轮询（依赖 rcs_adapter）
    "object_status",           # 物体状态检测（已有）
    "web_monitor",             # Web 运维监控（最后）
  ]
```

> **说明**: `rcs_adapter` 必须在 `bay_status_fusion` 和 `status_poller` 之前加载，因为这两个插件在 `_activate_impl` 中通过 `_get_rcs_adapter()` 获取 rcs_adapter 实例。`plugin_load_order` 决定了插件的 configure/activate 顺序。

---

## 9. 完善操作步骤速查表

### 9.1 需要新建的文件

| 文件 | 路径 | 说明 |
|:---|:---|:---|
| **rcs_adapter_plugin.py** | `plugins/rcs_adapter_plugin.py` | 见第 3.3 节完整代码 |

### 9.2 需要修改的文件

| 文件 | 当前问题 | 修改内容 |
|:---|:---|:---|
| `plugins/__init__.py` | 缺 RCSAdapterPlugin，StatusPollerPlugin 被注释 | 添加 RCSAdapterPlugin import，取消 StatusPollerPlugin 注释 |
| `config/params.yaml` | 缺 rcs_adapter 和 status_poller 配置块 + plugin_load_order 不完整 | 追加配置块 + 更新 plugin_load_order |

### 9.3 操作步骤速查

```
Phase 1 补齐（rcs_adapter 插件创建）:
1. 创建 plugins/rcs_adapter_plugin.py（复制第 3.3 节代码）
2. 修改 plugins/__init__.py：
   - 添加 from .rcs_adapter_plugin import RCSAdapterPlugin
   - 在 __all__ 中添加 'RCSAdapterPlugin'
   - 取消注释 from .status_poller_plugin import StatusPollerPlugin
   - 取消注释 __all__ 中的 'StatusPollerPlugin'

Phase 2 配置完善:
3. 修改 config/params.yaml：
   - 在 plugin_configs 中追加 rcs_adapter 配置块
   - 在 plugin_configs 中追加 status_poller 配置块
   - 更新 plugin_load_order 为完整顺序

Phase 3 验证:
4. pip install -e .
5. ros2 launch app_app.launch.py
6. 观察日志，验证：
   - [INFO] [rcs_adapter]: RCSAdapter 配置完成
   - [INFO] [rcs_adapter]: RCS 连通性检测成功
   - [INFO] [bay_status_fusion]: BayStatusFusion 配置完成
   - [INFO] [bay_status_fusion]: BayStatusFusion 激活完成
   - [INFO] [status_poller]: StatusPoller 配置完成
   - [INFO] [status_poller]: 初始全量加载完成
   - [INFO] [callback_handler]: 已注册 RCS 回调路由: POST /agvCallback
   - [INFO] [callback_handler]: 已注册 RCS 回调路由: POST /warnCallback
   - [INFO] [callback_handler]: 已注册 RCS 回调路由: POST /bindNotify
```

### 9.4 插件依赖与启动顺序图

```
启动顺序 (plugin_load_order):
─────────────────────────────────────────
[1] network             ← HTTP 会话基础
     │
[2] callback_handler    ← 注册 HTTP 路由 + RCS 回调
     │
[3] rcs_adapter         ← 封装 RCS 接口 (PHASE 1)
     │
     ├── [4] bay_status_fusion  ← AI → 绑定 (PHASE 2)
     │
     └── [5] status_poller      ← 定时轮询 (PHASE 2)
     │
[6] object_status       ← 物体状态检测 (已有)
     │
[7] web_monitor         ← Web 界面 (最后)
─────────────────────────────────────────
```

### 9.5 审查问题汇总

| # | 严重度 | 文件 | 问题 | 修复 |
|:---|:---|:---|:---|:---|
| 1 | 🔴 阻塞 | `rcs_adapter_plugin.py` | 文件不存在，bay_status_fusion / status_poller 依赖它 | 创建（第 3.3 节） |
| 2 | 🔴 阻塞 | `plugins/__init__.py` | `StatusPollerPlugin` 被注释 | 取消注释 |
| 3 | 🔴 阻塞 | `plugins/__init__.py` | 缺少 `RCSAdapterPlugin` | 添加 import |
| 4 | 🔴 阻塞 | `config/params.yaml` | 缺少 `rcs_adapter` 配置块 | 追加 |
| 5 | 🔴 阻塞 | `config/params.yaml` | 缺少 `status_poller` 配置块 | 追加 |
| 6 | 🔴 阻塞 | `config/params.yaml` | `plugin_load_order` 缺少 Phase 2 插件 | 更新 |
| 7 | 🟢 架构 | `rcs_callback_receiver_plugin.py` | 未作为独立文件存在，功能已合并到 callback_handler | 保持合并方案 |

---

> **文档结束**。Phase 2 四层插件 + 组件的审查结论：核心阻塞项为 **rcs_adapter_plugin 缺失**（1 个新文件 + 2 个文件修改）。rcs_callback_receiver 的合并方案合理，其他插件代码质量良好。组件层无问题。配置文件需要补齐 rcs_adapter / status_poller 配置块和 plugin_load_order。
