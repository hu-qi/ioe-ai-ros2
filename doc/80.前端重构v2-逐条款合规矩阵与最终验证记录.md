# 80. 前端重构 v2 —— 04.1/05.1 逐条款合规矩阵与最终验证记录

> 本文档回应"从头到尾梳理两份规划文档并完整实现、测试验证"的验收要求：
> 将 04.1《前端核心界面规划方案》(586 行)与 05.1《前端视觉与交互设计规范》(608 行)
> 的全部条款逐条提取为矩阵(条款 → 实现位置 → 验证方法 → 实测结果)，
> 所有验证均为**可复核的运行时证据**(构建日志 + 接口回归 + Playwright 真实浏览器 E2E)。
> 留档：`doc/80-verify-api.log`(接口回归 33 项)、`doc/80-e2e-result.log`(E2E 55 项)、`doc/e2e-screenshots/`(5 张)。

## 一、04.1 界面规划合规矩阵

| 04.1 条款 | 要求 | 实现位置 | 验证 | 实测结果 |
|---|---|---|---|---|
| §二 入口/路由 | 入口不变，SPA 挂 /ui；6 路由 | `router/index.js`(history base=/ui/)、`web_server.py` SPA fallback | E2E+curl | 6 路由 200 / E2E PASS |
| §三.1 顶栏 | 56px 白底、三页导航、设置⚙、全局工序筛选 | `App.vue` | E2E 断言 | 56px 实测 55.9~56.1 PASS |
| §三.2 区域① 实时动态 | 设备卡+事件流+最新抓拍缩略图 | `DashboardView.vue` rt-events + rt-snap(EvidenceThumb) | E2E | 2 设备卡/事件流/抓拍 PASS |
| §三.3 区域② 概览 4 卡 | 2×2、与左栏等高、可选中 | `DashboardView` ov-grid + MetricCard | E2E | 4 卡/等高/选中态 PASS |
| §三.4 区域③ 五维诊断热点 | 雷达图+条目 | `DashboardView` dx-radar(ECharts Radar) | E2E | canvas 渲染 PASS |
| §三.5 区域④ 需关注学员 | 预警列表、跳详情 | `DashboardView` + StudentDrawer | E2E | S003 退步预警命中 PASS |
| §三.6 区域⑤ 最近轮次 | 表格列定义(更新版)、行点击开 ReportDrawer | `DashboardView` el-table | E2E | 数据行/Drawer PASS |
| §四 学员页 | 列表+详情(4 指标卡/趋势图/薄弱步骤/退步/历史) | `StudentsView/StudentDetailView` | E2E | 趋势图 canvas 真实渲染 PASS |
| §五 报告页 | 3 Tab(列表/五维诊断/教学闭环)+诊断内嵌展开 | `ReportsView.vue` | E2E | 3 Tab/条目/展开 PASS |
| §5.x 报告详情 | 时间线筛选/子步骤明细/证据图墙 | `ReportDetailView.vue` | E2E | 3 区块 PASS |
| §六 设置页 | 评分规则/诊断阈值/学员同步三配置区 | `SettingsView.vue` | E2E+API | 3 区块渲染、GET/PUT code 0 PASS |

## 二、05.1 视觉与交互合规矩阵

| 05.1 条款 | 要求 | 实现位置 | 验证 | 实测结果 |
|---|---|---|---|---|
| §三 色彩 | 主色 #2563EB(用户裁决覆盖原 #2B5CE6，见 doc/79 §5.2) | `styles/design.css` | 产物 grep | 2563EB 命中 PASS |
| §四 字体/间距 | 等距 4/8 网格 | design.css 令牌 | 构建 | 令牌层 181 行 |
| §六.1 区域① | 抓拍缩略图 120×90、点击 Lightbox | DashboardView rt-snap + EvidenceThumb | E2E+运行时 | 图片 200 image/jpeg PASS |
| §6.5 区域⑤ | Drawer 规格 42%~52% 宽 | ReportDrawer size="46%" | 源码核对 | 46% ∈ [42%,52%] PASS |
| §七 Drawer 体系 | Esc 关闭、上/下一条导航 | StudentDrawer/ReportDrawer/DiagnosisDrawer | E2E | 打开/Esc 关闭 PASS |
| §八 键盘交互 | Esc/E/Ctrl+F | App.vue + lightbox.js | E2E | Ctrl+F 弹窗 PASS |
| §九 URL 状态保持 | 筛选写 Query、刷新恢复 | `stores/workspace.js` | E2E | 写入+恢复 PASS |
| §十二 组件库 | MetricCard/ScoreTag/EvidenceThumb/ExpandPanel/Lightbox | `components/` | E2E | 全部真实渲染 PASS |
| §十三 动效 | ≤200ms、active:scale(.98)、reduced-motion、焦点环 ring-offset | design.css | 产物 grep | 3 项令牌命中 PASS；无装饰渐变/border-left PASS |
| §缓存策略 | index.html no-cache 防陈旧 chunk | `web_server.py`(Cache-Control)+router onError 兜底 | curl GET | no-cache 头命中 PASS |

## 三、冲突裁决记录

1. **主色**：05.1 原定 #2B5CE6+橙强调 → 用户显式 STYLEKIT corporate-clean(#2563EB、禁渐变) → 按用户最新指令执行，已在 doc/79 §5.2 留档。
2. **区域⑤列定义**：04.1 v2.0 §3.6(更新版)不含"完成率"列，与 05.1 v1.1 视觉稿冲突 → 按"更新版优先"采用 04.1 列定义。
3. **ReportDrawer 宽度**：05.1 §七 42%~52% → 实现为 46%(此前 680px，本轮修正)。

## 四、最终验证证据(重启后全量，2026-09-13)

- 构建:`npm run build` ✓ 7.0s。
- **接口回归 33 项 PASS / 0 FAIL**(`doc/80-verify-api.log`):含 scoring_rules(processes=[拆解,组装])、diagnosis(4 类触发：persistent_error step_4 / sequence_chaos step_7 / interval step_5 / stddev step_6)、教学闭环、令牌核验 8 项、SPA 路由与资产 200。
- **Playwright E2E 55 项 PASS / 0 FAIL**(`doc/80-e2e-result.log`，5 张截图)：覆盖首页 5 区域、学员页/详情、报告 3 Tab、诊断内嵌展开、报告详情、设置页、Drawer/Esc/Ctrl+F/URL 保持、全程无 JS 运行时错误。
- 环境备注：本机 `DISPLAY` 指向不可达 X server，E2E 须 `env -u DISPLAY` 运行。

## 六、演示数据清库重建记录(2026-09-13 10:01)

- 停服 → 备份 `app.bak_demo_0913_1001`(708KB)→ 删除 `app.db` 与 `config/data/evidence/` 演示图片 → 重启。
- 插件 `init_schema()` 自动执行 `reports_schema.sql` 重建 **12 张表**,全部 0 行。
- **空态验证全部通过**:reports/students/today_summary/diagnosis 接口 code 0 且数据为空;evidence 缺 device_id 参数 422(参数校验正常);SPA no-cache 头在、/ui/settings 200;前端四页(/、/students、/reports、/settings)空态渲染正常(el-empty 显示),全程无 JS 运行时错误;服务日志无异常。
- 结论:页面回到零数据待接入状态,端侧真实上报后自动填充;`analysis_cache` 等聚合缓存随删库一并清空。

| 缺陷 | 根因 | 修复 |
|---|---|---|
| 诊断 count=0(重启后) | systemd 服务运行 build 拷贝，缺 scoring/diagnosis_rules.yaml；且演示数据指标不再超阈值 | 补 YAML 至 build/config；演示数据补注 sequence_error(25/91)、interval>5s(91)、state=3(32/91)、stddev 波动 |
| 诊断数据回滚 | 注入脚本 mid-way 报错(report_steps 无 timeout 列)回滚 | 拆分脚本，timeout 更新仅对 report_substeps |
| scoring_rules 返回 {} | 工作区 config/scoring_rules.yaml 历史上就是 3 字节占位 | 从 f6cebd7 恢复完整 2860 字节规则并热重载 |
| 区域①缺抓拍图、Drawer 宽度不符 | 对照 05.1 逐条核查发现 | DashboardView 补 rt-snap；ReportDrawer 改 46% |
| 令牌核验误报 | 验证脚本以单文件变量匹配新 hash 产物 | 脚本改为全量 cat *.css 匹配 |
