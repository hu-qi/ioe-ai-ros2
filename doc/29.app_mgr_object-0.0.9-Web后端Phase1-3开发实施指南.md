# app_mgr_object-0.0.9 Web 后端 Phase 1~3 开发实施指南

> 文档编号：29
> 目标工程：`app_mgr_object-0.0.9(基础版)-5`（已含 26/27 号修复，测试通过）
> 规划依据：[28.app_mgr_object-0.0.9-Web可视化与参数热加载实施指南.md](28.app_mgr_object-0.0.9-Web可视化与参数热加载实施指南.md)
> 本批范围：仅后端三阶段（前端 Phase 4 后续单独开发）
> ① 数据采集层（web_monitor_plugin.py）② API 层（web_server.py）③ 热加载机制（ParamManager + reload handlers + 配对覆盖）
> 说明：本文档给出可落地的完整代码与操作步骤；尚未修改 `-5` 工程源码，实施前请保存基线。

---

## 0. 实施范围与前置核对

### 0.1 本批实施内容

```
Phase 1 数据采集层   web_monitor_plugin.py  +9 采集方法 / 挂载 / 事件订阅
Phase 2 API 层       web_server.py           +20 REST 端点（只读业务9 + 配置10 + 任务操作1）
Phase 3 热加载机制   param_manager.py        YAML 读写 + 回调分发
                    web_monitor_plugin.py    reload_handlers + 配置应用
                    batch_matcher/trigger    pair_overrides + 优先级热加载
                    config/params.yaml       启用 web_monitor + 新配置项
```

### 0.2 `-5` 前置能力核对（2026-08-05 实测）

| 依赖能力 | 现状 | 说明 |
|---|---|---|
| `engine.get_active_instances / get_instance_snapshot / get_task_from_db / get_all_instances_from_db / get_exception_summary` | ✅ 已存在 | 采集层直接调用 |
| `bay_status_fusion.cache.get_all_bay_status()` | ✅ 已存在 | 起始仓位数据源 |
| `status_poller.robot_cache / dest_cache` | ✅ 已存在 | AGV / 终点仓位数据源 |
| `smart_trigger._task_queue.get_stats()`、`_trigger_engine.get_locked_count()` | ✅ 已存在 | 队列数据源 |
| `workflow_engine_plugin.get_engine()` | ✅ 已存在（27 号修复） | 任务操作中转 |
| `ParamManager._on_parameters_changed` | ⚠️ 只更新 `_params`，不回调 | Phase 3 需补充分发 |
| `BayCache.reload_config / DestBayCache.reload_config / ConfigCache.set_*` | ✅ 已存在 | 内存热应用直接复用 |
| `web_monitor` 加载 | ⚠️ 未加入白名单 | params.yaml 需启用 |
| `web_server` 路由 | 系统级 API 已有，业务 API 无 | Phase 2 新增 |

### 0.3 开发环境与验证工具

- 环境：ROS2 Humble + Python 3.10，`colcon build --packages-select app_mgr_object`
- 验证：`curl http://<host>:9183/api/...`、`ros2 param get /app_mgr_object plugin_configs`
- 模拟：`scripts/bay_status_simulator.py` + 上级目录 `jmsy-rcs_server` 模拟 RCS

---

## 1. 总实施步骤概览

| 步骤 | 文件 | 操作 | 验证 |
|---|---|---|---|
| S1 | `config/params.yaml` | 白名单启用 web_monitor；smart_trigger 增 pair_overrides | 启动日志出现"Web监控插件激活成功" |
| S2 | `plugins/web_monitor_plugin.py` | 挂载 node.web_monitor + `_get_plugin()` | 日志出现"v3.0 业务/配置能力已挂载" |
| S3 | `plugins/web_monitor_plugin.py` | +9 采集方法 + `collect_business_status()` | `curl /api/business/summary` 返回 JSON |
| S4 | `plugins/web_monitor_plugin.py` | 事件订阅 + `_on_business_event` | WebSocket 收到 `business_update` |
| S5 | `components/web/web_server.py` | `_setup_business_routes()` +20 端点 | `curl /api/bay/status` 返回 24 格数据 |
| S6 | `base/param_manager.py` | 回调分发 + YAML 持久化方法 | `ros2 param set` 触发回调日志 |
| S7 | `plugins/web_monitor_plugin.py` | reload_handlers + apply_config + task_control | PUT 配置 → 内存立即变化 + params.yaml 写回 |
| S8 | `components/trigger/batch_matcher.py` `trigger_engine.py` | pair_overrides + 优先级热加载 | 配置 A1005→T3005 后触发任务严格配对 |

建议合入顺序：S1→S2/S3/S4（Phase 1）→ S5（Phase 2）→ S6/S7/S8（Phase 3）。

---

## 2. Phase 1：数据采集层（web_monitor_plugin.py）

### 2.1 改动点总览

| 插入位置 | 内容 |
|---|---|
| `_configure_impl()` 成功分支（`return True` 前） | 挂载 `node.web_monitor` + 注册热加载处理器 |
| `_activate_impl()` 事件订阅块 | 增加业务事件订阅（`_subscribe_business_events`） |
| 类内新增（建议放在 `_get_interface_ip` 之前） | `_get_plugin` + 9 个采集方法 + `collect_business_status` |

### 2.2 改动一：`_configure_impl()` 末尾挂载

将原：

```python
                # 初始化组件配置
                self._init_components_config()

                self.logger.info(f"Web监控插件配置完成，端口: {web_config.get('port')}, 模式: {web_config.get('operation_mode')}")
                return True
```

替换为：

```python
                # 初始化组件配置
                self._init_components_config()

                # ===== v3.0：挂载自身 + 注册配置热加载处理器 =====
                self._register_reload_handlers()
                self.node.web_monitor = self

                self.logger.info(f"Web监控插件配置完成，端口: {web_config.get('port')}, "
                                 f"模式: {web_config.get('operation_mode')}, v3.0 业务/配置能力已挂载")
                return True
```

### 2.3 改动二：`_activate_impl()` 业务事件订阅

将原事件订阅块：

```python
            if self.event_bus:
                self.event_bus.subscribe('slot_status_changed', self._on_slot_status_changed)
                self.logger.info("✅ 已订阅 slot_status_changed 事件，巷道状态将实时推送")
            else:
                self.logger.error("❌ 事件总线不可用，无法实时推送巷道状态变化")
```

替换为：

```python
            if self.event_bus:
                self.event_bus.subscribe('slot_status_changed', self._on_slot_status_changed)
                self.logger.info("✅ 已订阅 slot_status_changed 事件，巷道状态将实时推送")
                # ===== v3.0：业务事件订阅 =====
                self._subscribe_business_events()
            else:
                self.logger.error("❌ 事件总线不可用，无法实时推送巷道状态变化")
```

### 2.4 改动三：新增采集方法（完整代码块）

在类中新增以下方法（建议插入到 `_get_interface_ip` 之前）：

```python
    # ═══════════════════════════════════════════════════════
    # v3.0 业务数据采集层（Phase 1）
    # ═══════════════════════════════════════════════════════

    def _get_plugin(self, name: str):
        """安全获取业务插件实例（优先节点挂载，其次插件管理器）"""
        plugin = getattr(self.node, name, None)
        if plugin is None and hasattr(self.node, 'plugin_manager'):
            plugin = self.node.plugin_manager.get_plugin(name)
        return plugin

    def _subscribe_business_events(self):
        """订阅业务事件 → 实时 WebSocket 推送"""
        events = [
            'bay.bound', 'bay.status_changed',
            'trigger.task_created',
            'workflow.task_started', 'workflow.task_completed',
            'workflow.task_failed', 'workflow.task_needs_manual',
        ]
        for evt in events:
            self.event_bus.subscribe(evt, self._on_business_event)
        self.logger.info(f"✅ 已订阅 {len(events)} 个业务事件（实时推送）")

    def _on_business_event(self, data: dict):
        """业务事件 → 立即推送业务汇总"""
        try:
            self._broadcast_message({
                'type': 'business_update',
                'timestamp': time.time(),
                'data': self.collect_business_status(),
            })
        except Exception as e:
            self.logger.debug(f"业务事件推送失败: {e}")

    def get_bay_status(self) -> dict:
        """采集起始仓位状态（BayCache）"""
        try:
            fusion = self._get_plugin('bay_status_fusion')
            if not fusion or not fusion.cache:
                return {'bays': [], 'total': 0}
            bays = fusion.cache.get_all_bay_status()
            now = time.time()
            result, by_type = [], {}
            for bid, bay in bays.items():
                ct = bay.get('cargo_type', 0)
                by_type.setdefault(str(ct), {'total': 0, 'bound': 0, 'available': 0})
                by_type[str(ct)]['total'] += 1
                if bay.get('bind_status') == 1:
                    by_type[str(ct)]['bound'] += 1
                    if not bay.get('in_task'):
                        by_type[str(ct)]['available'] += 1
                result.append({
                    'bay_id': bay['bay_id'],
                    'cargo_type': ct,
                    'bind_status': bay.get('bind_status', 0),
                    'bind_status_label': {0: '未绑定', 1: '已绑定', 2: '绑定中'}.get(
                        bay.get('bind_status', 0), '未知'),
                    'ai_detected': bay.get('ai_detected', False),
                    'in_task': bay.get('in_task', False),
                    'bind_time': bay.get('bind_time', 0),
                    'bind_duration': round(now - bay.get('bind_time', now), 1)
                        if bay.get('bind_status') == 1 else 0,
                    'last_seen': bay.get('last_seen', 0),
                })
            return {
                'timestamp': now, 'total': len(result),
                'bound_count': sum(1 for b in result if b['bind_status'] == 1),
                'in_task_count': sum(1 for b in result if b['in_task']),
                'by_type': by_type, 'bays': result,
            }
        except Exception as e:
            self.logger.error(f"采集起始仓位状态失败: {e}")
            return {'bays': [], 'total': 0, 'error': str(e)}

    def get_dest_status(self) -> dict:
        """采集终点仓位状态（DestBayCache）"""
        try:
            poller = self._get_plugin('status_poller')
            if not poller or not poller.dest_cache:
                return {'bays': [], 'total': 0}
            bays = poller.dest_cache.get_all_bay_status()
            result, by_floor = [], {}
            for bid, bay in bays.items():
                floor = bay.get('floor_no', 0)
                by_floor.setdefault(str(floor), {'total': 0, 'empty': 0})
                by_floor[str(floor)]['total'] += 1
                if bay.get('is_empty'):
                    by_floor[str(floor)]['empty'] += 1
                result.append({
                    'bay_id': bay['bay_id'],
                    'cargo_type': bay.get('cargo_type', 0),
                    'floor_no': floor,
                    'is_empty': bay.get('is_empty', True),
                    'update_time': bay.get('update_time', 0),
                })
            return {
                'timestamp': time.time(), 'total': len(result),
                'empty_count': sum(1 for b in result if b['is_empty']),
                'by_floor': by_floor, 'bays': result,
            }
        except Exception as e:
            self.logger.error(f"采集终点仓位状态失败: {e}")
            return {'bays': [], 'total': 0, 'error': str(e)}

    def get_agv_status(self) -> dict:
        """采集 AGV 状态（RobotCache）"""
        try:
            poller = self._get_plugin('status_poller')
            if not poller or not poller.robot_cache:
                return {'robots': [], 'available_count': 0}
            robots = poller.robot_cache.get_all_robot_status()
            result = [{
                'robot_id': rid, 'status': r.get('status', 'OFFLINE'),
                'battery': r.get('battery', 0), 'position_code': r.get('position_code', ''),
                'current_task_id': r.get('current_task_id', ''),
                'update_time': r.get('update_time', 0),
            } for rid, r in robots.items()]
            return {
                'timestamp': time.time(),
                'available_count': sum(1 for r in result if r['status'] == 'AVAILABLE'),
                'busy_count': sum(1 for r in result if r['status'] == 'BUSY'),
                'robots': result,
            }
        except Exception as e:
            self.logger.error(f"采集 AGV 状态失败: {e}")
            return {'robots': [], 'available_count': 0}

    def get_task_queue(self) -> dict:
        """采集任务队列状态（TaskQueue + 资源锁）"""
        try:
            trigger = self._get_plugin('smart_trigger')
            if not trigger or not getattr(trigger, '_task_queue', None):
                return {'queue_size': 0, 'queued_tasks': []}
            stats = trigger._task_queue.get_stats()
            engine = trigger._trigger_engine
            return {
                'queue_size': stats.get('queue_size', 0),
                'enqueue_count': stats.get('enqueue_count', 0),
                'dequeue_count': stats.get('dequeue_count', 0),
                'skip_duplicate_count': stats.get('skip_duplicate_count', 0),
                'aged_out_count': stats.get('aged_out_count', 0),
                'oldest_wait': stats.get('oldest_wait', 0),
                'queued_tasks': [
                    {'src_bay': c.src_bay, 'dst_bay': c.dst_bay,
                     'cargo_type': c.cargo_type, 'enqueue_time': c.enqueue_time}
                    for c in trigger._task_queue._queue
                ],
                'locked_resources': engine.get_locked_count() if engine else {},
            }
        except Exception as e:
            self.logger.error(f"采集任务队列失败: {e}")
            return {'queue_size': 0, 'queued_tasks': []}

    def get_workflow_instances(self) -> dict:
        """采集活跃工作流实例（WorkflowEngine 公开 API）"""
        try:
            wf = self._get_plugin('workflow_engine')
            if not wf:
                return {'active_count': 0, 'by_state': {}, 'instances': []}
            engine = wf.get_engine()
            by_state, instances = {}, []
            for task_id, snap in engine.get_active_instances().items():
                state = snap['current_state']
                by_state[state] = by_state.get(state, 0) + 1
                instances.append({
                    'task_id': task_id,
                    'current_state': state,
                    'status': snap['status'],
                    'src_bay': snap['src_bay'], 'dst_bay': snap['dst_bay'],
                    'robot_id': snap['robot_id'], 'cargo_type': snap['cargo_type'],
                    'rcs_task_no': snap['rcs_task_no'],
                })
            return {'active_count': len(instances), 'by_state': by_state, 'instances': instances}
        except Exception as e:
            self.logger.error(f"采集工作流实例失败: {e}")
            return {'active_count': 0, 'by_state': {}, 'instances': []}

    def get_task_stats(self) -> dict:
        """任务统计（引擎公开 API + 历史数据内存统计）"""
        try:
            wf = self._get_plugin('workflow_engine')
            if not wf:
                return {}
            engine = wf.get_engine()
            today = time.strftime('%Y-%m-%d')
            rows = engine.get_all_instances_from_db(limit=500)
            today_rows = [r for r in rows if str(r.get('created_at', '')).startswith(today)]
            by_state = {}
            for r in today_rows:
                st = r.get('current_state', 'unknown')
                by_state[st] = by_state.get(st, 0) + 1
            return {
                'today': {
                    'total': len(today_rows),
                    'completed': by_state.get('completed', 0),
                    'failed': by_state.get('failed', 0),
                    'manual_intervention': by_state.get('manual_intervention', 0),
                    'running': len(today_rows) - by_state.get('completed', 0)
                               - by_state.get('failed', 0),
                },
                'exception_summary': engine.get_exception_summary(),
            }
        except Exception as e:
            self.logger.error(f"采集任务统计失败: {e}")
            return {}

    def get_task_history(self, limit: int = 50, offset: int = 0,
                         state: str = '') -> dict:
        """历史任务（分页 + 状态过滤；中小数据量内存过滤）"""
        try:
            wf = self._get_plugin('workflow_engine')
            if not wf:
                return {'total': 0, 'tasks': []}
            engine = wf.get_engine()
            rows = engine.get_all_instances_from_db(limit=500)
            if state:
                rows = [r for r in rows if r.get('current_state') == state]
            total = len(rows)
            page = rows[offset:offset + limit]
            return {
                'total': total, 'limit': limit, 'offset': offset,
                'tasks': [{
                    'task_id': r.get('task_id'), 'workflow_name': r.get('workflow_name'),
                    'current_state': r.get('current_state'), 'status': r.get('status'),
                    'src_bay': r.get('src_bay'), 'dst_bay': r.get('dst_bay'),
                    'robot_id': r.get('robot_id'), 'cargo_type': r.get('cargo_type'),
                    'created_at': r.get('created_at'), 'completed_at': r.get('completed_at'),
                } for r in page],
            }
        except Exception as e:
            self.logger.error(f"采集历史任务失败: {e}")
            return {'total': 0, 'tasks': []}

    def get_trigger_status(self) -> dict:
        """触发器 + 轮询监控统计"""
        try:
            result = {}
            trigger = self._get_plugin('smart_trigger')
            if trigger and hasattr(trigger, 'get_status'):
                result['smart_trigger'] = trigger.get_status()
            monitor = self._get_plugin('task_status_monitor')
            if monitor and hasattr(monitor, 'get_status'):
                result['task_monitor'] = monitor.get_status()
            return result
        except Exception as e:
            self.logger.error(f"采集触发器状态失败: {e}")
            return {}

    def collect_business_status(self) -> dict:
        """聚合业务数据（首页一次拉取 / WebSocket 全量推送）"""
        return {
            'type': 'business_summary',
            'timestamp': time.time(),
            'bay': self.get_bay_status(),
            'dest': self.get_dest_status(),
            'agv': self.get_agv_status(),
            'queue': self.get_task_queue(),
            'workflow': self.get_workflow_instances(),
            'stats': self.get_task_stats(),
            'trigger': self.get_trigger_status(),
        }
```

### 2.5 Phase 1 验证

```bash
curl -s http://127.0.0.1:9183/api/business/summary | python3 -m json.tool
# 期望: bay.total=24, dest.total=18, agv.robots=2, workflow.active_count>=0

---

## 3. Phase 2：API 层（web_server.py）

### 3.1 改动点总览

| 插入位置 | 内容 |
|---|---|
| `__init__` 末尾（health_check 路由之后、方法结束前） | 调用 `self._setup_business_routes()` |
| 类内新增 | `_setup_business_routes()` 完整方法（+20 端点） |

### 3.2 调用点：`__init__` 末尾

在 `__init__` 的 `health_check` 路由定义之后追加一行：

```python
        # ==================== 业务/配置/任务路由（v3.0） ====================
        self._setup_business_routes()
```

### 3.3 新增端点清单（20 个）

| # | 方法 | 路径 | 说明 |
|---|---|---|---|
| 1 | GET | `/api/bay/status` | 起始仓位 |
| 2 | GET | `/api/dest/status` | 终点仓位 |
| 3 | GET | `/api/agv/status` | AGV |
| 4 | GET | `/api/task/queue` | 任务队列 |
| 5 | GET | `/api/task/instances` | 活跃工作流 |
| 6 | GET | `/api/task/stats` | 任务统计 |
| 7 | GET | `/api/task/history?limit=&offset=&state=` | 历史任务 |
| 8 | GET | `/api/trigger/status` | 触发器/轮询统计 |
| 9 | GET | `/api/business/summary` | 聚合 |
| 10 | GET | `/api/config/all` | 全部配置聚合 |
| 11-15 | GET | `/api/config/{bay,dest,schedule,pairs,params}` | 单段配置读 |
| 16-19 | PUT | `/api/config/{bay,dest,schedule,pairs}` | 段级热加载写 |
| 20 | PUT | `/api/config/params` | 顶层 ROS2 参数热加载写 |
| 21 | POST | `/api/task/control` | 任务人工操作 |

### 3.4 完整代码：`_setup_business_routes()` 方法

在 `WebServer` 类中新增（建议放在 `_start_websocket_broadcast` 之前）：

```python
    # ═══════════════════════════════════════════════════════
    # 业务可视化 / 配置热加载 / 任务操作 路由（v3.0，Phase 2）
    # ═══════════════════════════════════════════════════════
    def _setup_business_routes(self):
        """业务路由：只读业务 / 配置读写 / 任务操作（在 __init__ 末尾调用一次）"""

        def _wm():
            """获取 web_monitor 插件实例"""
            wm = getattr(self.node, 'web_monitor', None)
            if wm is None and hasattr(self.node, 'plugin_manager'):
                wm = self.node.plugin_manager.get_plugin('web_monitor')
            return wm

        def _not_loaded():
            return JSONResponse(
                content={'success': False, 'error': 'web_monitor 未加载'})

        # ──────────────────── 只读业务状态 ────────────────────
        @self.app.get("/api/bay/status")
        def api_bay_status():
            wm = _wm()
            return JSONResponse(content=wm.get_bay_status() if wm else _not_loaded())

        @self.app.get("/api/dest/status")
        def api_dest_status():
            wm = _wm()
            return JSONResponse(content=wm.get_dest_status() if wm else _not_loaded())

        @self.app.get("/api/agv/status")
        def api_agv_status():
            wm = _wm()
            return JSONResponse(content=wm.get_agv_status() if wm else _not_loaded())

        @self.app.get("/api/task/queue")
        def api_task_queue():
            wm = _wm()
            return JSONResponse(content=wm.get_task_queue() if wm else _not_loaded())

        @self.app.get("/api/task/instances")
        def api_task_instances():
            wm = _wm()
            return JSONResponse(content=wm.get_workflow_instances() if wm else _not_loaded())

        @self.app.get("/api/task/stats")
        def api_task_stats():
            wm = _wm()
            return JSONResponse(content=wm.get_task_stats() if wm else _not_loaded())

        @self.app.get("/api/task/history")
        def api_task_history(limit: int = 50, offset: int = 0, state: str = ''):
            wm = _wm()
            if not wm:
                return _not_loaded()
            return JSONResponse(content=wm.get_task_history(limit, offset, state))

        @self.app.get("/api/trigger/status")
        def api_trigger_status():
            wm = _wm()
            return JSONResponse(content=wm.get_trigger_status() if wm else _not_loaded())

        @self.app.get("/api/business/summary")
        def api_business_summary():
            wm = _wm()
            return JSONResponse(content=wm.collect_business_status() if wm else _not_loaded())

        # ──────────────────── 配置读写（热加载） ────────────────────
        CONFIG_GETTERS = {
            'bay': 'get_bay_config',
            'dest': 'get_dest_config',
            'schedule': 'get_schedule_config',
            'pairs': 'get_pair_overrides',
            'params': 'get_params_config',
        }
        CONFIG_SECTIONS = {
            'bay': 'bay_configs',
            'dest': 'dest_bay_configs',
            'schedule': 'work_schedule',
            'pairs': 'pair_overrides',
        }

        @self.app.get("/api/config/all")
        def api_get_config_all():
            wm = _wm()
            if not wm:
                return _not_loaded()
            return JSONResponse(content={
                'bay': wm.get_bay_config(),
                'dest': wm.get_dest_config(),
                'schedule': wm.get_schedule_config(),
                'pairs': wm.get_pair_overrides(),
                'params': wm.get_params_config(),
                'timestamp': time.time(),
            })

        @self.app.get("/api/config/{name}")
        def api_get_config(name: str):
            wm = _wm()
            if not wm:
                return _not_loaded()
            getter = CONFIG_GETTERS.get(name)
            if not getter or not hasattr(wm, getter):
                return JSONResponse(
                    content={'success': False, 'error': f'未知配置: {name}'})
            return JSONResponse(content=getattr(wm, getter)())

        @self.app.put("/api/config/{name}")
        async def api_put_config(name: str, request: Request):
            wm = _wm()
            if not wm:
                return _not_loaded()
            data = await request.json()
            if name == 'params':
                result = wm.apply_params(data.get('params', {}))
            else:
                section = CONFIG_SECTIONS.get(name)
                if not section:
                    return JSONResponse(
                        content={'success': False, 'error': f'未知配置: {name}'})
                value = data.get('mappings', data)
                result = wm.apply_config(section, value)
            return JSONResponse(content=result)

        # ──────────────────── 任务人工操作 ────────────────────
        @self.app.post("/api/task/control")
        async def api_task_control(request: Request):
            wm = _wm()
            if not wm:
                return _not_loaded()
            data = await request.json()
            result = wm.task_control(
                operation=data.get('operation', ''),
                task_id=data.get('task_id', ''),
                src_bay=data.get('src_bay', ''),
                dst_bay=data.get('dst_bay', ''),
                robot_id=int(data.get('robot_id') or 0),
                cargo_type=int(data.get('cargo_type') or 1),
            )
            return JSONResponse(content=result)
```

### 3.5 WebSocket 复用说明

- 前端连接：复用现有 `ws://<host>:9183/ws`（无需新增 WS 端点）；
- 实时推送：Phase 1 的 `_on_business_event` 通过 `self._broadcast_message()`（内部走 `web_server.manager.schedule_broadcast`）推送 `business_update`；
- 周期推送：`web_monitor._broadcast_status_update` 保持 `status_update` 不变，业务数据由事件驱动推送，避免高频全量广播。

### 3.6 Phase 2 验证

```bash
for ep in bay/status dest/status agv/status task/queue task/instances \
          task/stats trigger/status business/summary config/all; do
  echo "== /api/$ep =="; curl -s "http://127.0.0.1:9183/api/$ep" | head -c 300; echo
done
curl -s "http://127.0.0.1:9183/api/task/history?limit=5&state=completed" | python3 -m json.tool

---

## 4. Phase 3：热加载机制

### 4.1 改动点总览

| 文件 | 改动 |
|---|---|
| `base/param_manager.py` | `_on_parameters_changed` 补充分发；新增 YAML 持久化 + `update_plugin_config_section` + `set_param_and_persist` |
| `plugins/web_monitor_plugin.py` | `_register_reload_handlers` + `apply_config` + 校验 + 5 个 `_apply_*` + 配置读取 + `apply_params` + `task_control` |
| `components/trigger/batch_matcher.py` | `pair_overrides` + `set_pair_overrides` + `set_cargo_priority` + greedy 改造 |
| `components/trigger/trigger_engine.py` | matcher 注入 pair_overrides + `set_pair_overrides` + `set_cargo_priority` |

### 4.2 param_manager.py 增强

#### 4.2.1 替换 `_on_parameters_changed`（补充分发注册回调）

原代码：

```python
    def _on_parameters_changed(self, params: List[rclpy.Parameter]):
        for param in params:
            name = param.name
            value = param.value
            if isinstance(value, str) and value.startswith(('{', '[')):
                try:
                    value = json.loads(value)
                except json.JSONDecodeError:
                    pass
            self._params[name] = value
        return rclpy.SetParametersResult(successful=True)
```

替换为：

```python
    def _on_parameters_changed(self, params: List[rclpy.Parameter]):
        for param in params:
            name = param.name
            value = param.value
            if isinstance(value, str) and value.startswith(('{', '[')):
                try:
                    value = json.loads(value)
                except json.JSONDecodeError:
                    try:
                        value = yaml.safe_load(value)
                    except Exception:
                        pass
            self._params[name] = value

            # 热加载基础：分发参数变更回调（供插件重建定时器/刷新缓存）
            cb = self._param_callbacks.get(name)
            if cb:
                try:
                    cb(name, value)
                except Exception as e:
                    self.logger.error(f"参数回调执行失败 {name}: {e}")

        return rclpy.SetParametersResult(successful=True)
```

#### 4.2.2 新增配置持久化与热加载方法（放在 `update_param` 之后）

```python
    # ---------- 配置热加载（v3.0，Phase 3） ----------

    def _get_params_yaml_path(self) -> str:
        """获取 params.yaml 绝对路径（config_file 参数优先，其次 ament 包目录）"""
        import os
        override = self.get_param('config_file', '')
        if override:
            return override
        try:
            from ament_index_python.packages import get_package_share_directory
            pkg_share = get_package_share_directory('app_mgr_object')
            return os.path.join(pkg_share, 'config', 'params.yaml')
        except Exception:
            return os.path.join(os.getcwd(), 'config', 'params.yaml')

    def _read_yaml(self, path: str) -> dict:
        with open(path, 'r', encoding='utf-8') as f:
            return yaml.safe_load(f) or {}

    def _backup_and_write_yaml(self, path: str, config: dict) -> None:
        """写回前自动备份（.bak），避免写坏导致重启失败"""
        import shutil
        try:
            shutil.copyfile(path, f"{path}.bak")
        except Exception:
            pass
        with open(path, 'w', encoding='utf-8') as f:
            yaml.dump(config, f, allow_unicode=True, default_flow_style=False)

    def update_plugin_config_section(self, section: str, new_value: Any) -> bool:
        """更新 plugin_configs 指定段并写回 params.yaml，同时同步 ROS2 参数。

        Args:
            section: plugin_configs 下的配置段名（bay_configs / work_schedule / ...）
            new_value: 新配置值（dict / list）
        """
        try:
            path = self._get_params_yaml_path()
            config = self._read_yaml(path)
            app = config.setdefault('app_mgr_object', {}) \
                        .setdefault('ros__parameters', {})
            raw = app.get('plugin_configs', '{}')
            if isinstance(raw, str):
                plugin_configs = yaml.safe_load(raw) or {}
            else:
                plugin_configs = raw or {}
            plugin_configs[section] = new_value

            new_plugin_str = yaml.dump(plugin_configs, allow_unicode=True,
                                       default_flow_style=False)
            app['plugin_configs'] = new_plugin_str
            self._backup_and_write_yaml(path, config)

            # 同步 ROS2 参数，触发 _on_parameters_changed（含回调分发）
            param = rclpy.Parameter('plugin_configs', value=new_plugin_str)
            self.node.set_parameters([param])
            self._params['plugin_configs'] = plugin_configs
            self.logger.info(f"配置段热加载已触发: {section}")
            return True
        except Exception as e:
            self.logger.error(f"更新配置段 {section} 失败: {e}")
            return False

    def set_param_and_persist(self, name: str, value: Any) -> bool:
        """设置顶层 ROS2 参数（运行时生效）并持久化到 params.yaml（重启保留）"""
        try:
            if not self.update_param(name, value):
                return False
            path = self._get_params_yaml_path()
            config = self._read_yaml(path)
            app = config.setdefault('app_mgr_object', {}) \
                        .setdefault('ros__parameters', {})
            app[name] = value
            self._backup_and_write_yaml(path, config)
            return True
        except Exception as e:
            self.logger.error(f"参数持久化失败 {name}: {e}")
            return False
```

### 4.3 web_monitor_plugin.py 配置应用

在 Phase 1 新增的采集方法之后追加（`_register_reload_handlers` 在 `_configure_impl` 中被调用）：

```python
    # ═══════════════════════════════════════════════════════
    # v3.0 配置热加载（Phase 3）
    # ═══════════════════════════════════════════════════════

    def _register_reload_handlers(self):
        """配置段 → 内存应用函数 注册表"""
        self._reload_handlers = {
            'bay_configs': self._apply_bay_configs,
            'dest_bay_configs': self._apply_dest_configs,
            'work_schedule': self._apply_work_schedule,
            'pair_overrides': self._apply_pair_overrides,
            'cargo_priority': self._apply_cargo_priority,
        }

    def apply_config(self, section: str, value: Any) -> dict:
        """通用配置热加载入口：校验 → 持久化 → 内存应用 → 广播"""
        try:
            handler = self._reload_handlers.get(section)
            if not handler:
                return {'success': False, 'error': f'不支持的配置段: {section}'}

            error = self._validate_section(section, value)
            if error:
                return {'success': False, 'error': error}

            pm = getattr(self.node, 'param_manager', None)
            if pm is None or not pm.update_plugin_config_section(section, value):
                return {'success': False, 'error': '配置持久化失败（已保留原值）'}

            handler(value)
            self._publish_event('config.updated', {'section': section})
            self._broadcast_message({
                'type': 'config_updated', 'section': section,
                'timestamp': time.time(),
            })
            return {'success': True, 'message': f'配置段 {section} 已更新并热加载',
                    'hot_reload': True}
        except Exception as e:
            self.logger.error(f"apply_config 失败 [{section}]: {e}")
            return {'success': False, 'error': str(e)}

    def _validate_section(self, section: str, value: Any) -> str:
        """配置业务校验，返回错误信息（空串 = 通过）"""
        if section == 'bay_configs':
            if not isinstance(value, dict):
                return 'bay_configs 必须是 {bay_id: cargo_type} 字典'
            for bid, ct in value.items():
                try:
                    if int(ct) not in range(1, 7):
                        return f'{bid}: cargo_type 必须为 1~6，收到 {ct}'
                except (TypeError, ValueError):
                    return f'{bid}: cargo_type 必须为 1~6，收到 {ct}'
            return ''
        if section == 'dest_bay_configs':
            if not isinstance(value, dict):
                return 'dest_bay_configs 必须是 {bay_id: {cargo_type, floor}} 字典'
            for bid, cfg in value.items():
                try:
                    if int(cfg.get('cargo_type', 0)) not in range(1, 7):
                        return f'{bid}: cargo_type 必须为 1~6'
                except (TypeError, ValueError):
                    return f'{bid}: cargo_type 必须为 1~6'
            return ''
        if section == 'work_schedule':
            if not isinstance(value, list):
                return 'work_schedule 必须是列表'
            for s in value:
                if not all(k in s for k in ('day_of_week', 'start_time', 'end_time')):
                    return f'工作时段缺少字段: {s}'
            return ''
        if section == 'pair_overrides':
            if not isinstance(value, dict):
                return 'pair_overrides 必须是 {src_bay: dst_bay} 字典'
            return ''
        if section == 'cargo_priority':
            if not isinstance(value, list) or not value:
                return 'cargo_priority 必须是非空列表'
            return ''
        return ''

    def _apply_bay_configs(self, mappings: dict):
        """内存应用：起始仓位映射（cargo_type）"""
        fusion = self._get_plugin('bay_status_fusion')
        if fusion and fusion.cache:
            fusion.cache.reload_config(mappings)
        trigger = self._get_plugin('smart_trigger')
        if trigger and getattr(trigger, '_config_cache', None):
            trigger._config_cache.set_source_mapping(mappings)
        self.logger.info(f"起始仓位映射热加载: {len(mappings)} 个")

    def _apply_dest_configs(self, mappings: dict):
        """内存应用：终点仓位映射（cargo_type / floor）"""
        poller = self._get_plugin('status_poller')
        if poller and poller.dest_cache:
            poller.dest_cache.reload_config(mappings)
        trigger = self._get_plugin('smart_trigger')
        if trigger and getattr(trigger, '_config_cache', None):
            trigger._config_cache.set_dest_mapping(mappings)
        self.logger.info(f"终点仓位映射热加载: {len(mappings)} 个")

    def _apply_work_schedule(self, schedules: list):
        """内存应用：工作时段"""
        trigger = self._get_plugin('smart_trigger')
        if trigger and trigger._trigger_engine:
            trigger._trigger_engine.set_schedules(schedules)
        if trigger and getattr(trigger, '_config_cache', None):
            trigger._config_cache.set_schedules(schedules)
        self.logger.info(f"工作时段热加载: {len(schedules)} 条")

    def _apply_pair_overrides(self, overrides: dict):
        """内存应用：源→目的显式配对覆盖"""
        trigger = self._get_plugin('smart_trigger')
        if trigger and trigger._trigger_engine:
            trigger._trigger_engine.set_pair_overrides(overrides)
        self.logger.info(f"配对覆盖热加载: {len(overrides)} 条")

    def _apply_cargo_priority(self, priority: list):
        """内存应用：货物类型优先级"""
        trigger = self._get_plugin('smart_trigger')
        if trigger and trigger._trigger_engine:
            trigger._trigger_engine.set_cargo_priority(priority)
        self.logger.info(f"货物优先级热加载: {priority}")
```

### 4.4 配置读取与通用参数（web_monitor_plugin.py）

继续追加：

```python
    # ═══════════════════════════════════════════════════════
    # v3.0 配置读取 / 通用参数（Phase 3）
    # ═══════════════════════════════════════════════════════

    def get_bay_config(self) -> dict:
        fusion = self._get_plugin('bay_status_fusion')
        bays = fusion.cache.get_all_bay_status() if fusion and fusion.cache else {}
        mappings = {bid: b.get('cargo_type', 0) for bid, b in bays.items()}
        return {'mappings': mappings, 'last_modified': time.strftime('%Y-%m-%d %H:%M:%S')}

    def get_dest_config(self) -> dict:
        poller = self._get_plugin('status_poller')
        bays = poller.dest_cache.get_all_bay_status() if poller and poller.dest_cache else {}
        mappings = {bid: {'cargo_type': b.get('cargo_type', 0), 'floor': b.get('floor_no', 0)}
                    for bid, b in bays.items()}
        return {'mappings': mappings, 'last_modified': time.strftime('%Y-%m-%d %H:%M:%S')}

    def get_schedule_config(self) -> dict:
        trigger = self._get_plugin('smart_trigger')
        checker = trigger._trigger_engine.schedule_checker \
            if trigger and trigger._trigger_engine else None
        return {'schedules': list(getattr(checker, '_schedules', []) or [])}

    def get_pair_overrides(self) -> dict:
        trigger = self._get_plugin('smart_trigger')
        matcher = trigger._trigger_engine._batch_matcher \
            if trigger and trigger._trigger_engine else None
        return {'pair_overrides': dict(getattr(matcher, '_pair_overrides', {}) or {})}

    def get_params_config(self) -> dict:
        pm = getattr(self.node, 'param_manager', None)
        if not pm:
            return {'params': {}}
        keys = ['log_level', 'network_timeout', 'network_retries',
                'request_worker_count', 'request_max_queue_size',
                'status_report_interval', 'plugin_auto_start']
        all_params = pm.get_all_params()
        return {'params': {k: all_params.get(k) for k in keys if k in all_params}}

    def apply_params(self, params: dict) -> dict:
        """顶层 ROS2 参数热加载（set + persist）"""
        pm = getattr(self.node, 'param_manager', None)
        if not pm:
            return {'success': False, 'error': '参数管理器未加载'}
        allowed = {'log_level', 'network_timeout', 'network_retries',
                   'request_worker_count', 'request_max_queue_size',
                   'status_report_interval', 'plugin_auto_start'}
        applied = {}
        failed = {}
        for name, value in (params or {}).items():
            if name not in allowed:
                continue
            if pm.set_param_and_persist(name, value):
                applied[name] = value
            else:
                failed[name] = value
        return {'success': not failed, 'applied': applied, 'failed': failed,
                'message': f'已热加载 {len(applied)} 个参数，失败 {len(failed)} 个'}
```

### 4.5 任务人工操作（web_monitor_plugin.py）

继续追加：

```python
    # ═══════════════════════════════════════════════════════
    # v3.0 任务人工操作（Phase 3）
    # ═══════════════════════════════════════════════════════
    def task_control(self, operation: str, task_id: str = '',
                     src_bay: str = '', dst_bay: str = '',
                     robot_id: int = 0, cargo_type: int = 1) -> dict:
        """Web 人工操作：retry / cancel / force_trigger / continue / query"""
        import asyncio
        wf = self._get_plugin('workflow_engine')
        if not wf:
            return {'success': False, 'error': '工作流引擎未加载'}
        engine = wf.get_engine()
        loop = self.node.asyncio_loop

        try:
            if operation == 'query':
                snap = engine.get_instance_snapshot(task_id)
                if snap:
                    return {'success': True, 'task_id': task_id, **snap}
                row = engine.get_task_from_db(task_id)
                if row:
                    return {'success': True, 'task_id': task_id, 'archived': True,
                            'status': row.get('status'),
                            'current_state': row.get('current_state')}
                return {'success': False, 'error': f'任务不存在: {task_id}'}

            if operation in ('retry', 'cancel'):
                event = 'user_retry' if operation == 'retry' else 'user_cancel'
                future = asyncio.run_coroutine_threadsafe(
                    engine.inject_event(task_id, event, {'source': 'web'}), loop)
                future.result(timeout=5)
                return {'success': True, 'task_id': task_id,
                        'operation': operation,
                        'message': f'{operation} 命令已发送: {task_id}'}

            if operation == 'force_trigger':
                if not src_bay or not dst_bay:
                    return {'success': False, 'error': 'force_trigger 需要 src_bay 和 dst_bay'}
                import time as _t
                new_id = f"WEB_{int(_t.time() * 1000)}"
                ctx = {'task_id': new_id, 'src_bay': src_bay, 'dst_bay': dst_bay,
                       'robot_id': robot_id or 0, 'cargo_type': cargo_type or 1}
                future = asyncio.run_coroutine_threadsafe(
                    engine.create_instance('agv_transport_atomic', ctx), loop)
                future.result(timeout=5)
                return {'success': True, 'task_id': new_id,
                        'operation': 'force_trigger'}

            if operation == 'continue':
                snap = engine.get_instance_snapshot(task_id) or {}
                rcs_no = snap.get('rcs_task_no', '')
                if not rcs_no:
                    return {'success': False, 'error': f'无 RCS 任务号: {task_id}'}
                rcs = self._get_plugin('rcs_adapter')
                if not rcs:
                    return {'success': False, 'error': 'RCS 适配器未加载'}
                resp = rcs.continue_task(rcs_no)
                return {'success': resp.get('code') == '0',
                        'message': resp.get('msg', ''), 'task_id': task_id}

            return {'success': False, 'error': f'不支持的操作: {operation}'}
        except Exception as e:
            return {'success': False, 'error': str(e)}
```

### 4.6 仓位配对覆盖（batch_matcher.py）

`__init__` 增加参数：

```python
    def __init__(self,
                 bay_cache,
                 dest_cache,
                 cargo_priority: List[int] = None,
                 logger: logging.Logger = None,
                 pair_overrides: Dict[str, str] = None):
        ...
        self._pair_overrides: Dict[str, str] = dict(pair_overrides or {})
```

新增热加载方法：

```python
    def set_pair_overrides(self, pair_overrides: dict):
        """热加载：源→目的显式配对覆盖（空字典 = 全部按类型贪婪匹配）"""
        self._pair_overrides = dict(pair_overrides or {})

    def set_cargo_priority(self, cargo_priority: list):
        """热加载：货物类型优先级"""
        self._cargo_priority = list(cargo_priority or range(1, 7))
```

`greedy_scan_type` 替换为支持显式配对（原"获取可用起始仓位 → 排序 → 贪婪配对"整体替换）：

```python
    def greedy_scan_type(self,
                         cargo_type: int,
                         exclude_src: set = None,
                         exclude_dst: set = None) -> List[TaskCandidate]:
        """贪婪匹配单个货物类型：显式配对优先，其余 1:1 贪婪配对"""
        exclude_src = exclude_src or set()
        exclude_dst = exclude_dst or set()

        available_src = self._bay_cache.get_bound_bays_of_type(
            cargo_type, exclude_in_task=True)
        available_src = [b for b in available_src if b not in exclude_src]
        empty_dst = self._dest_cache.get_empty_bays_of_type(cargo_type)
        empty_dst = [d for d in empty_dst if d not in exclude_dst]
        if not available_src or not empty_dst:
            return []

        candidates = []
        used_src, used_dst = set(), set()

        # 1) 显式配对覆盖（源→目的，按用户配置）
        for src_bay in list(available_src):
            override_dst = self._pair_overrides.get(src_bay)
            if (override_dst and override_dst in empty_dst
                    and override_dst not in used_dst):
                bay_status = self._bay_cache.get_bay_status(src_bay) or {}
                candidates.append(TaskCandidate(
                    src_bay=src_bay, dst_bay=override_dst,
                    cargo_type=cargo_type,
                    bind_time=bay_status.get('bind_time', 0.0)))
                used_src.add(src_bay)
                used_dst.add(override_dst)

        # 2) 剩余仓位贪婪配对（bind_time 降序 ↔ 终点字典序）
        remaining_src = [s for s in available_src if s not in used_src]
        remaining_dst = [d for d in empty_dst if d not in used_dst]
        src_with_times = []
        for src_bay in remaining_src:
            bay_status = self._bay_cache.get_bay_status(src_bay) or {}
            src_with_times.append((src_bay, bay_status.get('bind_time', 0.0)))
        src_with_times.sort(key=lambda x: (-x[1], x[0]))
        remaining_dst.sort()
        for i in range(min(len(src_with_times), len(remaining_dst))):
            src_bay, bind_time = src_with_times[i]
            dst_bay = remaining_dst[i]
            candidates.append(TaskCandidate(
                src_bay=src_bay, dst_bay=dst_bay,
                cargo_type=cargo_type, bind_time=bind_time))

        if self._logger and candidates:
            self._logger.debug(
                f"贪婪匹配 type={cargo_type}: "
                f"{len(available_src)} src × {len(empty_dst)} dst "
                f"→ {len(candidates)} 配对（含显式 {len(used_src)} 条）")
        return candidates
```

### 4.7 trigger_engine.py 接线

matcher 创建时注入 pair_overrides：

```python
        self._batch_matcher = TaskBatchMatcher(
            bay_cache, dest_cache, self._cargo_priority, logger,
            pair_overrides=_get_config('pair_overrides', {}),
        )
```

新增热加载方法：

```python
    def set_pair_overrides(self, overrides: dict):
        """热加载：显式配对覆盖"""
        self._batch_matcher.set_pair_overrides(overrides)

    def set_cargo_priority(self, priority: list):
        """热加载：货物类型优先级"""
        self._cargo_priority = list(priority or range(1, 7))
        self._batch_matcher.set_cargo_priority(self._cargo_priority)
```

### 4.8 Phase 3 验证

```bash
# 1) 热加载起始仓位映射：A1001 类型 1 → 2
curl -s -X PUT http://127.0.0.1:9183/api/config/bay \
  -H 'Content-Type: application/json' \
  -d '{"mappings": {"A1001": 2}}'
# 期望: {"success": true, "hot_reload": true}

# 2) 验证内存立即生效
curl -s http://127.0.0.1:9183/api/bay/status | python3 -c \
  "import sys,json; d=json.load(sys.stdin); print([b for b in d['bays'] if b['bay_id']=='A1001'])"

# 3) 验证持久化（重启后保留）
ros2 param get /app_mgr_object plugin_configs

# 4) 配对覆盖
curl -s -X PUT http://127.0.0.1:9183/api/config/pairs \
  -H 'Content-Type: application/json' -d '{"mappings": {"A1005": "T3005"}}'

# 5) 顶层参数热加载
curl -s -X PUT http://127.0.0.1:9183/api/config/params \
  -H 'Content-Type: application/json' -d '{"params": {"log_level": "debug"}}'

# 6) 任务人工操作
curl -s -X POST http://127.0.0.1:9183/api/task/control \
  -H 'Content-Type: application/json' \
  -d '{"operation": "query", "task_id": "TASK_xxx"}'
curl -s -X POST http://127.0.0.1:9183/api/task/control \
  -H 'Content-Type: application/json' \
  -d '{"operation": "force_trigger", "src_bay": "A1001", "dst_bay": "T2001"}'

---

## 5. params.yaml 变更

文件：`config/params.yaml`

### 5.1 启用 web_monitor（白名单最后一位）

```yaml
      plugin_load_order: ["network", "callback_handler", "rcs_adapter", "bay_status_fusion",
                        "status_poller", "workflow_engine", "task_status_monitor",
                        "smart_trigger", "web_monitor"]
```

### 5.2 web_monitor 插件配置补充

```yaml
      web_monitor:
        auto_start: true
        config_file: ""                # params.yaml 绝对路径（空=自动探测）
        business_refresh_interval: 3.0 # 业务数据采集间隔（秒，预留）
```

### 5.3 smart_trigger 增加配对覆盖

```yaml
      smart_trigger:
        auto_start: true
        enabled: true
        scan_interval: 2.0
        cargo_priority: [1, 2, 3, 4, 5, 6]
        max_tasks_per_scan: 10
        max_queue_size: 50
        aging_ttl: 300.0
        pair_overrides: {}             # 源→目的显式配对（Web 可编辑，热加载）
```

> 注意：`plugin_load_order` 与 `plugin_configs` 均为 YAML 块字符串，修改后需保证缩进一致；首次启动后可通过 `ros2 param get /app_mgr_object plugin_configs` 校验解析结果。

---

## 6. 编译、重启与整体验证

```bash
# 1) 重新编译
colcon build --packages-select app_mgr_object
source install/setup.bash

# 2) 启动
ros2 launch app_mgr_object app_app.launch.py

# 3) 观察启动日志（期望顺序）
#    ... Web监控插件配置完成 ... v3.0 业务/配置能力已挂载
#    ... 已订阅 7 个业务事件（实时推送）
#    ... Web服务器启动成功

# 4) 端到端验证
curl -s http://127.0.0.1:9183/api/business/summary | python3 -m json.tool
curl -s http://127.0.0.1:9183/api/config/all | python3 -m json.tool
curl -s -X PUT http://127.0.0.1:9183/api/config/bay \
  -H 'Content-Type: application/json' -d '{"mappings": {"A1001": 2}}'
curl -s -X POST http://127.0.0.1:9183/api/task/control \
  -H 'Content-Type: application/json' \
  -d '{"operation": "force_trigger", "src_bay": "A1002", "dst_bay": "T2002"}'

# 5) 回归（26/27 修复）
#    触发任务 → 回调 9 → completed；manual 任务 retry 可用；重启后 MANUAL 恢复
```

---

## 7. 修改文件清单与回滚

### 7.1 文件清单

| 文件 | 操作 | 内容 | 风险 |
|---|---|---|---|
| `config/params.yaml` | 修改 | 启用 web_monitor；pair_overrides；config_file | 低 |
| `plugins/web_monitor_plugin.py` | 修改 | 挂载 + 9 采集方法 + 事件订阅 + reload handlers + 配置读取 + task_control | 中 |
| `components/web/web_server.py` | 修改 | `_setup_business_routes()` +20 端点 | 中 |
| `base/param_manager.py` | 修改 | 回调分发 + YAML 持久化 + 热加载方法 | 中 |
| `components/trigger/batch_matcher.py` | 修改 | pair_overrides + set_* + greedy 改造 | 中 |
| `components/trigger/trigger_engine.py` | 修改 | pair_overrides 注入 + set_* | 低 |

### 7.2 回滚说明

- 实施前对上述 6 个文件保存基线（git 或副本）；
- `param_manager` 写 params.yaml 前自动生成 `.bak`，写坏时 `cp params.yaml.bak params.yaml` 后重启；
- Phase 1/2 可独立合入（只读，无风险）；Phase 3 涉及写路径，先在模拟环境验证；
- 所有新端点依赖 `web_monitor` 插件，未启用时返回 `{"success": false, "error": "web_monitor 未加载"}`，不影响既有功能。

---

## 8. 前端衔接说明（Phase 4 预告）

后端完成后，前端开发直接消费以下契约（无需再改后端）：

| 页面 | 数据源 |
|---|---|
| 仪表盘 `/` | `GET /api/business/summary` + `GET /api/status` + WS `business_update` |
| 仓位 `/bay` | `GET /api/bay/status`、`/api/dest/status` |
| AGV `/agv` | `GET /api/agv/status` |
| 任务 `/task` | `GET /api/task/{queue,instances,stats,history}` + `POST /api/task/control` |
| 配置 `/config` | `GET/PUT /api/config/{all,bay,dest,schedule,pairs,params}` + WS `config_updated` |

前端文件规划（28 号文档第 4 章）：`base.html` 导航 + `index.html` 仪表盘 + `bay.html`/`agv.html`/`task.html`/`config.html` + `static/js/{api,dashboard,bay,task,config}.js` + `static/css/dashboard.css`。
```
```
```
