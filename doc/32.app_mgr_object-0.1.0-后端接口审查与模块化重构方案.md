# app_mgr_object-0.1.0 后端接口审查报告与模块化重构方案

> 文档编号：32
> 目标工程：`app_mgr_object-0.1.0(基础版)`（基于文档 29/30/31 实施后代码）
> 审查范围：`plugins/web_monitor_plugin.py`（1298 行）、`components/web/web_server.py`（1730 行）、`base/param_manager.py`、独立 YAML 配置文件
> 审查目标：① 后端 API 功能完整性 ② web_monitor_plugin 模块化重构 ③ 为前端开发提供坚实后端底座
> 审查日期：2026-08-05

---

## 0. 审查摘要

| 模块 | 状态 | 评价 |
|---|---|---|
| `base/param_manager.py` | ✅ 达标 | 独立 YAML 文件模式、原子写入、深度路径、文件锁均正确实现 |
| `config/` 独立 YAML 文件 | ✅ 达标 | 8 个插件配置 + web_monitor.yaml 已拆分，格式规范 |
| `plugins/web_monitor_plugin.py` | ⚠️ 需重构 | 1298 行承载 6 类职责，可分离 4 个组件模块 |
| `components/web/web_server.py` | ❌ 路由缺失 | **业务/配置/任务 API 路由全部未实现**（文档 29 规格未落地） |

**核心结论**：
- 参数热加载基础设施（YAML 拆分 + 原子写入）已就绪，可直接复用
- web_monitor_plugin 的业务采集/配置管理/任务控制方法**已实现但嵌在插件中**，需提取为独立组件
- web_server.py **缺少 14 个后端路由**（`/api/config/*`、`/api/bay/*`、`/api/task/*` 等），前端无法消费

---

## 1. param_manager.py 审查（通过）

### 1.1 独立 YAML 文件模式 — ✅ 正确实现

```
启动 → _declare_common_parameters()        ← 声明 plugin_configs_dir / web_monitor_config
     → _load_external_configs()            ← 遍历目录加载 8 个 YAML
       → _load_plugin_configs_from_dir()   ← plugin_configs/ → dict
       → _load_config_from_file()          ← web_monitor.yaml → dict
     → _setup_param_callback()             ← ROS2 参数变更回调
```

已验证项：
- ✅ `plugin_load_order` 支持逗号分隔字符串解析（`"a,b,c"` → `["a","b","c"]`）
- ✅ `update_plugin_config_nested` 深度路径导航（list index + dict key）
- ✅ `_get_plugin_config_file` 路径映射（`path[0]` → `plugin_configs/<name>.yaml`）
- ✅ `_write_yaml_atomic` 临时文件 + `os.fsync` + `os.replace` + 回读校验
- ✅ `_yaml_lock` 线程锁防并发写
- ✅ `_deep_merge` 配置合并（default + user）
- ✅ `set_param_and_persist` 双路径（web_monitor → 独立文件，其余 → params.yaml）
- ✅ `_notify_plugin_config_changed` EventBus 广播

### 1.2 小问题（不阻塞）

| 问题 | 位置 | 建议 |
|---|---|---|
| `plugin_load_order` YAML 中为逗号分隔字符串 | `params.yaml` | 建议改为 ROS2 原生数组 `[...]`（但当前兼容性 OK） |
| 旧 `web_monitor` / `plugin_configs` 参数未声明 | `_declare_common_parameters` | 旧代码如有引用会崩溃；确认已全部迁移 |

---

## 2. web_monitor_plugin.py 审查（需重构）

### 2.1 代码分布分析（1298 行 → 6 类职责）

```
web_monitor_plugin.py (1298 行)
├── ① 插件生命周期 (80 行) — __init__ / _configure_impl / _activate_impl / _deactivate_impl / _cleanup_impl
├── ② 网络工具 (30 行) — _get_interface_ip
├── ③ 业务数据采集 (220 行) — get_bay_status / get_dest_status / get_agv_status /
│         get_task_queue_data / get_active_instances / get_task_statistics /
│         get_task_history / collect_business_status
├── ④ 配置热加载 (200 行) — _register_reload_handlers / apply_config / _validate_section /
│         _apply_bay_configs / _apply_dest_configs / _apply_work_schedule /
│         _apply_pair_overrides / _apply_cargo_priority / get_bay_config /
│         get_dest_config / get_schedule_config / get_pair_overrides /
│         get_params_config / apply_params
├── ⑤ 任务人工操作 (80 行) — task_control
├── ⑥ 告警管理 (100 行) — get_alarms / get_alarm_stats / acknowledge_alarm /
│         resolve_alarm / clear_alarms / _handle_new_alarm
├── ⑦ 事件与广播 (100 行) — _subscribe_business_events / _on_business_event /
│         _on_slot_status_changed / _broadcast_message / _broadcast_alarm_update
├── ⑧ 公开 API (120 行) — get_system_status / execute_control_action / get_status
└── ⑨ 组件初始化 (80 行) — _init_components_config / _start_web_server / _start_monitoring
```

**问题**：
- ③④⑤⑥ 四类职责与"插件生命周期管理"无关，属于独立的**业务逻辑模块**
- ③ 业务采集 → 可被多个消费者使用（API 路由 / WebSocket 推送 / 外部查询）
- ④ 配置管理 → 与 param_manager 紧耦合，属于配置层的应用逻辑
- ⑤ 任务控制 → 独立的业务操作模块

### 2.2 应提取为 components/web/ 的模块

| 新文件 | 职责 | 从 web_monitor_plugin 迁移的方法 | 预估行数 |
|---|---|---|---|
| `components/web/business_collector.py` | 业务数据采集 | `get_bay_status`, `get_dest_status`, `get_agv_status`, `get_task_queue_data`, `get_active_instances`, `get_task_statistics`, `get_task_history`, `collect_business_status` | ~220 |
| `components/web/config_manager.py` | 配置热加载管理 | `apply_config`, `_validate_section`, `_apply_bay_configs`, `_apply_dest_configs`, `_apply_work_schedule`, `_apply_pair_overrides`, `_apply_cargo_priority`, `get_bay_config`, `get_dest_config`, `get_schedule_config`, `get_pair_overrides`, `get_params_config`, `apply_params` | ~200 |
| `components/web/task_controller.py` | 任务人工操作 | `task_control` | ~80 |
| `components/web/event_bridge.py` | 事件订阅与广播 | `_subscribe_business_events`, `_on_business_event`, `_on_slot_status_changed`, `_broadcast_message`, `_broadcast_alarm_update` | ~100 |

**重构后 web_monitor_plugin.py**：~400 行（仅为生命周期 + 组件组装 + API 门面）

### 2.3 存储于插件的告警方法

告警方法（`get_alarms`, `get_alarm_stats`, `acknowledge_alarm`, `resolve_alarm`, `clear_alarms`）由于依赖 `self.alarm_manager`（已作为组件存储在插件内），保持在插件中作为门面方法。AlarmManager 本身已在 `components/web/alarm_manager.py` 中。

---

## 3. web_server.py 审查（路由缺失 — 需补齐）

### 3.1 已实现的路由（✅）

```
# 页面
GET  /                          → index.html
GET  /monitor                   → monitor.html
GET  /control                   → control.html

# 系统 API
GET  /api/status                → 系统状态
GET  /api/control/mode          → 运行模式
GET  /api/control/dev_nodes     → 开发节点列表
POST /api/control/start_service → 启动服务
POST /api/control/stop_service  → 停止服务
POST /api/control/restart_service → 重启服务
POST /api/control/execute_command → 执行命令
POST /api/control/system_check  → 系统检查
POST /api/control/clear_cache   → 清理缓存
GET  /api/control/stats         → 操作统计
GET  /api/control/logs          → 操作日志
GET  /api/control/node_config   → 节点配置
GET  /api/control/service_config → 服务配置

# 校准
GET  /api/calibration/status    → 标定状态
POST /api/calibration/...       → 标定操作

# WebSocket
WS   /ws                        → 实时推送
```

### 3.2 缺失的路由（❌ — 文档 29 规格 VS 实际代码）

| 文档 29 规格 | 方法 | 路径 | 状态 | 对应数据源 |
|---|---|---|---|---|
| Phase 2 配置管理 | GET | `/api/config/all` | ❌ 缺失 | `config_manager.get_all_config()` |
| Phase 2 配置管理 | GET | `/api/config/bay` | ❌ 缺失 | `config_manager.get_bay_config()` |
| Phase 2 配置管理 | PUT | `/api/config/bay` | ❌ 缺失 | `config_manager.apply_config('bay_configs', ...)` |
| Phase 2 配置管理 | GET | `/api/config/dest` | ❌ 缺失 | `config_manager.get_dest_config()` |
| Phase 2 配置管理 | PUT | `/api/config/dest` | ❌ 缺失 | `config_manager.apply_config('dest_bay_configs', ...)` |
| Phase 2 配置管理 | GET | `/api/config/schedule` | ❌ 缺失 | `config_manager.get_schedule_config()` |
| Phase 2 配置管理 | PUT | `/api/config/schedule` | ❌ 缺失 | `config_manager.apply_config('work_schedule', ...)` |
| Phase 2 配置管理 | GET | `/api/config/pairs` | ❌ 缺失 | `config_manager.get_pair_overrides()` |
| Phase 2 配置管理 | PUT | `/api/config/pairs` | ❌ 缺失 | `config_manager.apply_config('pair_overrides', ...)` |
| Phase 2 配置管理 | GET | `/api/config/params` | ❌ 缺失 | `config_manager.get_params_config()` |
| Phase 2 配置管理 | PUT | `/api/config/params` | ❌ 缺失 | `config_manager.apply_params(...)` |
| Phase 2 业务数据 | GET | `/api/business/summary` | ❌ 缺失 | `business_collector.collect_business_status()` |
| Phase 2 业务数据 | GET | `/api/bay/status` | ❌ 缺失 | `business_collector.get_bay_status()` |
| Phase 2 业务数据 | GET | `/api/dest/status` | ❌ 缺失 | `business_collector.get_dest_status()` |
| Phase 2 业务数据 | GET | `/api/agv/status` | ❌ 缺失 | `business_collector.get_agv_status()` |
| Phase 2 业务数据 | GET | `/api/task/queue` | ❌ 缺失 | `business_collector.get_task_queue_data()` |
| Phase 2 业务数据 | GET | `/api/task/instances` | ❌ 缺失 | `business_collector.get_active_instances()` |
| Phase 2 业务数据 | GET | `/api/task/stats` | ❌ 缺失 | `business_collector.get_task_statistics()` |
| Phase 2 业务数据 | GET | `/api/task/history` | ❌ 缺失 | `business_collector.get_task_history()` |
| Phase 2 任务操作 | POST | `/api/task/control` | ❌ 缺失 | `task_controller.task_control(...)` |

**结论：14 个路由全部缺失**。前端无法消费任何业务/配置/任务数据。

---

## 4. 重构方案

### 4.1 目标架构

```
components/web/
├── alarm_manager.py          ← 已有，不变
├── control_executor.py       ← 已有，不变
├── image_manager.py          ← 已有，不变
├── status_monitor.py         ← 已有，不变
├── web_server.py             ← 重构：补齐 14 个业务路由
├── business_collector.py     ← 新增：从 web_monitor_plugin 提取
├── config_manager.py         ← 新增：从 web_monitor_plugin 提取
├── task_controller.py        ← 新增：从 web_monitor_plugin 提取
├── event_bridge.py           ← 新增：从 web_monitor_plugin 提取
├── __init__.py               ← 更新：导出新模块
├── static/
└── templates/

plugins/
└── web_monitor_plugin.py     ← 精简为 ~400 行（生命周期 + 组件组装 + 告警门面）
```

### 4.2 职责边界

| 模块 | 职责 | 输入 | 输出 |
|---|---|---|---|
| `business_collector` | 采集业务数据 | node, plugin references | dict |
| `config_manager` | 配置读写 + 热加载 | node.param_manager, plugin references | dict + bool |
| `task_controller` | 任务人工操作 | engine, rcs_adapter, asyncio_loop | dict |
| `event_bridge` | 事件订阅 + WebSocket 广播 | event_bus, web_server.manager | None |
| `web_server` | FastAPI 路由注册 | business_collector, config_manager, task_controller | HTTP 响应 |
| `web_monitor_plugin` | 插件生命周期 + 组件组装 | node, config | 无（门面） |

### 4.3 组件注入方式

`web_monitor_plugin` 在 `_configure_impl` 中组装组件，并将它们挂载到 `self.node`：

```python
# web_monitor_plugin.py — _configure_impl() 中
from ..components.web.business_collector import BusinessCollector
from ..components.web.config_manager import ConfigManager
from ..components.web.task_controller import TaskController
from ..components.web.event_bridge import EventBridge

# 创建组件
self.business_collector = BusinessCollector(self.node, self)
self.config_manager = ConfigManager(self.node)
self.task_controller = TaskController(self.node)

# 挂载到 node（供 web_server 路由访问）
self.node.business_collector = self.business_collector
self.node.config_manager = self.config_manager
self.node.task_controller = self.task_controller

# 事件桥在 _activate_impl 中初始化
self.event_bridge = EventBridge(self.node, self.web_server)
```

---

## 5. 实施步骤

### Phase 1：提取组件模块（约 1 天）

| # | 文件 | 操作 | 内容 |
|---|---|---|---|
| 1.1 | `components/web/business_collector.py` | 新建 | 从 web_monitor_plugin 移动 8 个采集方法，封装为 BusinessCollector 类 |
| 1.2 | `components/web/config_manager.py` | 新建 | 从 web_monitor_plugin 移动 `apply_config`, `_validate_section`, 5 个 `_apply_*`, 5 个 `get_*_config`, `apply_params` |
| 1.3 | `components/web/task_controller.py` | 新建 | 从 web_monitor_plugin 移动 `task_control` |
| 1.4 | `components/web/event_bridge.py` | 新建 | 从 web_monitor_plugin 移动 `_subscribe_business_events`, `_on_business_event`, `_on_slot_status_changed`, `_broadcast_message`, `_broadcast_alarm_update` |
| 1.5 | `plugins/web_monitor_plugin.py` | 修改 | 删除已迁移方法，改为调用组件实例方法；在 `_configure_impl` 中组装组件并挂载到 `node` |
| 1.6 | `components/web/__init__.py` | 更新 | 导出新模块 |

### Phase 2：补齐 web_server 路由（约 1 天）

| # | 操作 | 内容 |
|---|---|---|
| 2.1 | 新增 `_setup_business_routes()` | 14 个路由（配置 11 + 业务 8 + 任务 1），见下方代码 |
| 2.2 | 在 `__init__` 中调用 | `self._setup_business_routes()` 紧跟 `self._setup_routes()` |
| 2.3 | 验证 | `curl /api/bay/status`, `curl -X PUT /api/config/bay`, `curl -X POST /api/task/control` |

### Phase 3：端到端测试（约 0.5 天）

| # | 场景 | 验证 |
|---|---|---|
| 3.1 | GET `/api/config/all` | 返回 bay/dest/schedule/pairs/params 全部配置 |
| 3.2 | PUT `/api/config/bay` | 修改 A1001 → 2，params 文件变更，内存刷新 |
| 3.3 | GET `/api/bay/status` | 返回 24 格仓位状态 |
| 3.4 | GET `/api/agv/status` | 返回 AGV 状态卡片 |
| 3.5 | GET `/api/task/queue` | 返回任务队列 |
| 3.6 | POST `/api/task/control` | force_trigger 生成新任务 |
| 3.7 | WebSocket | 配置修改后收到 `config_updated` 事件 |

---

## 6. 核心代码实现

### 6.1 `components/web/business_collector.py`

```python
#!/usr/bin/env python3
"""
业务数据采集器 — 从各业务插件采集实时数据，供 Web API 和 WebSocket 消费。

依赖：通过 node 获取 bay_status_fusion / status_poller / smart_trigger / workflow_engine 插件实例。
"""

import time
from typing import Dict, Any


class BusinessCollector:
    """业务数据采集器"""

    def __init__(self, node, plugin_host):
        """
        Args:
            node: ROS2 Node（用于获取 _get_plugin 工具方法）
            plugin_host: WebMonitorPlugin 实例（用于 logger 和 _get_plugin）
        """
        self.node = node
        self._host = plugin_host    # 保留引用用于 _get_plugin / logger
        self.logger = plugin_host.logger

    def _get_plugin(self, name: str):
        """安全获取业务插件实例"""
        plugin = getattr(self.node, name, None)
        if plugin is None and hasattr(self.node, 'plugin_manager'):
            plugin = self.node.plugin_manager.get_plugin(name)
        return plugin

    # ==================== 仓位采集 ====================

    def get_bay_status(self) -> dict:
        """采集起始仓位状态（BayCache → 24 格热力图数据）"""
        try:
            fusion = self._get_plugin('bay_status_fusion')
            if not fusion or not fusion.cache:
                return {'bays': [], 'total': 0}
            bays = fusion.cache.get_all_bay_status()
            now = time.time()
            result, by_type = [], {}
            for bid, bay in bays.items():
                ct = bay.get('cargo_type', 0)
                by_type.setdefault(str(ct), {'total': 0, 'bound': 0, 'available': 0})
                by_type[str(ct)]['total'] += 1
                if bay.get('bind_status') == 1:
                    by_type[str(ct)]['bound'] += 1
                    if not bay.get('in_task'):
                        by_type[str(ct)]['available'] += 1
                result.append({
                    'bay_id': bay['bay_id'],
                    'cargo_type': ct,
                    'bind_status': bay.get('bind_status', 0),
                    'bind_status_label': {0: '未绑定', 1: '已绑定', 2: '绑定中'}.get(
                        bay.get('bind_status', 0), '未知'),
                    'ai_detected': bay.get('ai_detected', False),
                    'in_task': bay.get('in_task', False),
                    'bind_time': bay.get('bind_time', 0),
                    'bind_duration': round(now - bay.get('bind_time', now), 1)
                        if bay.get('bind_status') == 1 else 0,
                    'last_seen': bay.get('last_seen', 0),
                })
            return {
                'timestamp': now, 'total': len(result),
                'bound_count': sum(1 for b in result if b['bind_status'] == 1),
                'in_task_count': sum(1 for b in result if b['in_task']),
                'by_type': by_type, 'bays': result,
            }
        except Exception as e:
            self.logger.error(f"采集起始仓位状态失败: {e}")
            return {'bays': [], 'total': 0, 'error': str(e)}

    def get_dest_status(self) -> dict:
        """采集终点仓位状态（DestBayCache → 18 格网格数据）"""
        try:
            poller = self._get_plugin('status_poller')
            if not poller or not poller.dest_cache:
                return {'bays': [], 'total': 0}
            bays = poller.dest_cache.get_all_bay_status()
            result, by_floor = [], {}
            for bid, bay in bays.items():
                floor = bay.get('floor_no', 0)
                by_floor.setdefault(str(floor), {'total': 0, 'empty': 0})
                by_floor[str(floor)]['total'] += 1
                if bay.get('is_empty'):
                    by_floor[str(floor)]['empty'] += 1
                result.append({
                    'bay_id': bay['bay_id'],
                    'cargo_type': bay.get('cargo_type', 0),
                    'floor_no': floor,
                    'is_empty': bay.get('is_empty', True),
                    'update_time': bay.get('update_time', 0),
                })
            return {
                'timestamp': time.time(), 'total': len(result),
                'empty_count': sum(1 for b in result if b['is_empty']),
                'by_floor': by_floor, 'bays': result,
            }
        except Exception as e:
            self.logger.error(f"采集终点仓位状态失败: {e}")
            return {'bays': [], 'total': 0, 'error': str(e)}

    # ==================== AGV 采集 ====================

    def get_agv_status(self) -> dict:
        """采集 AGV 状态（RobotCache → 卡片数据）"""
        try:
            poller = self._get_plugin('status_poller')
            if not poller or not poller.robot_cache:
                return {'robots': [], 'available_count': 0}
            robots = poller.robot_cache.get_all_robot_status()
            result = [{
                'robot_id': rid,
                'status': r.get('status', 'OFFLINE'),
                'battery': r.get('battery', 0),
                'position_code': r.get('position_code', ''),
                'current_task_id': r.get('current_task_id', ''),
                'update_time': r.get('update_time', 0),
            } for rid, r in robots.items()]
            return {
                'timestamp': time.time(),
                'available_count': sum(1 for r in result if r['status'] == 'IDLE'),
                'total': len(result),
                'robots': result,
            }
        except Exception as e:
            self.logger.error(f"采集 AGV 状态失败: {e}")
            return {'robots': [], 'available_count': 0}

    # ==================== 任务采集 ====================

    def get_task_queue_data(self) -> dict:
        """采集任务队列（SmartTrigger._task_queue）"""
        try:
            trigger = self._get_plugin('smart_trigger')
            if not trigger:
                return {'queue': [], 'size': 0}
            stats = trigger._task_queue.get_stats() if hasattr(trigger, '_task_queue') else {}
            return {
                'timestamp': time.time(),
                'size': stats.get('size', 0),
                'queue': stats.get('items', []),
            }
        except Exception as e:
            self.logger.error(f"采集任务队列失败: {e}")
            return {'queue': [], 'size': 0}

    def get_active_instances(self) -> dict:
        """采集活跃工作流实例（WorkflowEngine.get_active_instances）"""
        try:
            wf = self._get_plugin('workflow_engine')
            if not wf:
                return {'instances': [], 'count': 0}
            engine = wf.get_engine()
            instances = engine.get_active_instances() if engine else []
            result = []
            for inst in instances:
                snap = engine.get_instance_snapshot(inst.task_id) if engine else {}
                result.append({
                    'task_id': inst.task_id,
                    'status': getattr(inst, 'status', 'unknown'),
                    'current_state': getattr(inst, 'current_state', ''),
                    'src_bay': snap.get('src_bay', ''),
                    'dst_bay': snap.get('dst_bay', ''),
                    'robot_id': snap.get('robot_id', ''),
                    'created_at': snap.get('created_at', ''),
                })
            return {'timestamp': time.time(), 'instances': result, 'count': len(result)}
        except Exception as e:
            self.logger.error(f"采集活跃实例失败: {e}")
            return {'instances': [], 'count': 0}

    def get_task_statistics(self) -> dict:
        """采集任务统计"""
        try:
            wf = self._get_plugin('workflow_engine')
            if not wf:
                return {'by_status': {}, 'total': 0}
            engine = wf.get_engine()
            if not engine:
                return {'by_status': {}, 'total': 0}
            active = engine.get_active_instances() or []
            by_status = {}
            for inst in active:
                st = getattr(inst, 'status', 'unknown')
                by_status[st] = by_status.get(st, 0) + 1
            return {'timestamp': time.time(), 'by_status': by_status, 'total': len(active)}
        except Exception as e:
            self.logger.error(f"采集任务统计失败: {e}")
            return {'by_status': {}, 'total': 0}

    def get_task_history(self, limit: int = 50, offset: int = 0) -> dict:
        """采集历史任务"""
        try:
            wf = self._get_plugin('workflow_engine')
            if not wf:
                return {'history': [], 'total': 0}
            engine = wf.get_engine()
            if not engine:
                return {'history': [], 'total': 0}
            rows = engine.get_all_instances_from_db(limit=limit, offset=offset) or []
            history = [{
                'task_id': r.get('task_id', ''),
                'status': r.get('status', ''),
                'current_state': r.get('current_state', ''),
                'src_bay': r.get('src_bay', ''),
                'dst_bay': r.get('dst_bay', ''),
                'created_at': r.get('created_at', ''),
                'completed_at': r.get('completed_at', ''),
            } for r in rows]
            return {'timestamp': time.time(), 'history': history, 'total': len(history)}
        except Exception as e:
            self.logger.error(f"采集历史任务失败: {e}")
            return {'history': [], 'total': 0}

    # ==================== 聚合摘要 ====================

    def collect_business_status(self) -> dict:
        """聚合采集所有业务数据，供 `/api/business/summary` 和 WebSocket 推送"""
        return {
            'timestamp': time.time(),
            'bay': self.get_bay_status(),
            'dest': self.get_dest_status(),
            'agv': self.get_agv_status(),
            'task_queue': self.get_task_queue_data(),
            'active_instances': self.get_active_instances(),
            'task_stats': self.get_task_statistics(),
        }
```

### 6.2 `components/web/config_manager.py`

```python
#!/usr/bin/env python3
"""
配置管理器 — 配置读写与热加载应用。

依赖：node.param_manager（用于 YAML 持久化）
"""

import time
from typing import Dict, Any


class ConfigManager:
    """配置热加载管理器"""

    def __init__(self, node):
        self.node = node
        self.logger = node.get_logger()
        self._reload_handlers = {}

    def register_handler(self, section: str, handler: callable):
        """注册热加载处理器"""
        self._reload_handlers[section] = handler

    def apply_config(self, section: str, value: Any) -> dict:
        """Web 配置热加载入口"""
        try:
            handler = self._reload_handlers.get(section)
            if handler:
                error = self._validate_section(section, value)
                if error:
                    return {'success': False, 'error': error}

            path_map = {
                'bay_configs':      ['bay_status_fusion', 'bay_configs'],
                'dest_bay_configs': ['status_poller', 'dest_bay_configs'],
                'work_schedule':    ['smart_trigger', 'work_schedule'],
                'pair_overrides':   ['smart_trigger', 'pair_overrides'],
                'cargo_priority':   ['smart_trigger', 'cargo_priority'],
            }
            path = path_map.get(section, [section])

            pm = getattr(self.node, 'param_manager', None)
            if pm is None:
                return {'success': False, 'error': '参数管理器未就绪'}

            if not pm.update_plugin_config_nested(path, value):
                return {'success': False, 'error': '配置持久化失败'}

            if handler:
                handler(value)

            return {'success': True, 'message': f'配置段 {section} 已更新', 'hot_reload': True}
        except Exception as e:
            self.logger.error(f"apply_config 失败 [{section}]: {e}")
            return {'success': False, 'error': str(e)}

    def _validate_section(self, section: str, value: Any) -> str:
        """配置业务校验，返回错误信息（空串 = 通过）"""
        if section == 'bay_configs':
            if not isinstance(value, dict):
                return 'bay_configs 必须是 {bay_id: cargo_type} 字典'
            for bid, ct in value.items():
                try:
                    if int(ct) not in range(1, 7):
                        return f'{bid}: cargo_type 必须为 1~6，收到 {ct}'
                except (TypeError, ValueError):
                    return f'{bid}: cargo_type 必须为 1~6，收到 {ct}'
            return ''
        if section == 'dest_bay_configs':
            if not isinstance(value, dict):
                return 'dest_bay_configs 必须是 {bay_id: {cargo_type, floor}} 字典'
            for bid, cfg in value.items():
                try:
                    if int(cfg.get('cargo_type', 0)) not in range(1, 7):
                        return f'{bid}: cargo_type 必须为 1~6'
                except (TypeError, ValueError):
                    return f'{bid}: cargo_type 必须为 1~6'
            return ''
        if section == 'work_schedule':
            if not isinstance(value, list):
                return 'work_schedule 必须是列表'
            for s in value:
                if not all(k in s for k in ('day_of_week', 'start_time', 'end_time')):
                    return f'工作时段缺少字段: {s}'
            return ''
        if section == 'pair_overrides':
            if not isinstance(value, dict):
                return 'pair_overrides 必须是 {src_bay: dst_bay} 字典'
            return ''
        if section == 'cargo_priority':
            if not isinstance(value, list) or not value:
                return 'cargo_priority 必须是非空列表'
            return ''
        return ''

    # ==================== 配置读取 ====================

    def get_all_config(self) -> dict:
        return {
            'bay_configs':      self.get_bay_config(),
            'dest_bay_configs': self.get_dest_config(),
            'work_schedule':    self.get_schedule_config(),
            'pair_overrides':   self.get_pair_overrides(),
            'params':           self.get_params_config(),
        }

    def get_bay_config(self) -> dict:
        pm = getattr(self.node, 'param_manager', None)
        pc = (pm._params.get('plugin_configs', {}) if pm else {})
        bay = pc.get('bay_status_fusion', {})
        return {'mappings': bay.get('bay_configs', {}), 'last_modified': time.strftime('%Y-%m-%d %H:%M:%S')}

    def get_dest_config(self) -> dict:
        pm = getattr(self.node, 'param_manager', None)
        pc = (pm._params.get('plugin_configs', {}) if pm else {})
        poller = pc.get('status_poller', {})
        return {'mappings': poller.get('dest_bay_configs', {}), 'last_modified': time.strftime('%Y-%m-%d %H:%M:%S')}

    def get_schedule_config(self) -> dict:
        pm = getattr(self.node, 'param_manager', None)
        pc = (pm._params.get('plugin_configs', {}) if pm else {})
        trigger = pc.get('smart_trigger', {})
        ws = trigger.get('work_schedule', {})
        return {'schedules': ws.get('schedules', [])}

    def get_pair_overrides(self) -> dict:
        pm = getattr(self.node, 'param_manager', None)
        pc = (pm._params.get('plugin_configs', {}) if pm else {})
        trigger = pc.get('smart_trigger', {})
        return {'pair_overrides': trigger.get('pair_overrides', {})}

    def get_params_config(self) -> dict:
        pm = getattr(self.node, 'param_manager', None)
        if not pm:
            return {'params': {}}
        keys = ['log_level', 'network_timeout', 'network_retries',
                'request_worker_count', 'request_max_queue_size',
                'status_report_interval', 'plugin_auto_start']
        all_params = pm.get_all_params()
        return {'params': {k: all_params.get(k) for k in keys if k in all_params}}

    def apply_params(self, params: dict) -> dict:
        pm = getattr(self.node, 'param_manager', None)
        if not pm:
            return {'success': False, 'error': '参数管理器未加载'}
        allowed = {'log_level', 'network_timeout', 'network_retries',
                   'request_worker_count', 'request_max_queue_size',
                   'status_report_interval', 'plugin_auto_start'}
        applied, failed = {}, {}
        for name, value in (params or {}).items():
            if name not in allowed:
                continue
            if pm.set_param_and_persist(name, value):
                applied[name] = value
            else:
                failed[name] = value
        return {'success': not failed, 'applied': applied, 'failed': failed}
```

### 6.3 `components/web/task_controller.py`

```python
#!/usr/bin/env python3
"""
任务控制器 — 任务人工操作（retry / cancel / force_trigger / continue / query）。
"""

import time
import asyncio
from typing import Dict, Any


class TaskController:
    """任务人工操作控制器"""

    def __init__(self, node):
        self.node = node
        self.logger = node.get_logger()

    def _get_plugin(self, name: str):
        plugin = getattr(self.node, name, None)
        if plugin is None and hasattr(self.node, 'plugin_manager'):
            plugin = self.node.plugin_manager.get_plugin(name)
        return plugin

    def task_control(self, operation: str, task_id: str = '',
                     src_bay: str = '', dst_bay: str = '',
                     robot_id: int = 0, cargo_type: int = 1) -> dict:
        """Web 人工操作入口"""
        wf = self._get_plugin('workflow_engine')
        if not wf:
            return {'success': False, 'error': '工作流引擎未加载'}
        engine = wf.get_engine()
        loop = self.node.asyncio_loop

        try:
            if operation == 'query':
                snap = engine.get_instance_snapshot(task_id)
                if snap:
                    return {'success': True, 'task_id': task_id, **snap}
                row = engine.get_task_from_db(task_id)
                if row:
                    return {'success': True, 'task_id': task_id, 'archived': True,
                            'status': row.get('status'),
                            'current_state': row.get('current_state')}
                return {'success': False, 'error': f'任务不存在: {task_id}'}

            if operation in ('retry', 'cancel'):
                event = 'user_retry' if operation == 'retry' else 'user_cancel'
                future = asyncio.run_coroutine_threadsafe(
                    engine.inject_event(task_id, event, {'source': 'web'}), loop)
                future.result(timeout=5)
                return {'success': True, 'task_id': task_id,
                        'operation': operation,
                        'message': f'{operation} 命令已发送: {task_id}'}

            if operation == 'force_trigger':
                if not src_bay or not dst_bay:
                    return {'success': False, 'error': 'force_trigger 需要 src_bay 和 dst_bay'}
                new_id = f"WEB_{int(time.time() * 1000)}"
                ctx = {'task_id': new_id, 'src_bay': src_bay, 'dst_bay': dst_bay,
                       'robot_id': robot_id or 0, 'cargo_type': cargo_type or 1}
                future = asyncio.run_coroutine_threadsafe(
                    engine.create_instance('agv_transport_atomic', ctx), loop)
                future.result(timeout=5)
                return {'success': True, 'task_id': new_id, 'operation': 'force_trigger'}

            if operation == 'continue':
                snap = engine.get_instance_snapshot(task_id) or {}
                rcs_no = snap.get('rcs_task_no', '')
                if not rcs_no:
                    return {'success': False, 'error': f'无 RCS 任务号: {task_id}'}
                rcs = self._get_plugin('rcs_adapter')
                if not rcs:
                    return {'success': False, 'error': 'RCS 适配器未加载'}
                resp = rcs.continue_task(rcs_no)
                return {'success': resp.get('code') == '0',
                        'message': resp.get('msg', ''), 'task_id': task_id}

            return {'success': False, 'error': f'不支持的操作: {operation}'}
        except Exception as e:
            return {'success': False, 'error': str(e)}
```

### 6.4 `components/web/web_server.py` — 补齐路由

在 `WebServer.__init__` 中，紧接 `self._setup_routes()` 后添加：

```python
        # 设置路由
        self._setup_routes()
        
        # ===== 业务/配置/任务路由（v3.0） =====
        self._setup_business_routes()
```

新增 `_setup_business_routes` 方法（追加到 `_setup_routes` 方法之后）：

```python
    def _setup_business_routes(self):
        """业务/配置/任务路由（v3.0）— 从 node 上挂载的组件获取数据"""
        import json as _json

        def _bc():
            """获取 BusinessCollector"""
            return getattr(self.node, 'business_collector', None)

        def _cm():
            """获取 ConfigManager"""
            return getattr(self.node, 'config_manager', None)

        def _tc():
            """获取 TaskController"""
            return getattr(self.node, 'task_controller', None)

        # ==================== 配置管理路由 ====================

        @self.app.get("/api/config/all")
        async def get_config_all():
            cm = _cm()
            if not cm:
                return JSONResponse(status_code=503, content={"success": False, "error": "配置管理器未就绪"})
            return JSONResponse(content={"success": True, "data": cm.get_all_config()})

        @self.app.get("/api/config/bay")
        async def get_bay_config():
            cm = _cm()
            if not cm: return JSONResponse(status_code=503, content={"success": False, "error": "配置管理器未就绪"})
            return JSONResponse(content={"success": True, **cm.get_bay_config()})

        @self.app.put("/api/config/bay")
        async def set_bay_config(request: Request):
            cm = _cm()
            if not cm: return JSONResponse(status_code=503, content={"success": False, "error": "配置管理器未就绪"})
            data = await request.json()
            return JSONResponse(content=cm.apply_config('bay_configs', data.get('mappings', {})))

        @self.app.get("/api/config/dest")
        async def get_dest_config():
            cm = _cm()
            if not cm: return JSONResponse(status_code=503, content={"success": False, "error": "配置管理器未就绪"})
            return JSONResponse(content={"success": True, **cm.get_dest_config()})

        @self.app.put("/api/config/dest")
        async def set_dest_config(request: Request):
            cm = _cm()
            if not cm: return JSONResponse(status_code=503, content={"success": False, "error": "配置管理器未就绪"})
            data = await request.json()
            return JSONResponse(content=cm.apply_config('dest_bay_configs', data.get('mappings', {})))

        @self.app.get("/api/config/schedule")
        async def get_schedule_config():
            cm = _cm()
            if not cm: return JSONResponse(status_code=503, content={"success": False, "error": "配置管理器未就绪"})
            return JSONResponse(content={"success": True, **cm.get_schedule_config()})

        @self.app.put("/api/config/schedule")
        async def set_schedule_config(request: Request):
            cm = _cm()
            if not cm: return JSONResponse(status_code=503, content={"success": False, "error": "配置管理器未就绪"})
            data = await request.json()
            return JSONResponse(content=cm.apply_config('work_schedule', data.get('schedules', [])))

        @self.app.get("/api/config/pairs")
        async def get_pairs_config():
            cm = _cm()
            if not cm: return JSONResponse(status_code=503, content={"success": False, "error": "配置管理器未就绪"})
            return JSONResponse(content={"success": True, **cm.get_pair_overrides()})

        @self.app.put("/api/config/pairs")
        async def set_pairs_config(request: Request):
            cm = _cm()
            if not cm: return JSONResponse(status_code=503, content={"success": False, "error": "配置管理器未就绪"})
            data = await request.json()
            return JSONResponse(content=cm.apply_config('pair_overrides', data.get('mappings', {})))

        @self.app.get("/api/config/params")
        async def get_params_config():
            cm = _cm()
            if not cm: return JSONResponse(status_code=503, content={"success": False, "error": "配置管理器未就绪"})
            return JSONResponse(content={"success": True, **cm.get_params_config()})

        @self.app.put("/api/config/params")
        async def set_params_config(request: Request):
            cm = _cm()
            if not cm: return JSONResponse(status_code=503, content={"success": False, "error": "配置管理器未就绪"})
            data = await request.json()
            return JSONResponse(content=cm.apply_params(data.get('params', {})))

        # ==================== 业务只读路由 ====================

        @self.app.get("/api/business/summary")
        async def get_business_summary():
            bc = _bc()
            if not bc: return JSONResponse(status_code=503, content={"success": False, "error": "业务采集器未就绪"})
            return JSONResponse(content=bc.collect_business_status())

        @self.app.get("/api/bay/status")
        async def get_bay_status():
            bc = _bc()
            if not bc: return JSONResponse(status_code=503, content={"success": False, "error": "业务采集器未就绪"})
            return JSONResponse(content={"success": True, **bc.get_bay_status()})

        @self.app.get("/api/dest/status")
        async def get_dest_status():
            bc = _bc()
            if not bc: return JSONResponse(status_code=503, content={"success": False, "error": "业务采集器未就绪"})
            return JSONResponse(content={"success": True, **bc.get_dest_status()})

        @self.app.get("/api/agv/status")
        async def get_agv_status():
            bc = _bc()
            if not bc: return JSONResponse(status_code=503, content={"success": False, "error": "业务采集器未就绪"})
            return JSONResponse(content={"success": True, **bc.get_agv_status()})

        @self.app.get("/api/task/queue")
        async def get_task_queue():
            bc = _bc()
            if not bc: return JSONResponse(status_code=503, content={"success": False, "error": "业务采集器未就绪"})
            return JSONResponse(content={"success": True, **bc.get_task_queue_data()})

        @self.app.get("/api/task/instances")
        async def get_task_instances():
            bc = _bc()
            if not bc: return JSONResponse(status_code=503, content={"success": False, "error": "业务采集器未就绪"})
            return JSONResponse(content={"success": True, **bc.get_active_instances()})

        @self.app.get("/api/task/stats")
        async def get_task_stats():
            bc = _bc()
            if not bc: return JSONResponse(status_code=503, content={"success": False, "error": "业务采集器未就绪"})
            return JSONResponse(content={"success": True, **bc.get_task_statistics()})

        @self.app.get("/api/task/history")
        async def get_task_history(limit: int = 50, offset: int = 0):
            bc = _bc()
            if not bc: return JSONResponse(status_code=503, content={"success": False, "error": "业务采集器未就绪"})
            return JSONResponse(content={"success": True, **bc.get_task_history(limit=limit, offset=offset)})

        # ==================== 任务人工操作路由 ====================

        @self.app.post("/api/task/control")
        async def task_control_api(request: Request):
            tc = _tc()
            if not tc: return JSONResponse(status_code=503, content={"success": False, "error": "任务控制器未就绪"})
            data = await request.json()
            return JSONResponse(content=tc.task_control(
                operation=data.get('operation', 'query'),
                task_id=data.get('task_id', ''),
                src_bay=data.get('src_bay', ''),
                dst_bay=data.get('dst_bay', ''),
                robot_id=data.get('robot_id', 0),
                cargo_type=data.get('cargo_type', 1),
            ))

        self.logger.info("业务/配置/任务路由注册完成（v3.0）")
```

### 6.5 `plugins/web_monitor_plugin.py` — 精简后的组装代码

```python
# web_monitor_plugin.py — _configure_impl() 末尾（精简后）

from ..components.web.business_collector import BusinessCollector
from ..components.web.config_manager import ConfigManager
from ..components.web.task_controller import TaskController
from ..components.web.event_bridge import EventBridge

def _configure_impl(self):
    # ... 现有配置获取 + 网络接口 IP 获取代码不变 ...
    self._init_components_config()

    # ===== 创建业务组件 =====
    self.business_collector = BusinessCollector(self.node, self)
    self.config_manager = ConfigManager(self.node)
    self.task_controller = TaskController(self.node)

    # ===== 注册配置热加载处理器 =====
    self._register_reload_handlers()      # 内部调用 self.config_manager.register_handler(...)

    # ===== 挂载到 node（供 web_server 路由访问） =====
    self.node.web_monitor = self
    self.node.business_collector = self.business_collector
    self.node.config_manager = self.config_manager
    self.node.task_controller = self.task_controller

    self.logger.info(f"Web监控插件配置完成（组件化模式）...")
    return True


def _register_reload_handlers(self):
    """配置热加载处理器注册"""
    # 获取插件引用用于 _apply_*
    bay_fusion = self._get_plugin('bay_status_fusion')
    poller = self._get_plugin('status_poller')
    trigger = self._get_plugin('smart_trigger')

    self.config_manager.register_handler('bay_configs',
        lambda v: self._apply_bay_configs(v))
    self.config_manager.register_handler('dest_bay_configs',
        lambda v: self._apply_dest_configs(v))
    self.config_manager.register_handler('work_schedule',
        lambda v: self._apply_work_schedule(v))
    self.config_manager.register_handler('pair_overrides',
        lambda v: self._apply_pair_overrides(v))
    self.config_manager.register_handler('cargo_priority',
        lambda v: self._apply_cargo_priority(v))
```

---

## 7. 改动文件清单

| 文件 | 操作 | 行数变化 | 风险 |
|---|---|---|---|
| `components/web/business_collector.py` | 🟢 新建 | +220 | 低（从 plugin 移动） |
| `components/web/config_manager.py` | 🟢 新建 | +200 | 低（从 plugin 移动） |
| `components/web/task_controller.py` | 🟢 新建 | +80 | 低（从 plugin 移动） |
| `components/web/event_bridge.py` | 🟢 新建 | +100 | 低（从 plugin 移动） |
| `components/web/__init__.py` | 🟡 更新 | +4 | 低 |
| `components/web/web_server.py` | 🔴 补充 | +150 | 中（新路由） |
| `plugins/web_monitor_plugin.py` | 🔴 精简 | -600 | 中（删除+重组） |

---

## 8. 前端开发接口契约（供 Phase 4 前端对接）

### 8.1 配置管理

| 接口 | 方法 | 请求体 | 响应 |
|---|---|---|---|
| `/api/config/all` | GET | — | `{success, data: {bay_configs, dest_bay_configs, ...}}` |
| `/api/config/bay` | GET | — | `{success, mappings: {A1001: 1, ...}}` |
| `/api/config/bay` | PUT | `{mappings: {A1001: 2}}` | `{success, message, hot_reload: true}` |
| `/api/config/dest` | GET/PUT | `{mappings: {T2001: {cargo_type: 1, floor: 2}}}` | 同上 |
| `/api/config/schedule` | GET/PUT | `{schedules: [...]}` | 同上 |
| `/api/config/pairs` | GET/PUT | `{mappings: {A1005: "T3005"}}` | 同上 |
| `/api/config/params` | GET/PUT | `{params: {log_level: "debug"}}` | `{success, applied, failed}` |

### 8.2 业务只读

| 接口 | 方法 | 用途 |
|---|---|---|
| `/api/business/summary` | GET | 仪表盘汇总（含 bay/agv/task 全部） |
| `/api/bay/status` | GET | 起始仓位 24 格热力图 |
| `/api/dest/status` | GET | 终点仓位 18 格网格 |
| `/api/agv/status` | GET | AGV 状态卡片 |
| `/api/task/queue` | GET | 任务队列 |
| `/api/task/instances` | GET | 活跃工作流实例 |
| `/api/task/stats` | GET | 任务统计 |
| `/api/task/history` | GET | 历史任务（支持 ?limit=&offset=） |

### 8.3 任务操作

| 接口 | 方法 | 请求体 | 响应 |
|---|---|---|---|
| `/api/task/control` | POST | `{operation, task_id, src_bay, dst_bay, robot_id, cargo_type}` | `{success, message}` |

### 8.4 WebSocket 事件类型

| 事件 type | 触发时机 |
|---|---|
| `business_update` | 仓位变化、AGV 状态变化、任务创建/完成 |
| `config_updated` | 配置热加载成功 |
| `slot_update` | 巷道状态变化 |
| `alarm_update` | 新告警产生 |

---

> **文档结束** — 下一步：Phase 1 提取 4 个组件模块 → Phase 2 补齐 14 个 web_server 路由 → Phase 3 端到端验证 → 交付前端开发。
