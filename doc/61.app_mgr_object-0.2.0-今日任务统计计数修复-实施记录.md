# 61. app_mgr_object-0.2.0 今日任务统计计数修复-实施记录

> 状态：**已实施（doc/60 修订版 P1-P4 完成，含代码审查与测试）**
> 依据：`doc/60.app_mgr_object-0.2.0-业务监控页今日任务统计计数-开发方案.md`（修订版）
> 工程：`app_mgr_object-0.2.0(标准版)`
> 日期：2026-09-01 ｜ 范围：仅修复任务信息卡“今日完成”与“异常”两项统计正常显示；未新增其他统计/前端卡片

---

## 1. 实施概述

| 阶段 | 内容 | 结果 |
|---|---|---|
| P1 | persistence.py：状态兼容常量 + `count_by_date` 支持状态集合（IN 查询） | ✅ |
| P2 | engine.py：`count_today_completed/failed` 透传兼容集合 | ✅ |
| P3 | business_collector.py：回退遍历分支兼容 SUCCESS/FAILED 与 completed/failed | ✅ |
| P4 | 测试：真实 SQLite 计数用例 + 回退兼容用例 | ✅ 10/10 新增 |
| P5 | 验收（板端） | 待现场执行 |

> 前端 `renderTaskStats`（dashboard.js）无需修改：doc/58/59 已实现三态渲染并读取 `today_completed/failed`，后端数据正确后自动正常显示；WS `business_update` 链路已有。

---

## 2. 变更文件清单

| 文件 | 改动 | 验证 |
|---|---|---|
| `app_mgr_object/components/workflow/persistence.py` | 新增 `STATUS_COMPLETED_SET/STATUS_FAILED_SET`；`count_by_date` 参数支持单值/集合并用 `status IN (...)` 查询 | 语法 OK + 单测 PASS |
| `app_mgr_object/components/workflow/engine.py` | import 兼容常量；`count_today_completed/failed` 透传集合 | 语法 OK |
| `app_mgr_object/components/web/business_collector.py` | 回退遍历分支状态兼容 | 语法 OK + 单测 PASS |
| `test/test_persistence_count.py` | **新增**：真实 SQLite 计数测试（5 项） | 5/5 PASS |
| `test/test_business_stats_ready.py` | 扩展：回退兼容计数用例（1 项，共 5 项） | 5/5 PASS |
| `doc/60...开发方案.md` | 更新为修订版（范围收窄说明） | — |
| `doc/61...实施记录.md` | 本文档 | — |

---

## 3. 代码变更说明

### 3.1 persistence.py

```python
# 状态值兼容集合（doc/60）：历史 completed/failed 与现行 SUCCESS/FAILED 并存
STATUS_COMPLETED_SET = ('SUCCESS', 'completed')
STATUS_FAILED_SET = ('FAILED', 'failed')
```

`count_by_date(statuses, date_field, day)`：
- 参数兼容单值字符串或集合（内部归一 tuple）；
- SQL：`WHERE date(?) = ? AND status IN (?,...)`（占位符拼接，无注入）；
- 保留 `date_field` 白名单校验、空集合返回 0、SQL 异常返回 -1（调用方回退）。

### 3.2 engine.py

```python
from .persistence import WorkflowPersistence, STATUS_COMPLETED_SET, STATUS_FAILED_SET

def count_today_completed(self, day):
    return self._persistence.count_by_date(STATUS_COMPLETED_SET, 'completed_at', day)

def count_today_failed(self, day):
    return self._persistence.count_by_date(STATUS_FAILED_SET, 'completed_at', day)
```

### 3.3 business_collector.py（回退遍历分支）

```python
if created.startswith(today) or completed.startswith(today):
    if status in ('SUCCESS', 'completed'):
        today_completed += 1
    elif status in ('FAILED', 'failed'):
        failed += 1
```

---

## 4. 代码审查结论

| 文件 | 审查项 | 结论 |
|---|---|---|
| persistence.py | 状态集合语义、IN 占位符、白名单、单值/空集合/异常分支 | ✅ 通过，向后兼容 |
| engine.py | 导入与透传一致 | ✅ 通过 |
| business_collector.py | 回退分支与 SQL 路径计数语义一致 | ✅ 通过 |
| 前端 | 无需改动（doc/58/59 已三态化） | ✅ 通过 |

**审查发现补充**：`engine.py:245` 的 `terminal = ('completed','failed','cancelled','idle')` 仅用于活跃实例 `executing` 计算（内存对象状态，非 DB status），与本次修复无关，保持原样。

---

## 5. 测试与验证

### 5.1 语法

- 5 个改动/新增 Python 文件 `ast.parse`：**SYNTAX OK**。

### 5.2 单元测试（23/23 PASS）

| 套件 | 结果 |
|---|---|
| `test_persistence_count.py`（新增） | 5/5：今日完成集合计数 / 今日失败集合计数 / 昨日不计 / 单值兼容 / 非法字段与空集合 |
| `test_business_stats_ready.py`（扩展） | 5/5：含回退遍历兼容计数（SUCCESS+completed=2、FAILED+failed=2） |
| `test_trigger_yaml_config.py` | 8/8 无回归 |
| `test_trigger_ghost_lock.py` | 5/5 无回归 |

### 5.3 板端验收项（待现场）

- 任务信息卡“今日完成 / 异常”显示真实计数（非 0）；
- 任务完成/失败后 WebSocket `business_update` 自动刷新（+1）；
- 与 doc/58 的“数据源未就绪”提示可区分。

---

## 6. 修改操作记录

| 序 | 操作 | 文件 | 说明 |
|---|---|---|---|
| 1 | 更新 | `doc/60...开发方案.md` | 范围收窄为仅修复今日完成与异常（修订版） |
| 2 | 修改 | `components/workflow/persistence.py` | 状态兼容常量 + count_by_date 集合查询 |
| 3 | 修改 | `components/workflow/engine.py` | 统计透传兼容集合 |
| 4 | 修改 | `components/web/business_collector.py` | 回退分支状态兼容 |
| 5 | 新增 | `test/test_persistence_count.py` | 真实 SQLite 计数测试（5 项） |
| 6 | 扩展 | `test/test_business_stats_ready.py` | 回退兼容计数用例（+1） |
| 7 | 新增 | `doc/61...实施记录.md` | 本文档 |

---

## 7. 结论

1. “今日完成/异常恒 0”根因（统计状态值与 DB 写入值不一致）已修复：精确 SQL 与回退遍历两条路径均兼容 `SUCCESS/FAILED` 与历史 `completed/failed`；
2. 前端与 WS 链路无需改动，任务信息卡两项统计将自动正常显示；
3. 验证覆盖：语法、新增 10 项测试、既有 13 项回归全部通过；
4. 剩余板端现场验收（§5.3），无遗留代码改动。