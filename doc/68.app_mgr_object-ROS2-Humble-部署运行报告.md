# app_mgr_object 在 Orange Pi AI Pro 上的 ROS2 Humble 部署与运行报告

> 文档编号: 68
> 生成日期: 2026-09-03
> 目标主机: `root@192.168.31.249` (Orange Pi AI Pro, aarch64, Ubuntu 22.04.5)
> 源代码: `E:\develop\ioe-ai\code` (ROS2 ament_python 包 `app_mgr_object`)

---

## 一、结论摘要

| 项目 | 结果 |
|---|---|
| 远程主机能否安装 ROS2 Humble | ✅ 可以。环境完全符合 Humble 要求 (Ubuntu 22.04 Jammy + Python 3.10 + aarch64) |
| ROS2 Humble desktop 是否安装成功 | ✅ 成功。273 个 ROS2 包就绪，`/opt/ros/humble` 可用 |
| 本地代码能否在远程编译运行 | ✅ 可以。经补全缺失模块骨架后，节点成功启动并稳定运行 |
| 节点实际运行状态 | ✅ 运行中。9 个插件全部加载激活，话题 `/dev01_object_status` 订阅正常，Web 服务器监听 `0.0.0.0:8080` |

### 需注意的运行时警告 (非致命)

1. **`cpp_ros2_interfaces` 自定义消息未编译** — `bay_status_fusion_plugin` 回退到通用订阅模式 (订阅 `/dev01_object_status`，类型 `std_msgs/msg/String`)。若需要 `BayStatus`/`BayChannelStatus`/`BayArea` 强类型消息，需单独编译 `cpp_ros2_interfaces` 消息包。
2. **RCS 服务器连接失败** — 配置指向 `http://192.168.31.191:8080`，当前主机无法路由到该地址 (`No route to host`)。这是配置/网络问题，不影响节点本身运行。
3. **`workflow_engine_plugin` / `smart_trigger_plugin` 为骨架实现** — 只满足 import 和生命周期激活，实际工作流编排和触发匹配逻辑未实现。需参照 `doc/` 下相关文档补全。

---

## 二、远程主机环境核查

### 2.1 硬件与系统

```
主机:       Orange Pi AI Pro
架构:       aarch64 (ARM64)
内核:       Linux 5.10.0+ SMP aarch64
系统:       Ubuntu 22.04.5 LTS (Jammy Jellyfish)
CPU 核数:   3
内存:       7.4 GiB 总计 (验证时可用 6.4 GiB)
磁盘:       /dev/root 59G, 已用 19G (33%), 可用 38G
网络接口:   eth0  192.168.31.249/24
```

### 2.2 环境符合度对照

| ROS2 Humble 要求 | 远程主机实际 | 符合? |
|---|---|---|
| 操作系统: Ubuntu 22.04 (Jammy) | Ubuntu 22.04.5 LTS (jammy) | ✅ |
| 架构: amd64 / arm64 | aarch64 (arm64) | ✅ |
| Python: 3.10 | Python 3.10.12 | ✅ |
| 磁盘空间: ≥ 10GB (desktop) | 38GB 可用 | ✅ |
| 网络: 可访问 ROS2 仓库 | 清华 TUNA 镜像可达 (HTTP 200) | ✅ |
| root/sudo 权限 | root 登录 | ✅ |

### 2.3 安装前的缺失项

| 项目 | 状态 | 处理 |
|---|---|---|
| `python3-pip` | 未安装 | 安装时一并装上 |
| `python3-colcon-common-extensions` | 未安装 | 安装时一并装上 |
| `python3-rosdep` | 未安装 | 安装时一并装上 |
| GitHub raw 访问 | 被墙 (curl 卡死) | 改用清华 TUNA ROS2 镜像 + `trusted=yes` 绕过签名 |

---

## 三、ROS2 Humble 安装过程

### 3.1 配置 ROS2 apt 仓库 (清华 TUNA 镜像)

```bash
# 安装基础工具
apt-get update
apt-get install -y curl gnupg2 lsb-release ca-certificates software-properties-common wget

# 配置 ROS2 源 (清华镜像，arm64，trusted=yes 绕过签名验证)
echo "deb [arch=arm64 trusted=yes] https://mirrors.tuna.tsinghua.edu.cn/ros2/ubuntu jammy main" \
  > /etc/apt/sources.list.d/ros2.list

apt-get update
```

> **说明**: 原方案用 `curl https://raw.githubusercontent.com/ros/rosdistro/master/ros.key` 获取 GPG key，但 `raw.githubusercontent.com` 在该主机被墙 (curl 卡死 5+ 分钟)。改用清华 TUNA 镜像 + `trusted=yes` 绕过签名验证。此方案适用于内网测试机；生产环境建议通过代理获取官方 key 并用 `signed-by` 验证。

### 3.2 安装 ROS2 Humble Desktop

```bash
DEBIAN_FRONTEND=noninteractive apt-get install -y \
  ros-humble-desktop \
  ros-dev-tools \
  python3-colcon-common-extensions \
  python3-rosdep \
  python3-argcomplete \
  python3-pip
```

### 3.3 安装结果验证

```
ROS_DISTRO=humble
/opt/ros/humble/setup.bash  存在
ros2 pkg list 总数: 273
已安装 ros-humble-* 包数: 282
ros-humble-desktop 版本: 0.10.0-1jammy.20260805.015616 arm64

关键包就绪检查:
  rclpy: OK
  std_msgs: OK
  std_srvs: OK
  cv_bridge: OK
  ament_index_python: OK

Python: 3.10.12
rclpy import: OK (version: builtin)
```

---

## 四、Python 依赖安装

### 4.1 requirements.txt 依赖

本包的 `requirements.txt` 要求:

```
requests>=2.25.0
aiohttp>=3.8.0
netifaces>=0.11.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pyyaml>=6.0
psutil>=5.9.0
websockets>=12.0
```

### 4.2 安装方式与最终版本

| 包 | 安装方式 | 最终版本 | 备注 |
|---|---|---|---|
| netifaces | apt 系统包 | 0.11.0 | ROS2 常用 |
| psutil | apt 系统包 | 7.2.2 | 进程监控 |
| pyyaml | apt 系统包 | 6.0.3 | 配置解析 |
| requests | apt 系统包 | 2.25.1 | HTTP 客户端 |
| fastapi | pip 强制升级 | 0.141.1 | 原系统版 0.63 不兼容 pydantic v2 |
| pydantic | pip | 2.13.5 | fastapi 0.100+ 依赖 |
| starlette | pip | 1.6.0 | fastapi 底层 |
| uvicorn[standard] | pip | 0.52.4 | ASGI 服务器 |
| aiohttp | pip | 3.14.3 | 异步 HTTP |
| websockets | pip | 16.1.1 | WebSocket |
| jinja2 | pip | 3.1.6 | FastAPI Jinja2Templates 依赖 |
| python-multipart | pip | 0.0.32 | FastAPI form 解析 |

### 4.3 关键踩坑点

1. **fastapi 0.63 + pydantic 2.x 冲突** — 系统 apt 装的是 `python3-fastapi 0.63`，但 pip 升级把 `pydantic` 升到了 v2，导致 `PydanticUserError: Field 'type_' defined on a base class was overridden`。
   - **修复**: `apt remove python3-fastapi python3-pydantic python3-starlette` 后 `pip install fastapi>=0.100`。

2. **numpy 2.x 与系统 cv_bridge 冲突** — pip 升级 opencv 时连带把 numpy 升到 2.2.6，但系统 `cv_bridge` 是用 NumPy 1.x 编译的，报 `_ARRAY_API not found`。
   - **修复**: `pip install "numpy<2"` 降级回 1.26.4，并卸载 pip 装的 `opencv-python-headless 5.0.0`，回到系统自带 `cv2 4.5.4`。

3. **cv2.Mat 在旧 opencv 不存在** — 代码 `image_manager.py` 用了 `Optional[cv2.Mat]` 类型注解，但系统 opencv 4.5.4 没有 `Mat` 类 (新版 opencv 4.5.1+ 才有)。
   - **修复**: 将 `image_manager.py` 第 26、181 行的 `cv2.Mat` 改为 `np.ndarray`，兼容旧 opencv。

---

## 五、源代码补全 (缺失模块骨架)

### 5.1 缺失模块扫描结果

`plugins/__init__.py` 引用了 10 个插件，其中本地源码缺失 2 个:

| 缺失文件 | 引用位置 | 处理 |
|---|---|---|
| `plugins/workflow_engine_plugin.py` | `plugins/__init__.py:16` | 创建骨架 |
| `plugins/smart_trigger_plugin.py` | `plugins/__init__.py:19` | 创建骨架 |

另外代码还引用了 `cpp_ros2_interfaces` 自定义消息包，但该 import 被 try/except 包裹 (`bay_status_fusion_plugin.py:27-36`)，缺失时 `MSG_AVAILABLE=False` 不致命。

### 5.2 创建的骨架文件

#### 5.2.1 `app_mgr_object/components/trigger/__init__.py`

trigger 模块入口，声明子模块。

#### 5.2.2 `app_mgr_object/components/trigger/pairing_policy.py`

配对策略骨架，提供:

- `Lane(lane_id, floor, strategy, bay_ids)` — 巷道对象
- `StrategyPolicy` — 策略枚举 (ANY/FIFO/FILO/LIFO/NEAREST)，含 `parse(str)` 类方法
- `select_slot(strategy, empty_indices)` — 按策略从空位索引列表中选一个

骨架语义:
- `any` / `fifo` / `nearest`: 取第一个 (索引最小)
- `filo` / `lifo`: 取最后一个 (索引最大)
- 空列表返回 `None`

#### 5.2.3 `app_mgr_object/components/trigger/hot_reload.py`

配置热加载骨架，提供:

- `HotReloadRegistry(logger=None)` — 热加载注册器
  - `register(key, validator, applier)` — 注册热加载项
  - `apply(name, cfg)` — 应用配置变更
- `validate_dict(value)` — 校验器，要求值为 dict
- `validate_positive_number(value)` — 校验器，要求值为正数

#### 5.2.4 `app_mgr_object/plugins/workflow_engine_plugin.py`

工作流引擎插件骨架，继承 `BasePlugin`:

- `PLUGIN_NAME = "workflow_engine"`, `PLUGIN_VERSION = "0.1.0"`
- `_configure_impl`: 从配置加载工作流定义 (仅记录，不解析执行)
- `_activate_impl`: 激活 (骨架，无实际编排)
- `_deactivate_impl` / `_cleanup_impl`: 清理状态

#### 5.2.5 `app_mgr_object/plugins/smart_trigger_plugin.py`

智能触发器插件骨架，继承 `BasePlugin`:

- `PLUGIN_NAME = "smart_trigger"`, `PLUGIN_VERSION = "0.1.0"`
- `_configure_impl`: 从配置加载触发规则 (仅记录)
- `_activate_impl`: 激活 (骨架，无实际匹配)
- `_deactivate_impl` / `_cleanup_impl`: 清理状态

### 5.3 骨架实现的功能边界

| 骨架文件 | 已实现 | 未实现 (需后续补全) |
|---|---|---|
| `pairing_policy.py` | 数据结构、parse、select_slot 基本语义 | 真实策略算法 (FIFO 队列、最近邻计算等) |
| `hot_reload.py` | 注册/应用框架、校验器 | 具体业务字段的 applier 逻辑 |
| `workflow_engine_plugin.py` | 生命周期、配置加载 | 工作流编排执行 (节点调度、状态机、条件分支) |
| `smart_trigger_plugin.py` | 生命周期、规则加载 | 触发匹配引擎 (事件订阅、规则求值、动作派发) |

补全时请参照 `doc/` 下相关文档:
- `21_smart_trigger_v1.md` — 智能触发器设计
- `50.app_mgr_object-0.2.0-触发机制全链路分析报告.md` — 触发机制分析
- `9.Phase3-4-工作流引擎与智能触发器插件开发审查报告.md` — 工作流引擎开发

---

## 六、编译与运行

### 6.1 workspace 布局

```
/root/ros2_ws/
├── src/
│   └── app_mgr_object/        ← 本地 E:\develop\ioe-ai\code 上传
│       ├── app_mgr_object/    ← Python 包
│       ├── config/            ← YAML 配置
│       ├── launch/            ← launch 文件
│       ├── package.xml
│       ├── setup.py
│       └── resource/
├── build/
├── install/
└── log/
```

### 6.2 编译命令

```bash
source /opt/ros/humble/setup.bash
cd /root/ros2_ws
colcon build --packages-select app_mgr_object --symlink-install
```

编译结果: 1 包 3.42s 完成，`app_mgr_node` 可执行文件生成于 `install/app_mgr_object/lib/app_mgr_object/app_mgr_node`。

### 6.3 运行命令

```bash
source /opt/ros/humble/setup.bash
source /root/ros2_ws/install/setup.bash

# 方式 1: launch 文件 (推荐，自动加载 params.yaml)
ros2 launch app_mgr_object app_app.launch.py

# 方式 2: 直接运行节点
ros2 run app_mgr_object app_mgr_node
```

---

## 七、运行验证证据

### 7.1 节点活跃性

```
--- 进程树 ---
31767  /usr/bin/python3 /opt/ros/humble/bin/ros2 launch app_mgr_object app_app.launch.py
31769  /usr/bin/python3 /root/ros2_ws/install/app_mgr_object/lib/app_mgr_object/app_mgr_node ...
        (--ros-args -r __node:=app_mgr_object --params-file .../config/params.yaml)

--- ros2 node list ---
/app_mgr_object

--- ros2 topic list ---
/dev01_object_status
/parameter_events
/rosout

--- /dev01_object_status 详情 ---
Type: std_msgs/msg/String
Publisher count: 0
Subscription count: 1

--- ros2 service list (前6) ---
/app_mgr_object/describe_parameters
/app_mgr_object/get_parameter_types
/app_mgr_object/get_parameters
/app_mgr_object/list_parameters
/app_mgr_object/set_parameters
/app_mgr_object/set_parameters_atomically
```

### 7.2 插件加载与激活

日志显示 9 个插件全部发现、加载、激活成功:

```
发现插件: bay_status_fusion (类: BayStatusFusionPlugin)
发现插件: callback_handler (类: CallbackHandlerPlugin)
发现插件: network (类: NetworkPlugin)
发现插件: rcs_adapter (类: RCSAdapterPlugin)
发现插件: smart_trigger (类: SmartTriggerPlugin)
发现插件: status_poller (类: StatusPollerPlugin)
发现插件: task_status_monitor (类: TaskStatusMonitorPlugin)
发现插件: web_monitor (类: WebMonitorPlugin)
发现插件: workflow_engine (类: WorkflowEnginePlugin)
插件发现完成，共 9 个

指定加载顺序: ['network', 'callback_handler', 'rcs_adapter', 'bay_status_fusion',
               'status_poller', 'workflow_engine', 'task_status_monitor',
               'smart_trigger', 'web_monitor']
成功加载 9 个插件
```

### 7.3 资源占用

```
app_mgr_node PID=31769
  PID %CPU %MEM    RSS    VSZ    ELAPSED
31769 21.1  2.4  186952 1963648  00:14

/proc/31769/status:
  State:    S (sleeping)
  VmSize:   1963648 kB  (~1.9 GB 虚拟内存)
  VmRSS:    186952 kB   (~183 MB 物理内存)
  Threads:  26

系统级:
  Mem: 7.4Gi total, 777Mi used, 702Mi free, 5.9Gi buff/cache, 6.4Gi available
  /dev/root 59G, 19G used (33%), 38G avail
```

### 7.4 Web 服务器与端口

```
回调服务器端口 8080 监听:
  LISTEN 0  2048  0.0.0.0:8080  0.0.0.0:*  users:(("app_mgr_node",pid=31769,fd=25))

日志确认:
  [INFO] 启动回调服务器: 0.0.0.0:8080
  [INFO] 回调服务器已启动，监听 0.0.0.0:8080
  [INFO] ✅ Web服务器启动成功
  [INFO] ✅ Web监控插件激活成功（告警模块已启用）
```

### 7.5 已注册的 RCS 回调路由

```
POST /api/callback/agvCallback   → /agvCallback
POST /api/callback/warnCallback  → /warnCallback
POST /api/callback/bindNotify    → /bindNotify
GET  /api/callback/health
绝对路径: /eyeSky/robot/reporter/*
```

---

## 八、已知问题与排查指南

### 8.1 问题: `ModuleNotFoundError: No module named 'cpp_ros2_interfaces'`

**现象**: 日志出现 `[DEBUG] 导入 BayStatus 失败: No module named 'cpp_ros2_interfaces'`

**根因**: `bay_status_fusion_plugin.py` 尝试 import 自定义消息包 `cpp_ros2_interfaces`，但该消息包未编译安装。

**影响**: 非致命。插件回退到通用订阅模式，订阅 `/dev01_object_status` (类型 `std_msgs/msg/String`)，但无法使用强类型的 `BayStatus`/`BayChannelStatus`/`BayArea` 消息。

**解决方案**:
1. 获取 `cpp_ros2_interfaces` 消息包源码 (本地 `E:\develop\ioe-ai\code` 中没有此包)
2. 上传到 `/root/ros2_ws/src/cpp_ros2_interfaces/`
3. `cd /root/ros2_ws && colcon build --packages-select cpp_ros2_interfaces`
4. `source install/setup.bash` 后重新启动节点

### 8.2 问题: RCS 服务器连接失败

**现象**: 日志出现 `RCS 请求失败 [1], 0.5s 后重试: HTTPConnectionPool(host='192.168.31.191', port='8080'): Max retries exceeded ... No route to host`

**根因**: 配置文件 `config/params.yaml` 中 RCS 服务器地址指向 `192.168.31.191:8080`，当前主机 (`192.168.31.249`) 无法路由到该地址。

**影响**: 非致命。节点持续运行，RCS 相关功能 (AGV 状态查询、仓位绑定等) 暂时不可用。

**解决方案**: 修改 `config/params.yaml` 中 RCS 服务器地址为可达地址，或确保 `192.168.31.191` 在同一网段且网络通畅。

### 8.3 问题: 骨架插件功能不完整

**现象**: `workflow_engine` 和 `smart_trigger` 插件能加载激活，但不执行实际业务逻辑。

**根因**: 这两个插件是本轮为让节点启动而创建的骨架实现，只实现了生命周期方法，未实现工作流编排和触发匹配的核心逻辑。

**影响**: 工作流编排和智能触发功能不可用。其他插件 (网络、回调、数据融合、状态轮询、Web 监控、任务监控) 功能正常。

**解决方案**: 参照 `doc/` 下相关设计文档补全核心逻辑:
- `doc/9.Phase3-4-工作流引擎与智能触发器插件开发审查报告.md`
- `doc/21_smart_trigger_v1.md`
- `doc/50.app_mgr_object-0.2.0-触发机制全链路分析报告.md`

### 8.4 通用排查命令

```bash
# 查看 ROS2 环境
source /opt/ros/humble/setup.bash
echo $ROS_DISTRO          # 应为 humble
ros2 pkg list | wc -l     # 应为 273

# 查看节点状态
source /root/ros2_ws/install/setup.bash
ros2 node list
ros2 topic list
ros2 service list

# 查看节点日志
ros2 launch app_mgr_object app_app.launch.py 2>&1 | tee /tmp/launch.log

# 查看进程资源
ps -p $(pgrep -f app_mgr_node) -o pid,pcpu,pmem,rss,vsz,etime,cmd

# 查看端口监听
ss -tlnp | grep 8080

# 停止节点
pkill -f "ros2 launch app_mgr_object"
pkill -f "app_mgr_node"
```

---

## 九、部署 Checklist (可复制执行)

以下命令在 `root@192.168.31.249` 上依次执行，可从零复现整个部署:

```bash
# ════════ 1. 配置 ROS2 仓库 (清华镜像) ════════
apt-get update
apt-get install -y curl gnupg2 lsb-release ca-certificates software-properties-common wget
echo "deb [arch=arm64 trusted=yes] https://mirrors.tuna.tsinghua.edu.cn/ros2/ubuntu jammy main" \
  > /etc/apt/sources.list.d/ros2.list
apt-get update

# ════════ 2. 安装 ROS2 Humble Desktop ════════
DEBIAN_FRONTEND=noninteractive apt-get install -y \
  ros-humble-desktop ros-dev-tools \
  python3-colcon-common-extensions python3-rosdep \
  python3-argcomplete python3-pip

# ════════ 3. 安装 Python 依赖 ════════
pip3 install --break-system-packages \
  "fastapi>=0.100.0" "pydantic>=2.0" "starlette>=0.27" \
  "uvicorn[standard]>=0.23.0" "aiohttp>=3.8.0" \
  "websockets>=12.0" "jinja2>=3.0" "python-multipart>=0.0.6" \
  "pyyaml>=6.0" "psutil>=5.9.0" "netifaces>=0.11.0" "requests>=2.25.0"
# 保持 numpy 1.x 以兼容系统 cv_bridge
pip3 install --break-system-packages "numpy<2"

# ════════ 4. 上传源代码 (在本地 Windows 执行) ════════
# 用 paramiko/sftp 把 E:\develop\ioe-ai\code 上传到
# /root/ros2_ws/src/app_mgr_object/

# ════════ 5. 编译 ════════
source /opt/ros/humble/setup.bash
cd /root/ros2_ws
colcon build --packages-select app_mgr_object --symlink-install

# ════════ 6. 运行 ════════
source /root/ros2_ws/install/setup.bash
ros2 launch app_mgr_object app_app.launch.py
```

---

## 十、附录: 远程主机 SSH 访问方式

由于 Windows OpenSSH 不带 `sshpass`，且 bash 为非交互式无法直接输密码，本部署通过 Python `paramiko` 库实现 SSH 自动化:

```python
import paramiko
c = paramiko.SSHClient()
c.set_missing_host_key_policy(paramiko.AutoAddPolicy())
c.connect("192.168.31.249", username="root", password="Mind@123",
          timeout=15, allow_agent=False, look_for_keys=False)
# 执行命令: c.exec_command(...)
# 上传文件: c.open_sftp().put(local, remote)
```

辅助脚本存放于 `C:\Users\huqi1\.ssh_check\`:
- `ssh_env.py` — 环境核查
- `ssh_install_ros2_v2.py` — ROS2 安装 (镜像版)
- `ssh_pydeps.py` — Python 依赖安装
- `ssh_upload.py` — 源代码上传
- `ssh_colcon.py` — colcon 编译
- `ssh_run_verify.py` — 节点运行验证
- `ssh_final_run.py` — 最终启动验证

---

*文档结束*
